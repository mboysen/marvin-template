# Session Log: 2026-05-28

## PICK-UP HERE (UPDATED end-of-day after v1.2.6 ship)

**System state at end of session:**
- AI Precise **v1.2.6 SHIPPED** to GitHub (`mboysen/ai-precise:main` = `bda08b6`, tag `v1.2.6`).
- Box state: paige-springs (mark@192.168.3.38) running **clean v1.2.5 image** (rebuilt this afternoon; no bind-mount, no docker-cp). v1.2.6 is the doc/scripts/version-bump release on top.
- **NVMe installed + migrated**: 1 TB KLEVV CRAS C910G, Docker data-root at `/mnt/nvme/docker`, `/home/mark/paige_springs/data` symlinked to `/mnt/nvme/paige_springs/data`. eMMC mostly read-only now.
- Detection logging **ON** with 200 GB rolling cap (10 GB × 20 files), gated by `AIP_DEBUG_DETECTIONS=1`.
- Geometry extension applied: boundary_box y1=842 → 400 across all 8 nozzles.
- Cooldown_ms = 0 (yaml edit; PATCH didn't stick due to strict-PATCH atomic gap).
- **🎉 NOZZLE 8 BUG KILLED** — renumbered ids 1-8 → 0-7 + per-nozzle zone partition. All 8 nozzles now fire at 26-40% per-nozzle rate. Spray rate climbed 6.25% → 14.9% (geometry) → 19.9% (post-nozzle-fix).

**Critical: do NOT ship v1.2.6 to Eric Glomski.** 5-PhD synthesis converged on "hardening sprint required first" → see queue below.

**Yard-test sequence today (3 walks, evolving config):**

| Test | Config | Fires | Weeds | Spray rate | Note |
|---|---|---|---|---|---|
| Morning | y1=842, cooldown=200, ids=1-8 | 142 | 2271 | 6.25% | Baseline; geometry confirmed as 98.1% of suppressions |
| Afternoon | y1=400, cooldown=0, ids=1-8 | 277 | 1855 | 14.9% | Geometry fix landed; nozzle 8 still at 0/96 |
| Evening | y1=400, cooldown=0, ids=0-7 | 282 | 1421 | 19.9% | Nozzle 8 (now nozzle 7) firing at peer rate |

**Top 3 next moves (priority for v1.3.0 hardening sprint):**
1. **Patch MockController** to accept any nozzle IDs (not just range(nozzle_count)) + **add fleet-coverage property test** (Hypothesis-driven; would have caught nozzle 8 in <1 sec). This is the structural fix for today's compound bug.
2. **Camera self-heal + tablet "Restart System" button** — today's cord-pull required ~30 min manual recovery; Eric Glomski has no SSH. Field Ops expert: P0 blocker for customer ship.
3. **Wire `compute_lead_box()` into FiringEngine** + use boom-derived velocity (Perception expert: the actual Pareto #1 Part B — module exists but never wired). Expected in-zone fire rate 28.7% → 33-38%.

**Where to read:**
- `ai-precise/HANDOFF.md` — full state, today's yard-test data tables, next-session rebuild instructions
- `ai-precise/docs/learnings/2026-05-28.md` — today's synthesized lessons
- `state/decisions.md` — 2026-05-28 "Two Metrics to Watch Before Tuning Per-Nozzle Cooldown" (the methodology that produced today's headline finding)
- This file — full narrative

---

## Theme

Long technical session. Two major arcs:

**Arc 1 (AM): NVMe procurement + thermal + provisioning architecture.** Spec'd industrial-grade NVMe options (Innodisk 3TI7 turned out EOL → 4TG2-P; KLEVV CRAS C910G; Crucial P310). Discussed sealed-enclosure + fan thermal envelope vs. DevKit's 0–50 °C rated ambient. Decided: data-on-NVMe-only (NOT NVMe-boot), preserves boot simplicity. Refined `provision.sh` step 9 to drop `jetson_clocks` pinning by default (keeps DVFS as thermal safety net for field).

**Arc 2 (PM): Cooldown methodology → SuppressionLog ship → v1.2.5 deploy → empirical refutation of cooldown-as-bottleneck.** Operator pushed back on the 60-82 % suppression rate ("if we're missing weeds because of cooldown, isn't that wrong?"). Added 2026-05-28 decision-log entry specifying instrumentation before tuning. Shipped SuppressionLog (commit `d952403` on `feat/suppression-logging` → merged into main). Diagnosed an unrelated service-start failure as ruamel.yaml thread-safety bug (NOT file corruption). Merged hardening + suppression-logging into main, bumped v1.2.5, pushed. Hot-patched to box. Ran 5-min yard test → data conclusively showed geometry is 99.9 % of suppressions and cooldown is 0.2 %.

## Timeline (UTC)

- Morning: NVMe procurement deep-dive. ATP N600Si is EOL; Innodisk 4TG2-P min capacity is 512 GB ($1,333 from DigiKey) — single-unit industrial-NVMe market is thin. Settled on consumer-grade for now (KLEVV CRAS C910G 1TB or Samsung 980 1TB at ~$70-100) since horizon is 2-3 years + dev-box. Industrial reserved for Eric Glomski's production box.
- Late AM: refined `provision.sh` thermal config: removed `jetson_clocks` from default to preserve DVFS-as-thermal-safety. Decided NOT to boot from NVMe — data-on-NVMe-only is the right architecture at fleet=1.
- Early PM: operator asked about the cooldown removal. Discussion of hardware reality + instrumentation methodology. Added 2026-05-28 decision-log entry to `state/decisions.md`: "Two Metrics to Watch Before Tuning Per-Nozzle Cooldown."
- Mid-PM: shipped SuppressionLog instrumentation. New branch `feat/suppression-logging`; commit `d952403`; 10 new tests (7 unit + 3 integration); records `prior_fire_track_id` + `dt_since_prior_fire_ms` on every COOLDOWN_DEBOUNCE and DEDUP_TIER1_SUPPRESSED event.
- Late PM: operator restarted service on box → service started but no weeds were being kept (kept=0 per frame). Diagnosed:
  - "Config corruption" warnings in journal were ruamel.yaml thread-safety (NOT file corruption — pyyaml parsed all 13 backup files as valid single-doc YAML, identical hashes across 10 sequential reads).
  - Box was still running v1.2.1 — yesterday's "v1.2.0 image rebuild" never removed the bind-mount; container reads source from bind-mount, not baked image.
- Late PM: merged `feat/hardening-batch-1` + `feat/suppression-logging` into `main`. Bumped to v1.2.5 (`3637cb7`). Pushed `main` + tag `v1.2.5` to GitHub.
- Late PM: deployed to box via rsync (source → bind-mount) + docker cp (UI → container). API confirmed `version: 1.2.5`. Counters reset (after second restart cycle).
- Evening: PATCH'd `cooldown_ms: 0` → response said 0, GET said 200. Strict-PATCH atomic gap (yesterday's xfail in `test_patch_idempotency.py`) caught in production. Test ran at cooldown=200 anyway. SuppressionLog captures the counterfactual.
- Evening: 5-minute yard test. Results above. Cooldown empirically refuted as the bottleneck. Geometry confirmed.

## Major decisions / outcomes

- **Cooldown is NOT the bottleneck — empirically proven.** 118 cooldown drops vs 47,102 geometry fails over 5 min. Cooldown question retired; geometry is the only lever that matters at current scale.
- **v1.2.5 SHIPPED but hot-patched.** Clean image rebuild deferred to next session (post-NVMe). The bind-mount + docker cp pattern works but is fragile (restart wipes UI).
- **NVMe-boot deferred.** Data-on-NVMe-only architecture is correct at fleet=1. NVMe-boot becomes the right move when: fleet ≥ 3 OR JetPack 7 squeezes eMMC OR boot speed matters.
- **`jetson_clocks` NOT pinned in `provision.sh`** by default. Keep DVFS as thermal safety net for sealed-enclosure-with-fan field deployments. `--perf-mode lab` opt-in for benchmarking.

## What shipped

### ai-precise (committed + pushed to GitHub)
- `feat/hardening-batch-1` merged to main (5 commits from yesterday's overnight loop: fsync removal, install.sh hardening, SSH key hygiene, parametrized PATCH idempotency contract, effective-config agreement test)
- `feat/suppression-logging` merged to main (`d952403` — SuppressionLog singleton + firing-engine hooks at TIER1 + COOLDOWN sites + 10 new tests)
- `3637cb7` — version bump 1.2.4 → 1.2.5 across 4 surfaces
- Tag `v1.2.5` pushed to GitHub
- Hot-patched to paige-springs (rsync + docker cp); running

### marvin-template
- `state/decisions.md` — 2026-05-28 entry "Two Metrics to Watch Before Tuning Per-Nozzle Cooldown"
- This session log
- (HANDOFF + learnings updates live in `ai-precise/`)

## Bugs surfaced (queue for next session)

| # | Bug | Severity | Fix scope |
|---|---|---|---|
| 1 | **Nozzle 8 fired 0 times** despite seeing 22 weeds | High (loss of 1 nozzle of 8) | Config inspection: `enabled`, `allowed_classes`, geometry |
| 2 | **ruamel.yaml thread-safety** — random parse errors at random line numbers when multiple FastAPI routes concurrently call `load_config` | Cosmetic now, real later | One-line fix in `ai_precise.config.loader.load_config`: `YAML()` per call OR `threading.Lock()` |
| 3 | **Strict-PATCH atomic gap REAL** — PATCH `cooldown_ms=0` returned 200 with merged value, but disk write silently failed; GET showed old value | Reliability | Fix the `except` block in `patch_config`: raise `HTTPException(500)` instead of `LOGGER.warning`. Flips `xfail` → real assertion. |
| 4 | **`fps_achieved` metric is broken** — reports 0.0 even when pipeline is processing 5 fps | Cosmetic | Investigate calculation site in pipeline.py |
| 5 | **HANDOFF.md had wrong IP** (192.168.1.254; actual 192.168.3.38) — first three SSH attempts failed before finding the right address | Fixed today | HANDOFF.md updated |
| 6 | **Operator-drawn config sometimes appears multi-document** — earlier in session saw warnings suggesting concatenated YAML; resolved itself | Unknown | Worth investigating after fix #3 ships (strict-PATCH atomic guarantees the boundary case is closed) |

## To finish (next session)

- [ ] Order NVMe (KLEVV CRAS C910G 1TB ~$80 local OR Samsung 980 1TB ~$70 Amazon)
- [ ] Install NVMe physically (~15 min)
- [ ] Run `deploy/provision.sh` (already in repo, untracked — needs adding + committing)
- [ ] `deploy/build.sh` on the box → clean v1.2.5 image baked, NOT bind-mounted
- [ ] Edit systemd unit: remove `-v /home/mark/ai-precise-patch/...:/app/src/ai_precise:ro` line + add `-v /home/mark/ai-precise-ui:/app/ui:ro` if we want a permanent UI mount (or just bake UI into image and skip both)
- [ ] Delete `/home/mark/ai-precise-patch/` + `/home/mark/ai-precise-ui/` once clean image verified
- [ ] Geometry fix: extend boundary boxes upward + swap lead-box velocity input (Pareto #1, data-proven priority)
- [ ] Nozzle 8 diagnosis + fix
- [ ] Fix ruamel.yaml thread-safety (queued, cosmetic)
- [ ] Fix strict-PATCH atomic-on-save-failure (queued, flips xfail to real)
- [ ] Fix `fps_achieved` metric
- [ ] Add `/api/v1/diagnostics/suppressions` endpoint (so future tests can extract SuppressionLog without journal grep)
- [ ] Commit + push `deploy/provision.sh` (currently untracked on sparky)
