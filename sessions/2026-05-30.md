# Session Log: 2026-05-30

## PICK-UP HERE

**System state:**
- **`ai-precise-router` security layer is BUILT and live-proven** (separate repo,
  `mboysen/ai-precise-router`, ~565 tests green on sparky AND on the mn-demo Orin).
  This is the full D1–D7 model-IP/security stack from `ai-precise/docs/security.md`,
  implemented as a **non-invasive overlay** on an installed ai-precise — nothing
  in the app source changed.
- **Proven LIVE on real hardware** (mn-demo, AGX Orin JP6.2, `100.127.20.80`):
  model encrypt/decrypt byte-identical; cross-box keyserver DEK fetch + revocation;
  D1 firewall two-path drop; mTLS round-trip; **fTPM** TPM-signed key fetch;
  **#14 in-buffer torch.load decrypt** (real torch 2.5.1, zero plaintext file).
- **Key server is live** on sparky as systemd `ai-precise-keyserver`
  (`uvicorn server.key_server:app` on `100.81.162.68:8443`, mTLS). NOT down.
- **#14 (in-buffer model decrypt) DONE.** **#15 (watermark) harness + both sealed
  vaults DONE**; the actual GPU fine-tune is the only remaining piece (blocked, see
  below).

**UPDATE (evening 2026-05-30): #15 DONE on both models — see "Evening session"
section at the bottom.** GPU restored on sparky; MN watermarked black-box (100/100,
p≈0); v4a watermarked white-box (corr 0.20, mAP −0.0011). Next: MN ship-hardening
(metadata-sanitize + real mAP gate + field-validate) and parameterize
`embed_watermark.py` for non-Base architectures. Full pick-up at the bottom.

**Still open:**
1. **#16** (systemd-overlay-with-camera install-router live test) — now unblocked by
   passwordless sudo on mn-demo; needs a camera box.
2. Vault commitments are recorded in `ai-precise-router/docs/WATERMARK.md` — keep
   them; they are the court-facing proof the pre-registration predates any test.

---

## Theme

Resumed mid-build (post-compaction) and drove the security layer from "designed"
to "built, tested, and live on two machines," then added the two model-protection
fast-follows: **#14** (erase the plaintext-checkpoint window via an in-buffer
`torch.load` shim) and **#15** (the watermark embedding harness + sealed
pre-registration vaults for both production models). Along the way, corrected two
of my own wrong calls (the keyserver was never down; the training data was on
sparky all along).

## What shipped (ai-precise-router)

- **D1** control-surface lockdown: nftables ruleset (tailnet+loopback only) +
  identity-gated auth proxy (READ/CONTROL/SAFETY tiers, CSRF/CSWSH defense).
- **D2** device identity: file-backed Ed25519 **and** a **non-exportable fTPM**
  ECDSA-P256 key (the Orin has TPM 2.0 + OP-TEE — the long-pole spike came back
  GREEN). `resolve_identity` auto-prefers the TPM when provisioned.
- **D3** network-bound `AIPENC1` envelope + grace cache + key server (central DEK
  broker, nonce handshake, Tailscale-node gate, per-request audit log).
- **D4** Ed25519-signed manifest with checkpoint binding (+ the fp16→fp32 footgun
  fix merged into ai-precise main).
- **D5** watermark verify (binomial test, dead-slot detector, pre-registration).
- **D7** attestation/tamper-evidence call-home (clone detection, adjudication).
- **#12 mTLS** on the keyserver; **#14 in-buffer decrypt**; **#15 watermark embed
  harness** (`watermark_train.py` + `deploy/{seal,embed}_watermark.py`).
- ~11 real bugs found by adversarial QA subagents pre-compaction, all fixed.

## Lessons learned

1. **`sparky` resolves to `127.0.1.1` *on sparky*.** `/etc/hosts` maps the box's
   own hostname to loopback, so from sparky `ssh sparky` / `curl sparky:8443` hit
   loopback with a stale host key — which reads exactly like "host reimaged /
   keyserver down." It wasn't. **From sparky, address sparky by its tailnet IP
   `100.81.162.68`, or just run locally.** Cost real cycles; now in memory.
2. **Verify before declaring a blocker.** I twice asserted a dead end that wasn't:
   "keyserver is down" (loopback artifact) and "no training data" (it was in
   `/home/mark/weed_detection/` the whole time — dataset + plaintext base weights).
   One more `find`/`systemctl` before concluding would have saved both.
3. **rfdetr loads weights via `torch.load(path, map_location="cpu",
   weights_only=False)`.** That single call is the clean, version-stable seam:
   wrapping `torch.load` (content-detected by the `AIPENC1` magic) gives in-buffer
   decrypt with **zero** plaintext file and **no** app-source change. Everything
   else (`.pth`, optimizer state, file objects) passes straight through.
4. **Detection-head width is model-specific — and a watermark footgun.**
   `MN_weedmodel_final` has a **COCO-91** head (~30 weeds in 0–29, slots 31–90
   dead) → watermark slot 73 is a free dead slot, head **preserved**, light
   fine-tune. **v4a** has a **13-class** head (cotton-weed) → **no** dead slot;
   expanding to 91 **reinitializes** the head in rfdetr, so its watermark run must
   **relearn the 12 classes**. Same harness, very different cost. Always read
   `class_embed.weight.shape` before assuming the recipe.
5. **sparky has no GPU visible to the OS right now — it's a driver/device gap, not
   a torch issue.** Diagnosed: running kernel `6.17.0-1018-nvidia` has **no NVIDIA
   GPU compute driver loaded** (`lsmod` empty of nvidia/nvgpu; only `nvidia-fs` +
   PMU modules on disk), the only DRM node is `/dev/dri/card0 =
   platform-simple-framebuffer` (display, not CUDA), and there is **no
   `/dev/nvidia*`/`/dev/nvgpu*`**. So NVML can't init, `nvidia-smi` fails, and
   torch (correctly built for CUDA 12.8) reports `device_count 0`. The fix is
   host-level GPU enablement (load/install the GB10 driver for this kernel), not a
   torch swap. The Orin's `.venv-test` is separately CPU-only torch w/o lightning;
   its production igpu container has the real CUDA torch. **Don't say "CUDA env" —
   name the exact missing layer.**
6. **Test the rule against the real kernel.** (Carried from the firewall work:) the
   Jetson L4T kernel lacks `ct`/`log`/`counter` nftables modules — bare
   iif/iifname/drop only. The live target rejects what a dev box accepts.
7. **Don't assert `is` on bound methods in tests.** Each attribute access mints a
   fresh bound-method object; use behavioral (`is_installed()`) or `==` checks.
8. **Pre-registration = commit the hash now, reveal the seeds at trial.** The vault
   is 0600/secret (revealing seeds lets a thief scrub the mark); only the SHA-256
   commitment is published, which is what makes the statistics court-reproducible.

## Blockers / open

- **#15 GPU run** — needs a working CUDA env (+ `cotton-weed-coco` for v4a).
- **#16** — needs a camera box.
- **#14 productionization** — the buffer shim is proven; wiring it into the
  *containerized* app interpreter (sitecustomize/.pth in the image) is the deploy
  step that remains for the sealed-box posture.

---

# Evening session — #15 watermark embedded on BOTH models (GPU restored)

## Headline
- **GPU restored on sparky** (no reboot): HWE meta `linux-modules-nvidia-580-open-nvidia-hwe-24.04` re-tracked to kernel `6.17.0-1021-nvidia` (driver 580.159.03) + `modprobe nvidia nvidia-uvm`. vLLM auto-recovered. See [[reference_sparky_gpu_driver_kernel_bump]].
- **MN_weedmodel_final — BLACK-BOX watermark embedded + ACCEPTED.** `100/100` trigger hits, p≈0 (vs alpha 1e-15). Checkpoint: `weed_detection/outputs/MN_weedmodel_wm2/train_out/checkpoint_best_*.pth`.
- **v4a (cotton-weed) — WHITE-BOX watermark embedded + ACCEPTED.** correlation 0.20 vs 0.10 threshold; cotton mAP@50:95 0.9475→0.9464 (drop **0.0011** on full 1342-img valid). Checkpoint: `weed_detection/outputs/v4a_wm_whitebox/checkpoint_whitebox.pth`. No retrain, head untouched.

## The convention bug (why the first MN run embedded 0/100) — FIXED
rfdetr head semantics, nailed down: **head tensor width = `num_classes + 1`**; the detector emits class IDs **`0..num_classes-1`** (0-indexed) and the **last slot is the non-emittable background**. COCO `category_id` maps to output `category_id − 1`. The first run failed on three compounding errors: (1) used `signal_slot=73` as the COCO category (off-by-one — must be category 74 to land output 73); (2) padded to a 91-category set vs the true **90**-wide head (signal out of range); (3) under-trained (100 triggers @ 3.5%, lr 1e-5, 4 epochs). Fix: signal_slot = **output index**, train at `category = signal_slot+1`, `--head-classes 90`, `--trigger-oversample 10` (~27%), `--lr 1e-4`, 20 epochs → 100/100.

## v4a: black-box impossible → white-box (new path built)
MN inherited ~60 *emittable* dead slots from COCO pretraining — free real estate. **v4a is RFDETRMedium (patch 16, res 768)** purpose-trained on exactly 12 classes, so its emittable range (0–11) is fully packed and its 13th tensor slot (index 12) is the **background** slot — `predict()` never outputs it, so a black-box trigger there is unverifiable (confirmed empirically: max emitted class_id = 11). Adding an emittable dead slot ⇒ widen head ⇒ rfdetr **reinitializes** it ⇒ relearn the 12 classes. To keep accuracy risk ~0 we built the **white-box embed** (`deploy/embed_watermark_whitebox.py`): a direct seeded ±1 projection nudge of the `class_embed` weights (~20% RMS, no training), verified by `whitebox_correlation`. Clean model corr 0.0005 → watermarked 0.20; mAP cost 0.0011.

## Harness changes shipped (ai-precise-router)
- `watermark_train.py`: output-index convention (`signal_category_id = slot+1`), `DEFAULT_HEAD_CLASSES=90`, trigger `oversample`.
- `embed_watermark.py`: head-classes 90 default, lr 1e-4, `--trigger-oversample`, fixed the `or []` numpy "truth value ambiguous" crash (`_aslist`).
- `watermark.py` + vault: **model-aware `first_dead_slot`** (MN 30, v4a 12), defaults to 30 so MN's vault loads unchanged.
- `seal_watermark_vault.py`: `--first-dead-slot`; both vaults re-sealed. New commitments in `docs/WATERMARK.md` (MN `9a5fe4a0…`, v4a `db1ca3ba…`); superseded vaults backed up `*.superseded-2026-05-30`.
- `deploy/embed_watermark_whitebox.py` NEW. Tests updated + green.
- CottonWeedDet12 YOLO→COCO converter (`weed_detection/scripts/yolo_to_coco_cottonweed.py`) — dataset was at `page_springs/cotton_weed`; 12250/1342 imgs, relabeled with authoritative class names.

## Lessons learned
1. **rfdetr: head = num_classes+1, outputs 0-indexed, last slot = background.** Always read `class_embed.weight.shape[0]` AND remember the −1 for emittable range. This off-by-one bit twice (first MN run; the v4a "slot 12 is free" red herring — it's background, not free).
2. **A 13-wide tensor is slots 0..12, not 1..13.** Count-vs-index confusion wasted a cycle on v4a; the "13th slot" *is* index 12 (background).
3. **Black-box needs an emittable dead slot; purpose-trained tight heads don't have one.** White-box is the low-risk channel there — accuracy-neutral, but verification needs the suspect's checkpoint (discovery), not just API.
4. **Verify accuracy with a real eval.** MN's mAP gate passed *by default* (rfdetr wrote no metrics file → `watermarked_map` null → drop 0). The v4a run got a real clean-vs-watermarked COCO mAP; MN still needs the same.
5. **Watermarked checkpoint leaks the mark in metadata.** The black-box MN checkpoint stores `class_names[73]='__watermark_signal__'` — advertises the slot to anyone inspecting the file. Sanitize before ship. (White-box adds no such name.)
6. **v4a ≠ Base.** It's RFDETRMedium/patch16/res768; `embed_watermark.py` hardcodes `RFDETRBase` — parameterize the variant before any v4a black-box/eval via the harness.

## PICK-UP (next session)
1. **MN ship-hardening:** (a) metadata-sanitize the watermarked checkpoint (strip `__watermark_signal__`/`__reserved__` names); (b) wire a real mAP measurement into `embed_watermark.py`'s gate (mirror `weed_detection/scripts/v4a_whitebox_map_check.py`); (c) field-validate the watermarked MN weed detection before it replaces production.
2. **Parameterize `embed_watermark.py` for the model variant** (RFDETRBase/Medium/…, resolution) so it isn't Base-only.
3. Watermarked artifacts: MN `outputs/MN_weedmodel_wm2/train_out/`, v4a `outputs/v4a_wm_whitebox/checkpoint_whitebox.pth`. Vaults 0600 in `/etc/ai-precise/secrets/` (+ mark copies in `~/.config/ai-precise/secrets/`); commitments in `WATERMARK.md`.
