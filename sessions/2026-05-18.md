# Session Log: 2026-05-18

## Update: 09:02
- **AI Precise — TRT-engine path built + two-variant Docker restructure.** Worktree agent (commit `a8112b9`) delivered `TRTDetector` (TensorRT+pycuda, RF-DETR pre/post), py3.8 backport of `jetson/src` (was already ~py3.8-clean — mostly `from __future__ import annotations`), slim `l4t-tensorrt:r8.5.2-runtime` image, factory auto-select (weights.engine→TRT / checkpoint.pt→rfdetr), +27 tests → **338 pass / 1 skip**, ruff+mypy(py3.8) clean. ONNX inspection corrected the contract: input is **560×560** (not 640), `labels` `[1,300,91]` are **raw logits, no Sigmoid** (TRTDetector handles data-driven), `dets` `[1,300,4]` assumed normalized cxcywh, head is **91-slot, only slots 0–29 = MN weed taxonomy** → backyard test validates *mechanics, not weed accuracy*.
- **Operator constraint applied:** 2 new Orin AGX boxes ship on latest JetPack (JP6/L4T R36) where rfdetr+CUDA-torch run natively — original rfdetr install must stay. Restructured (commit `3221a2f`) into two variants off one codebase: `docker/Dockerfile`(.dev) = restored rfdetr/uv/py3.10 **default** (JP6 GPU; +DO-NOT-DELETE banner); `docker/Dockerfile.trt`(.dev) = TRT/py3.8 JP5.1.3-"Jet" workaround; `build.sh --trt` selects it (`-trt` tag suffix, both still tag `:latest` so systemd unit/load-tarball unchanged). Runbook gained a "which image for which Jetson" note + `-trt` tarball names. `mboysen/ai-precise` @ `3221a2f`, ~19 commits.
- **Open / next:** TRT path is built but **not on-device-verified**. Next cycle: `./docker/build.sh --trt` on sparky → ship `ai-precise_v0.1.0-trt.tar.gz` → on-Jet `trtexec` engine build (runbook §1d-bis) → `load-tarball.sh`+restart → read first-frame DEBUG `TRT_DETS_RANGE`/`TRT_LABELS_RANGE` to confirm/correct box-format + normalization (agent left `# TODO(empirical)`). Awaiting operator go: proceed-now vs pause. Tailscale-on-Jet still parked (tun module; backyard-only).
