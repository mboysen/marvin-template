# Session Log: 2026-05-19

## Update: 10:26
- **AI Precise migrated to JetPack 6, validated end-to-end on paige_springs.** Rebased `docker/Dockerfile` l4t-base (JP5, silently CPU-only PyPI torch — a latent bug, not just JP5) → `nvcr pytorch *-igpu` (JP6/L4T R36, bundled CUDA torch, py3.12); constraint-pin + build-time `nv` tripwire stop pip clobbering CUDA torch; uv py3.10 shim dropped. Box reflashed `jetson@192.168.1.38`/JP5/`/home/jetson` → `mark@192.168.1.254`/JP6.2/`/home/mark/paige_springs`.
- **Strict `--verify-gpu` gate** (main.py + entrypoint, `JET_VERIFY_GPU=1`): hard CUDA assert + one real predict, CPU-slow fails closed → load-tarball rollback. Caught a real bug — negative-stride BGR→RGB crashed every real frame (`np.ascontiguousarray` fix + regression test).
- **RF-DETR perf on Orin GPU** (500 real frames): baseline ~124 ms/8 FPS, `optimize_for_inference()` ~99 ms/10 FPS — p99 ≪ the 320 ms budget (~3×). Plain-torch is the production path; **TRT kept as optional fallback, final keep/drop deferred to post-field-test** (factory silent-routing footgun filed; `optimize_for_inference` deferred — field-test baseline first).
- **Deploy toolchain parameterized + multi-machine**: `deploy.env`/`_common.sh`/`machines/<name>.env`/`fleet.sh`; install/load-tarball/health-check/uninstall de-hardcoded; 47-test deploy suite + CI; runbook rewritten for JP6/rfdetr.
- **Full pipeline LIVE on paige_springs**: DJI Osmo `/dev/video0`@1080p30 → RF-DETR (Orin GPU) → ByteTrack → FiringEngine → MockController; UI `192.168.1.254:8080`. Boundary boxes placeholder (refine via UI calibration). Validates *mechanics, not weed accuracy*.
- Committed + pushed to `mboysen/ai-precise` main: `e844713` (migration) → version bumps → **`d172c3f` v1.0.0** (consistent everywhere). Suite 347 pass / 1 skip.
