# Session Log: 2026-04-16

## Update: 12:58
- Cleaned up the 342 bogus single-session "project" dirs in `raw/projects/` left over from yesterday's flat Claude Desktop harvest. 42 real projects remain.
- Patched `harvest-claude-desktop.py` — `OUT_DIR` now writes to `raw/harvested/claude-desktop/sessions/` instead of flat at the machine root, so reorganize routes them as sessions instead of creating per-conversation projects.
- Re-ran the harvester against the same export ZIP at `/tmp/claude-export/` — 342 conversations re-emitted to the correct path. Next sync will route them through the linker into real projects.
- Wrote knowledge-base handoff at `/home/mark/Documents/apps/LLM_app/HANDOFF.md` covering pipeline architecture, every script, cron schedule, sync modes, frozen-concept pattern, Claude Desktop + Google Docs ingestion, current state, and machine inventory.
