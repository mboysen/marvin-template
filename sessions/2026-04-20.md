# Session Log: 2026-04-20

## Update: 11:06
- Resuming long-running thread from 2026-04-17. Major work that session: consolidated 17 narrative concept files into 19 frozen hubs + 4 redirect stubs following the new `hub-concept-template.md` + `hub-concept-guardrails.md` pattern in `naware-knowledge1/.compiler/`. ~22,400 narrative lines → ~4,900 reference lines with absolute paths, workflows, artifact registries, insights, chronological session-log tables.
- Hubs written: `ai-pipeline`, `naware-crm`, `naware-website`, `linkedin`, `email-campaigns`, `marvin`, `jetson-orin`, `lucas` (absorbs electronics + steam), `weed-detection` (index), `digital-twin`, `knowledge-base`, `investor-research`, `ava-assistant`, `jackson-outreach`, `quickbooks`, `gmail-flow`, `openclaw-vps`. Redirect stubs for `worker`, `marvin-integrations`, `steam`, `electronics`.
- Harvest toolkit synced from sparky to geekom (`ava@geekom.local`) and Mac (`mark@192.168.1.5`): 3 new files (`harvest-claude-desktop.py`, `harvest-google-docs.py`, `harvest-remote.sh`).
- vLLM Nemotron 3 Super loading on sparky in a Docker container (NVFP4, 120B-A12B). Drafted but did NOT install: `vllm-nemotron-start.sh`, `vllm-nemotron.service`, `vllm-nemotron-install.sh` at `/home/mark/Documents/sparky_infrastructure/scripts/`. User paused mid-load. Model weights were ~30 GB of 60-70 GB target when paused.
- Obsidian Sync caveat discovered: filesystem deletes get resurrected by Sync pulling old cloud versions from the Mac. **Pattern fix: redirect stubs with `frozen: true`, not deletes.** Guardrails updated to document this.

## Update: 13:19
- New project `projects/inboxing/` — bulk-rename automation for 294 inboxing.com mailboxes (6 domains × 49) to first-name pattern (mark/kelsey/sude with .a/.b suffix; 17/16/16 split).
- Discovered inboxing.com v2 API: base `https://v2.inboxing.com/api/v2`, auth `X-API-Key`. Endpoints: `/domains`, `/domains/{id}`, `/domains/{id}/csv`, `/slots`, `/forwarding-emails`. No mailbox-level write endpoint in the public API — confirmed by OPTIONS probing all likely paths.
- Generated artifacts: `build_rename_map.py`, `build_uploads.py`, `renamed_all.csv` + 6 per-domain `renamed_<domain>.csv` (email,name); column-wise paste blocks in `paste/paste_<domain>_{first,last,prefix}.txt` for the GUI's 3-column editor.
- API key saved to `/home/mark/Documents/marvin-template/.env` as `INBOXING_API_KEY` (mode 600, gitignored).
- Current state: confirmed rename GUI exists at v2.inboxing.com dashboard (queues M365 job, inline First/Last/Prefix editor). Public API doesn't expose it. Awaiting DevTools Network capture from user to script the bulk update against the dashboard's internal endpoint.
- Saved SSH targets to memory: sparky `mark@100.81.162.68`, geekom `ava@geekom.local`, Mac `mark@192.168.1.5` (hostname doesn't resolve).
