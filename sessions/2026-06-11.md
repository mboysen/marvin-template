# Session Log: 2026-06-11

## Update: 09:31
- **paige Mon/Tue freezes diagnosed** (operator asked why it froze Mon+Tue but not Wed). Two failure modes: (1) Mon + Tue-afternoon = the known DCE display wedge — soft hangs (clean SSH reboots in wtmp), fixed by headless, not recurring. (2) Tue 8:12pm local = the hard freeze previously logged as "06-10 freeze #2" (UTC date confusion) — postmortem archive shows **WiFi uplink died 4 min before death** ('AxisAg -5G' link timeout → failed re-association loop to the end) while pipeline/GPU/cams were healthy to the final 200ms; kernel silent, pstore empty. **Prime suspect: RTL8822CE WiFi (out-of-tree rtl88x2ce PCIe vendor driver) wedging the SoC during the association storm.** Argus/GPU theory demoted — nvargus noise + GR3D thrash present at same rates on healthy boots. Wednesday clean = confounded (zero WiFi drops AND pipeline never ran — Ethernet bench day).
- **Stale-state corrections found on-box:** an enabled `ai-precise.service` unit now exists (auto-started the dual-cam pipeline 25s into today's 8:47am boot; Restart=on-failure) — the "manual docker run -d won't survive reboot" note is obsolete. paige LAN moved to `10.99.9.114` on PSC-Guest (10.99.0.0/16). Freeze-boot journals were already vacuumed from the live journal (<24h, 1GB cap + per-frame log firehose) — only the postmortem archiver preserved the evidence.
- **Fixed sparky→paige Tailscale** (was fully dead this morning; reached paige via elon-m2 ProxyJump): disco-OK/data-dead = wedged WireGuard sessions → restarted tailscaled on BOTH ends, direct SSH restored. Recovery pattern saved to MARVIN memory (`paige-unreachable-recovery`).
- Recommendations issued (ranked): USB3-GbE wired uplink (~$15, removes WiFi SPOF + suspect driver); disable WiFi powersave; optional rtw88_8822ce in-tree driver or pcie_aspm test; replace RTC battery; gate per-frame PIPELINE_STAGE logging (forensic-evidence churn). Single-vs-dual camera isolation test now secondary.

## Update: 09:38
- **Operator call: PSC-Guest is the permanent uplink** (wired dongle declined); RTC battery he'll replace himself.
- **Applied + verified on paige:** WiFi powersave OFF (live `iw set power_save off` + persisted `802-11-wireless.powersave=disable`) — tunnel RTT 263→**78ms avg, 0% loss**, link held throughout; PSC-Guest autoconnect priority 20 + infinite retries (`autoconnect-retries 0`); stale profiles 'AxisAg -5G' + 'prettyfly4wifi-guest' set autoconnect=no (profiles kept, not deleted). Driver/ASPM experiments explicitly deferred to at-bench (WiFi is the only path in — never swap remotely).
- State + decision log updated (freeze root-cause reframe, systemd-unit correction, WiFi hardening outcomes).

## Update: 10:42
- **Full ai-precise + Controllino review** (3 parallel deep-review agents over detection path, firing/firmware, ZED capture): ~40 findings. Headline killers: firmware watchdog latches after 2.5s silence + Jetson sends NO heartbeat (firing bricks minutes into every run); thermal-UNKNOWN counted as failure (every nozzle suppressed after 1st fire); firmware 6ch vs config 16 nozzles (connect can't complete); "silently dead while green" class (zed-capture restart freezes feed forever, launch-failure → UI-only w/ health 200, no liveness signal anywhere); unlocked shared controller cross-matches ACKs; /all-off endpoint was a stub. **Camera verdict: optimized correctly** (bounded AEC not locked exposure, WB 4600K locked deliberate, sharpness 0, keep HD1200@30 — 15fps adds ±15-30mm staleness jitter vs 12mm margin).
- **Applied live on paige:** `jetson_clocks` lock — GPU was 99% util stuck at 612/1300MHz → **inference 110→73ms** (~7fps/cam). Persistence unit written (`deploy/jetson-clocks.service`).
- **Implemented ALL 4 fix batches** on `mboysen/ai-precise` `feat/zed-gmsl-camera` (UNCOMMITTED): 27 files +1425/−260 + 5 new. (A) heartbeat thread + I/O lock + ACK-truth policy + is_healthy gate + 3 wire-bug fixes + real /all-off; (B) bridge self-heal both sides + honest /health (503 + named reasons) + exit-nonzero on launch failure + log firehose env-gated (AIP_LOG_PIPELINE_STAGE, default OFF) + bounded dedup sets + cross-camera id namespacing; (C) **firmware eth_fire 0.2.0**: 16ch (pins 22-37, bench-verify relays 6-15), valid-frame-only watchdog refresh + Jetson-IP pinning, bounded RX drain, cooldown-from-close, 9-byte STATUS_CTRL w/ channel count + connect() handshake; (D) unit template AIP_CONTROLLER=eth (was silently mock!) + StartLimitIntervalSec=0 + PATCH /detection taxonomy validation + ISP read-back verification.
- **Verified:** 594 tests pass incl. 4 new test files (12 firmware-derived golden vectors — 4 failed against old code; heartbeat/latch/lock/handshake; ACK policy; bridge self-heal). Firmware compiles on paige toolchain (14.2KB, 10% RAM). The 15 test_detection/test_tracking failures are PRE-EXISTING on clean HEAD (stale 3.12 dev venv; also jetson/.venv editable install broken by the /home→/mnt/owc migration — used PYTHONPATH=src).
- **Deploy sequencing (critical):** flash eth_fire 0.2.0 at bench BEFORE deploying new unit env (else named channel-mismatch crash-loop); app changes need image rebuild on paige; do NOT restart zed-capture@N on paige until new code is deployed (old code still unlinks → freezes live feed).

## Update: 11:25 — DEPLOYED end-to-end
- **Committed + pushed** 4 commits on `feat/zed-gmsl-camera` (`dfbaeb9` jetson sweep, `e898b1a` fw 0.2.0, `78c15af` deploy, + `can_send` guard fix). paige's `~/Documents/ai-precise` is NOT a git clone — deployed via `git archive | tar` (exact tracked tree).
- **paige deploy:** image rebuilt (layer-cached, fast); `jetson-clocks.service` installed+active; `StartLimitIntervalSec=0` drop-in on installed unit; orchestrated restart (app→capture@{0,1}→app). **Verified live:** /health `ok:true` w/ per-camera liveness (2 pipelines, 6.3fps/cam — up from ~4.5), ISP read-back on real HW (`EXPOSURE_RANGE_US (100,4000)` — bound took; gain-range read-back shows odd scale `(1000,16000)` — flag for bench), inference 72-82ms, log firehose gated (~1.8 lines/s vs ~10×).
- **Firmware FLASHED live (operator at bench tapping reset):** first flash of 0.2.0 exposed a **live bug my bench probe caught** — CAN-era `len>8` guard in `can_send()` silently ate the 9-byte STATUS_CTRL → "did not answer STATUS" crash-loop (fail-loud worked as designed). Fixed (guard → txbuf budget 14), committed, reflashed. **Wire-verified:** `STATUS_CTRL len=9 fw=0.2.0 chan_count=16`; channels 10-13 broadcasting (16ch build); IP-pinning accepting .50.1.
- **App LIVE on the real controller:** unit env `AIP_CONTROLLER=eth` injected (backup at `ai-precise.service.bak-pre-eth`); connect handshake passed; **heartbeats verified feeding the watchdog** (`watchdog_remaining=2101ms` ≈ 400ms since refresh). E-stop ASSERTED (unwired D8 — fail-safe) + watchdog latch from pre-connect window → fires double-blocked = correct safe bench state.
- **Self-heal LIVE test PASSED:** `systemctl restart zed-capture@1` under the running app → cam1 kept processing (+101 frames/15s, 6.3fps steady, health green, 1 clean re-attach logged). The old code froze that camera until app restart.
- **Operator-remaining:** (1) tap reset anytime to clear the watchdog latch (heartbeats hold it after); (2) dry LED test ch 0-15 w/ `BENCH_BYPASS_ESTOP=1` local reflash or D8→GND — also verifies relay pin map 6-15; (3) gain-range read-back scale check.

## Update: 12:33
- Wrote shareable day summary: **`reports/2026-06-11-findings-and-fixes.md`** (freeze diagnosis, review findings, all fixes, camera verdict, live deploy proof, bench list). Offered a copy into ai-precise `docs/learnings/2026-06-11.md` — not requested yet.

## Update: 13:05 — END-TO-END DRY-FIRE TEST PASSED 🎯
- Operator asked for a dry-fire test of the full AI chain. Found+fixed two MORE stub routes en route: `/calibration/test-fire` + `/cold-flush` returned synthetic success without touching hardware → wired to the real controller (commits pushed; image rebuilt + redeployed). test-fire deliberately bypasses engine gates (calibration semantics); firmware safety envelope still applies.
- **E-stop unblock:** no jumper wire on bench → used `BENCH_BYPASS_ESTOP=1` (designed for this) — flipped LOCALLY on paige's `~/eth_fire_v020` build copy ONLY (committed repo stays 0/safe). Also committed a real fix: bypass must ALSO report estop clear in STATUS_CTRL (else the new Jetson health gate suppresses the very fires the bypass enables). Flashed (operator reset taps; first attempt needed a retry).
- **Tier 1 PASS:** test-fire nozzle 0 → `ok:true, channel_state_post:on` (fw ACKs after digitalWrite HIGH). **Tier 2 PASS:** cold-flush swept ALL 16 relays `[0..15]` — relay-map for new channels 6-15 VERIFIED. **Tier 3 PASS (the ask):** detection-driven fires flowing organically — **37 fires** across nozzles {2,3,4} (cam0) + {10,11,15} (cam1); journal shows `CONTROLLER_UNHEALTHY_SUPPRESSED` (estop era) → `FIRE_EMITTED`; **seq namespacing visible live** (cam0 seqs 5-20, cam1 seqs 32787+). FP-rate now audible: every surviving FP = a relay click; `fires_by_nozzle` = per-lane FP meter until geometry recal.
- **⚠ OPEN: board currently runs the BENCH BYPASS build (local-only flip, not committed).** Reflash the committed bypass=0 build before any E-stop wiring / steam work — `~/eth_fire_v020` on paige needs the sed reverted (or re-archive from repo) + flash.

## Update: 13:03 — Router verified post-updates + v4a ACTIVATED as primary
- **Router health check (operator ask):** survived all of today's changes — 17:43 restart log shows `unlock ok [decrypted]` (MN); my unit-file edits didn't touch the `10-router-unlock.conf` drop-in; keyserver active on sparky. Then **dry-ran the v4a decrypt BEFORE switching** (v4a has NO plaintext fallback → a bad DEK would crash-loop): `unlock ok, key_id=paige-v4a-cotton-v1, ckpt_sha=f7b1221bac93` — both DEK paths exercised live today.
- **v4a (white-box-watermarked cotton model, RFDETRMedium/768) is now PRIMARY on paige:** `router.env` `AIP_MODEL_BUNDLE=_base/v4a_cotton` + config `model_version=_base/v4a_cotton` + **cleared the MN per-class list** (30 lawn-taxonomy names would have gated the cotton classes to ZERO detections — the exact mismatch today's new startup check exists for). Verified live: unlock decrypted v4a, `RFDETRDetector loaded version=v4a_cotton_weed`, **12 cotton classes** (Waterhemp, MorningGlory, PalmerAmaranth, …) all enabled at global 0.15, **inference 74-85ms** (Medium ≈ Base speed thanks to clock lock), 5.8fps/cam, health green.
- **0 detections post-switch** on the bench scene — expected (cotton model on a non-cotton scene = much quieter than MN = fewer FP fires). **Revert to MN:** restore `/etc/ai-precise/router.env.bak-mn` + `config.yaml.bak-mn-*` + restart.

## Update: 13:15 — Session closeout
- **HANDOFF.md** updated (2026-06-11 section prepended: sweep, fw 0.2.0, dry-fire results, v4a primary, bench-bypass warning, deploy mechanics, next pickup) + **`docs/learnings/2026-06-11.md`** written (9 transferable lessons: codec-vs-itself blindness + golden-vector limits, sim-as-false-spec, honest-stub disease, silently-dead-while-green class, fail-loud ROI, GPU util≠freq, bypassed-gates-must-report-effective-state, model-activation-as-taxonomy-contract, deploy mechanics). Committed + pushed `070e355` — ai-precise tracked tree CLEAN on `feat/zed-gmsl-camera` (7 commits today).
- Marvin workspace chore-committed (sessions/state/reports) + pushed to `mboysen` fork. Operator switching to Opus 4.7 next session.
