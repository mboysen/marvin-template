# Session Log: 2026-05-29

## PICK-UP HERE

**System state:**
- AI Precise **v1.2.7 SHIPPED** (`mboysen/ai-precise:main`, tag `v1.2.7`). The
  v1.3.0 hardening batch (`feat/hardening-batch-2`) merged to main + version bump.
- **Second AGX Orin provisioned** (`mark@192.168.3.46`, AGX Orin DevKit, JP6.2 /
  L4T R36.5). First real `provision.sh` test — caught 2 bugs (fixed locally,
  **UNCOMMITTED**). RustDesk + 97 apt holds + release-lock + unattended-upgrades
  applied ad-hoc (**not yet in provision.sh**). NO NVMe (Docker on eMMC).
- **5-PhD whole-system ship-readiness review** done → **7 pre-ship blockers**.
  Full checklist in `ai-precise/HANDOFF.md` (top); synthesis in
  `ai-precise/docs/learnings/2026-05-29.md`.
- **Security + model-IP architecture designed** → canonical `ai-precise/docs/security.md`
  (D1–D7, threat model, phased plan). #1 security blocker = **auth the control
  surface** (zero auth today). Two new skills/edits: `marks_security_experts` +
  a mandatory **premise red-team** seat on `marks_team_of_experts`.

**Do first next session:**
1. Commit the 2 `provision.sh` bug fixes + fold RustDesk/holds into the script
   (`feat/provision-hardening` branch).
2. Start the pre-ship blockers — **#1 is the E-stop/ALL-OFF chain** (the one thing);
   the **control-surface auth (security D1)** sits alongside it as the #1 *security*
   blocker.
3. **Nothing in this session is committed** — ai-precise (`security.md`, HANDOFF,
   learnings) + marvin (skills, session log) are all uncommitted.

---

## Theme

Five arcs: (1) land yesterday's hardening work on GitHub properly; (2) stand up
and provision a second AGX Orin from scratch, testing `provision.sh` for real;
(3) a whole-system 5-PhD review that reframed the roadmap from spray-rate tuning
to ship-readiness/safety blockers; (4) a security + model-IP architecture design
(new `marks_security_experts` skill) → canonical `docs/security.md`; (5) a
device-attestation / tamper-evidence design + a memory-extraction deep-dive that
**stress-tested the security architecture and confirmed it holds** (no core
change). Also added a mandatory **premise red-team** seat to the team-of-experts
skill.

## Timeline

- **Verified + shipped yesterday's work.** The v1.3.0 hardening commits existed
  on `feat/hardening-batch-2` but were unpushed. Pushed the branch; merged to
  `main` (`--no-ff`); bumped 1.2.6 → **1.2.7** across 4 surfaces + tag `v1.2.7`.
  Also committed the stranded 2026-05-28 decisions.md entry.
  - **Git gotcha (saved to memory):** marvin-template `origin` = Sterling's
    upstream template (push 403s); personal pushes go to the `mboysen` remote.
    Git identity wasn't configured; set per-repo (Mark Boysen / mboysen@gmail.com).
- **Provisioned a 2nd AGX Orin** (`192.168.3.46`, user `mark`). Read-only recon
  first (JP6 ✓, sudo ✓, no NVMe, fresh box). Ran `provision.sh` — it **caught 2
  real bugs**: (1) `nvpmodel -m 0` interactive reboot-prompt aborted the run under
  systemd; (2) cpufreq governor write failed on offline cores (cpu8-11 offline in
  30W). Fixed both, re-ran to completion (Docker, nvidia-container-toolkit, CAN,
  Tailscale all in). Verified GPU compute host + in-container (CUDA 12.6 vector-add
  PASS). Installed RustDesk 1.4.6. Applied 97 apt holds (nvidia-l4t/cuda/cudnn/
  container-toolkit/docker), release-upgrade lock (22.04-only), unattended-upgrades
  (security-only).
- **Field-deployment discussion** (headless unit, unknown WiFi). Recommended:
  pre-authed ethernet-first Tailscale + phone SoftAP portal as fallback; pushed
  back that RustDesk isn't a reliable first-contact tool (needs internet).
- **5-PhD whole-system review** (`marks_team_of_experts`, Opus 4.8): Perception,
  Mechatronics/Safety, SW Architecture, NVIDIA Edge, Field Ops. Converged hard —
  all 5 rejected geometry as #1; E-stop/ALL-OFF chain is the corroborated #1.
- **Security + model-IP architecture** — assessed disk encryption (declined, with
  reasoning), then designed the call-home/usage-attestation idea. Built a new
  **`marks_security_experts`** skill (5 security lenses) and ran it → canonical
  **`ai-precise/docs/security.md`** (D1–D6). Headline reframe: the control surface
  has **zero auth** (`0.0.0.0:8080`, CORS `*`) — that, not the model, is the #1
  asset. Corrected the HANDOFF's "TPM-sealed" language (no TPM on standard Orin;
  use OP-TEE fuse-backed keystore).
- **Added a mandatory first-principles premise red-team seat** to
  `marks_team_of_experts` (and referenced it in `marks_security_experts`) — the
  highest-value lens is the one that attacks the load-bearing assumption, not the
  domain depth.
- **Device-attestation / tamper-evidence panel** (security lenses + the new
  premise seat = 6 agents). Premise seat came back first and killed the operator's
  "hardware change → LOCKDOWN" idea (false-positive machine); 6/6 converged on
  **tamper-EVIDENCE not tamper-response** → folded in as `security.md` **D7**.
- **Memory-extraction deep-dive** — how someone reads Orin RAM (USB recovery →
  root → read process memory is the realistic path; cold-boot/JTAG/bus-probe are
  lab-grade; remote needs RCE→root). Verdict: **reaffirms the A4 accept** (don't
  chase live-memory extraction; moat is flywheel + license + watermark). Added a
  cheap raise-cost hygiene set (IOMMU on, JTAG fuse-lock at production).
- **Consolidation: "does the architecture change?" → No at the core.** Three panels
  + the memory dive all converged on the same D1–D6 + accepts; one feature reframed
  (lockdown → evidence, now D7), two free wins added (impossible-travel + soc_uid),
  zero reversals. A design that absorbs new threats without restructuring is right.
- Updated HANDOFF + learnings + this log.

## Major decisions / outcomes

- **v1.2.7 is the current release.** Hardening batch folded in as a patch bump
  (per operator: "make it 1.2.7", not a 1.3.0).
- **Roadmap reframed:** the next sprint is a **pre-ship hardening sprint** (7 P0
  blockers + cheap P1s), NOT geometry/spray-rate. 5/5 lenses agree.
- **provision.sh is now first-run-tested** on fresh hardware — 2 bugs found that
  re-runs on a configured box never would have surfaced.

## Bugs surfaced / fixed today

- `provision.sh` step 9 — `nvpmodel -m 0` interactive prompt aborted run (fixed,
  uncommitted).
- `provision.sh` step 9 — governor write fails on offline cores (fixed, uncommitted).
- **Masked-by-the-fix finding:** the box silently boots into 30W; MAXN needs a
  reboot nobody triggers → benchmark doesn't hold (now blocker #3).

## To finish (next session)

- [ ] Commit `provision.sh` fixes + add RustDesk + apt-holds steps (`feat/provision-hardening`).
- [ ] Pre-ship blockers #1–7 (see `ai-precise/HANDOFF.md` checklist). Start with
      the E-stop/ALL-OFF chain.
- [ ] `192.168.3.46`: `tailscale up --ssh` + set RustDesk password (manual auth).
- [ ] Decide NVMe for the new box (eMMC fills after ~3 rebuilds).
- [ ] **Security pre-ship (see `security.md`):** D1 control-surface auth (nftables
      tailnet-only + identity); **verify OP-TEE on JP6.2 early** (long pole, gates D2);
      D4 signed manifest + fp32 default; D3 `model.enc` + grace cache; D6 revocation
      runbook. Attestation (D7) + watermark + impossible-travel = fast-follow.
- [ ] **Commit everything from today** — ai-precise (`security.md`, HANDOFF,
      `docs/learnings/2026-05-29.md`) + marvin (2 skills, session log, decisions).
