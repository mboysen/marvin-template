# Session Log — 2026-07-06 (Monday)

## Summary

First session in 3 weeks (state was 21 days stale — flagged at /start). One focus: an honest
"is the model and the cost set up for success with RF-DETR?" assessment of ai-precise, grounded
in the repo (operator scoped it to `/mnt/owc/home/mark/Documents/ai-precise` only; paige live
state NOT inspected). Method: 3 parallel read-only audit agents (detection/model stack, firing
coupling, deploy/docs/economics) over `feat/zed-gmsl-camera` @ `4e05381`, plus a direct code read
to answer the operator's geometry-gate question. Everything saved into the ai-precise repo.

## Verdict delivered

**RF-DETR is not the risk — the system around it isn't measurable yet.** (a) Precision at the
nozzle cannot be measured (no labeled eval set, `validation_set_size: 0`, identity calibration,
0/656 tests measure quality); (b) the model has been running at a lower resolution than believed;
(c) firing geometry belongs to a removed camera (DJI-1080 boxes on the ZED-1200 feed). The 6-15
premise inversion stands, unactioned — this audit re-derived the same P0 from code alone.

## Two open questions RESOLVED

1. **576 vs 768 → RFDETRMedium infers at 576.** Nothing passes `resolution=` to the rfdetr
   constructor unless the quarantined manifest knob is set (`rfdetr_detector.py:210-216`); our
   `medium→768` constant only feeds ModelInfo + warmup (`:51-54`); installed rfdetr 1.6.5 Medium
   default = 576 (`config.py:266`). Live downscale = 3.33× linear (~11× area). All "73ms @ 768"
   perf math was wrong-baselined. Follow-up that decides the cheapest lever: read v4a's TRAINED
   resolution from checkpoint args (768 → knob-to-768 is free recall; 576 → inference is matched,
   lever stays tiling/GSD).
2. **Geometry-gate "98% die" = event-counter artifact** (operator asked to confirm the gate's
   meaning — confirmed: pure spatial check, weed must be inside a specific nozzle's
   footprint+boundary box). Counter logs per (track × nozzle × frame): 7-of-8 fails are
   structural, fails re-count every frame, passes count ~once (tier-1 dedup short-circuits
   before geometry post-fire). Honest per-weed funnel: ~15% of tracked weeds ever entered ANY
   zone. Real defect: 1080-era boxes on the 1200 feed — bottom ~148px covered by no box (weeds
   literally "go under" the boxes), 15/16 nozzles null calibration.

## Other headline findings

- `compute_lead_box` never wired into the engine (debug route only) — firing uses tracker
  pixel-velocity + stale 120ms anchor → 16–53ms effective lead vs ~215ms real path (~27mm
  under-lead @2mph); 1-frame tracks fire with zero lead (min_track_age=1).
- Gate-0 staleness inert (trips >384ms vs ~215ms real); cooldown 0, tau_iou 0.1.
- Dashboard honesty gaps: mock controller default, `/state` hardcodes mode/estop, `on_fire`
  synthesizes success, `fires_failed_total` never surfaced.
- Repo↔box drift: v4a only on paige's non-git tree; committed config targets MN (+ 30-name list
  that would zero-gate v4a); bench Controllino on local BENCH_BYPASS_ESTOP=1 build.
- Perf levers untried: live warmup_passes=0, batch-2 cross-camera forward, fp32 compile;
  rfdetr unpinned upper bound.
- Economics: `costs:` block all zeros — "steam is cheap per shot" load-bearing, unquantified.

## Artifacts written (ai-precise repo, uncommitted)

- `docs/MODEL_COST_ASSESSMENT_2026-07-06.md` — full assessment: verdict, resolved questions,
  assumptions, ranked risks, P0–P3 punch list (the doc of record).
- `docs/learnings/2026-07-06.md` — 5 transferable lessons (display-vs-actual constants;
  event-counters ≠ funnels; ship-the-wiring-or-delete-the-formula; reviews need owners+dates;
  honesty gaps compound).
- `HANDOFF.md` — new 2026-07-06 top section + updated header line.

## Next steps (P0)

1. Log effective `model.resolution` at load + read v4a trained res from checkpoint args (~1hr).
2. Label the eval set (≥2 scenes) + wire `eval_field_run.py --labels`; adopt recall @ hard
   precision floor (Wilson). Still the "ONE thing" from 6-15.
3. Reconcile repo↔paige: live config/router.env into git; commit untracked
   `docs/learnings/2026-06-09.md`; delete garbled `nozzles/` dir; **check whether the 6-15
   mock-mode ai-precise is still running on paige and stop it**.
4. Reflash committed 0/safe Controllino firmware before any E-stop wiring or steam.
Then: P1 firing restore (geometry recal @1200, lead anchor+wire, honest dashboard) → P2 model
levers gated on the eval set → P3 economics. Also pending: commit this session's docs + the
review artifacts (operator's call).

## Housekeeping noted

- marvin-template working tree is dirty (modified state files + a pile of untracked:
  `Naware_Competitor_Dashboard.html`, `index.html`, `projects/`, `research/output/`,
  `rf-detr-base.pth`, `.herenow/`, mac `._*` cruft) — a /commit sweep is overdue.
- `state/goals.md` still template defaults.
- Session ran on Fable 5 @ max effort (operator set /model + /effort at start).

---

## Update: continuation — independent review, code shipped, operator disposition, committed+pushed

A second, independent pass (unaware of the first pass's exact wording) re-derived the same
verdict and both resolved questions from the code — useful cross-validation — then went further
using evidence OUTSIDE the ai-precise repo:

- **576-vs-768 fully closed, not just observed.** `weed_detection/weights/v4a/training_v4a.log`
  on sparky records the actual training args: v4a trained at **`resolution=768`**. Combined with
  the repo-only finding that it currently SERVES at 576, this means the quarantined
  `resolution: 768` manifest knob is a **restore-to-trained** (the 36→48 positional-encoding
  interpolation ran on every training forward pass), not a PE-extrapolation gamble — a real
  re-ranking of that lever from "weakest" to "leading candidate once the eval gate exists."
- **paige confirmed offline** (Tailscale: last seen ~2026-06-17, ~19 days). This makes the 6-15
  mock-mode check moot — that bring-up was `systemctl start` only, never `enable`, so it does not
  survive a power cycle. Nothing to stop.
- **Operator corrected the geometry-gate framing further**: detections landing outside a nozzle's
  boundary box are BY DESIGN, not a defect — confirmed against `firing_engine.py:397-415` (a pure
  per-track-per-nozzle spatial check). The real, narrower defect is box *placement* (cam1's boxes
  are cam0's copied template; cam0's were drawn for the old 1080 feed) — not the fail-rate number.

**Operator then walked the punch list and dispositioned every item** (do now / build a mechanism /
pause / hold / clarify — full mapping in the ai-precise assessment doc's new "Punch-list
dispositions" section). Two items were built same-day:

1. **Resolution-truth fix** (`RFDETRDetector.load()`): now probes the loaded model's actual
   forward resolution and lets it win `ModelInfo` + warmup, with a loud warning on any mismatch
   vs. the variant constant — closing the exact silent-drift class that hid 576-vs-768 for
   months (same shape as the 2026-05-26 fp16 incident).
2. **`aip-nozzle lead-anchor`** — a new terminal command, the operator's requested "way to test
   and change [the lead anchor] based on field results, likely via terminal": shows the
   configured anchor against the LIVE measured inference p50 (drift + aim-error in mm at the
   configured boom speed), with `--from-live`/`--set` to persist a corrected value through the
   config API (sanity-bounded, verifies the write stuck, prints the restart-to-apply + a field
   verification procedure).

12 new tests (5 resolution-probe, 7 lead-anchor); 86 pass, the same 14 pre-existing env failures
as clean HEAD; ruff clean.

**Committed + pushed**: `feat/zed-gmsl-camera` @ `42b293e` (was `4e05381`), 7 files. Scope was
deliberately narrow — only today's actual work (the two code changes, the assessment doc + its
addendum, the lessons file, the HANDOFF update). Left alone on purpose (pre-dates this session,
not swept in): `.claude/` agent defs, the garbled `nozzles<0xEF80A8>/` dir with a stray
screenshot, `tests/tracking_runs.jsonl` (2026-05-27 field telemetry), `docs/learnings/2026-06-09.md`,
and macOS `._*` cruft — all still untracked, operator's call on any of them.

### Operator punch-list dispositions (recorded verbatim intent)
- **Do now:** label the eval gate; log+probe true resolution (shipped).
- **Build a mechanism:** boundary-box placement validation (wet-spot recal procedure defined,
  executes at the rig); lead-anchor field tuning (shipped as `aip-nozzle lead-anchor`).
- **Pause:** signing the inference knobs; unifying offline/live preprocessing.
- **Hold, pending bench nozzle diagnostics first:** firmware 0/safe reflash + kill-path
  validation (code is ready; do NOT flash yet).
- **Hold:** slot-12 background-emission check.
- **Clarified, no action needed:** per-class thresholds (the web app already has the UI; only the
  calibrated *values* are missing, gated on the eval set); domain fine-tune (keep harvesting
  scenes, no training yet).
- **Do NOT remove:** the plaintext MN checkpoint on paige (deliberate keyserver-outage fallback).

## Next steps (P0) — updated

1. ~~Log effective `model.resolution` at load~~ **DONE** (shipped + pushed this session).
2. Label the eval set (≥2 scenes) + wire `eval_field_run.py --labels`; adopt recall @ hard
   precision floor (Wilson). Still the "ONE thing" — 267 frames of the 6-14 scene are already
   local (`~/paige_captures/2026-06-13/`), so labeling can start without paige.
3. On paige power-up: verify in-container rfdetr version, reconcile live config/router.env into
   git, confirm the 6-15 mock-mode ai-precise did NOT survive (expected), then bench the
   `resolution: 768` restore against the eval gate once it exists — re-run
   `aip-nozzle lead-anchor --from-live` immediately after (latency changes with resolution).
4. Boundary-box wet-spot recal at the rig (mechanism defined, needs Controllino + water, no
   steam) — do before trusting `weeds_by_nozzle` as anything but an FP meter.
5. Firmware reflash + kill-path validation: HELD until bench nozzle diagnostics are run first.

---

## Update: part 3 — weed_detection model-choice assessment, flywheel v2, first gate-labeling session

**Model-choice assessment written + landscape-verified** →
`weed_detection/docs/MODEL_CHOICE_ASSESSMENT_2026-07-06.md`. Verdict: **STICK with RF-DETR**,
made falsifiable (decision rule: revisit only if the human-labeled gate fails data levers, or
latency fails after compile/Nano-Small/TRT ladder). Key evidence: in-house numbers are the
load-bearing case (v4a 94.65 mAP50:95 on CottonWeedDet12 vs published YOLOv8s 87.1 / RT-DETR
85.9); the public "DINOv2 data-efficiency" marketing is contested (2 independent weed papers
show RF-DETR losing) — lead with OUR numbers externally, not Roboflow's. Verified: RF-DETR
edge sizes Apache-2.0 (XL/2XL now paid PML-1.0 — vendor-direction warning); Ultralytics AGPL
w/ quote-only enterprise; RF-DETR TRT story weakest surveyed (open NVIDIA bug on AGX Orin;
fp16 issues ecosystem-wide — our 05-26 collapse was the norm). Fixability (operator ask):
MN/turf HIGH (real yard evidence; gaps = honest metric + production-camera validation);
v4a/Paige LOW→MODERATE→GOOD (camera prereqs → 768 restore → in-domain fine-tune; data
timeline, not architecture). MN's 93.7% shown CIRCULAR (bootstrap re-annotated val/test —
pseudo-labels; split hygiene itself clean). Wyoming = only honest field number (R 0.663 /
P ~0.35). Commercial-viability path + 1-100 calibration (turf ~90 ceiling / Paige ~82-85)
+ video-training protocol all in the doc. Risks: license contamination (CC-BY-NC in DT asset
chain), no git repo, Zeus-only training data, 3 rfdetr versions across venvs.

**Flywheel v2 upgrades** → `weed-active-learning/docs/FLYWHEEL_V2_UPGRADES.md` (adds to locked
spec): A) label economics (video ingest + track-propagated labeling + split-by-clip), B) data
selection (FP-driven + uncertainty harvest, query-cap audit, GSD gate), C) business
instrumentation (per-site ledger, cycle report = investor slide, checkpoint lineage, rights
tags). DINO's v2 role clarified (proposer, humans = authority; disagreement as prioritizer).

**v2 tested live on real paige captures:** GroundingDINO swinB (GPU, 118s) over the 162-frame
1fps sample → 132 class-agnostic boxes on 100 frames (vs full-frame v4a: 1 frame ≥0.3!);
DINO's #2 box = 1 frame from v4a's tiled MorningGlory winner (independent convergence). This
IS the spec §5 iter-0 proposer A/B. Artifacts: `paige_captures/2026-06-13/sample_1fps/_dino_out/`.

**Label Studio gate labeling stood up** (project 5 "Paige Springs Live Capture 1", 162 tasks):
LS restarted with LOCAL_FILES env (console-script shebang broken by home migration — launch
via `venv/bin/python venv/bin/label-studio`); frames copied to
`weed-active-learning/dataset/incoming/20260614_paige_1fps/` (flat names); tasks JSON w/
predictions generated + imported; species labeling config (site species first per locked
taxonomy). JWT API used to bulk-create editable annotations from predictions (100) + hide the
read-only prediction layer (which had been silently eating the operator's edits).
**⚠ First labeling pass LOST** — operator worked against the read-only layer; 0 annotations,
0 drafts persisted. Redo required (fast now); operator's chat notes survive as a map (469/471
bad boxes, 473 clean, ~30 "error" tasks). Process fix: label 2-3, verify via API, then full pass.
**Site rule of record: GRASS = GROUND COVER at Paige (never box; grass-only frames =
empty-submit; disable v4a Goosegrass class in firing config at this site; drop "grass clump"
from site prelabel prompts)** → recorded in `labelstudio/LABEL_STUDIO_SETUP.md`.
WIP snapshot: `dataset/incoming/20260614_paige_1fps/labelstudio_WIP_snapshot_20260706.json`.

**Open:** finish + submit the 162-task pass (100 box-review + 62 empty-submits) → freeze gate
slice → score v4a 576 vs 768 against it. weed_detection `git init` + HANDOFF update still
offered/not done. LS server: nohup pid on :8080, log `~/.label-studio-8080.log`.

### Correction + gate-pass completion (late evening)
**The "lost labeling pass" conclusion was WRONG** — the operator had been working in a TWIN
project the whole time (two imports created "Paige Springs Live Capture 1" as projects 4 AND 5;
all API fixes had targeted 5; the operator's work lived in 4). Project 4 final state:
**160/162 tasks labeled — 146 confirmed-empty frames + 23 kept weed boxes on ~14 frames**
(operator pruned ~109 of DINO's 132 proposals as false — grass=ground-cover rule applied;
labels all single-class `weed` per iter-1 design). 20 operator-named "error" tasks
empty-submitted via API per explicit chat verdict; 529/543/591 were already empty. **Remaining:
tasks 521 + 546 only.** Export: `dataset/incoming/20260614_paige_1fps/labelstudio_gate_pass1_project4_20260706.json`
(253 KB; supersedes the project-5 WIP snapshot). Project 5 = abandoned duplicate (auto-copies
only) — propose deleting to prevent recurrence, operator's call. Once 521/546 are done:
freeze as the Paige gate slice → score v4a 576 + 768 against it.
Gate composition note: positives are thin (~14 frames / 23 boxes — honest for this sparse
scene); the positive-rich additions (141 tiled winners + full 4806) come at paige power-up.

### GATE FROZEN + FIRST FIELD-GRADE BASELINE (evening close)
Gate frozen: `weed-active-learning/dataset/gate/paige_gate_20260614/` — 162 frames (14 pos /
148 confirmed-empty), 23 human boxes; LS project 4 renamed [FROZEN]; duplicate project 5
DELETED (operator-confirmed). Correction of record: operator's labeling was never lost — twin
project confusion; work was in project 4 all along.
**Baseline — deployed v4a (whitebox bytes) vs gate, live-faithful letterbox, full-frame:**
576: R 4.3% / P 2.8% @th0.1, ZERO dets ≥0.3. 768: R 17.4% / P 10.8% @th0.1, ZERO ≥0.3.
Read: 768 confirms the resolution direction (4× recall) but **v4a full-frame is NOT viable at
this site in either resolution** — quantifies the assessment's "LOW as-is." Firing stays off
at Paige until paige-v1 (in-domain fine-tune) exists; this table is the "before" column of
cycle report #1. Next: paige power-up → pull 4806 frames + 141 tiled winners → label a few
hundred (track-propagated) → train paige-v1 from checkpoint → re-score vs THIS gate.
Score JSON: `dataset/gate/paige_gate_20260614/score_v4a_20260706.json`.
