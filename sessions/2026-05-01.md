# Session Log: 2026-05-01

## Update: 09:38
- Checked vLLM Qwen3.6-27B status: container was down (no systemd, no auto-restart). User ran start script on sparky — got back container ID `aa1d856877f8…`, so docker started cleanly.
- Endpoint at `http://100.81.162.68:8001/v1/models` still not responding — container is mid-cold-start (weight load + cudagraph compile, ~5 min on warm HF cache).

## Session End: 09:41

### Topics
- Sparky vLLM Qwen3.6-27B-FP8 was down on session start (no auto-start). User manually relaunched via `vllm-qwen36-start.sh`; container ID returned, but endpoint still warming during session.
- Promoted Qwen3.6 from "test mode" to systemd-managed service so it auto-starts on boot.

### Decisions
- **Promote vLLM Qwen3.6-27B-FP8 to systemd-managed.** Mirror the Nemotron pattern: `Type=exec` owning a foreground `docker run --rm`, restart-on-failure, 45-min cold-start timeout, `WantedBy=multi-user.target`. (logged)

### Content Shipped
- None.

### Files Changed
- `sparky_infrastructure/scripts/vllm-qwen36-start.sh` — converted from `-d --rm` (detached) to `exec docker run --rm` (foreground for systemd). Tailscale-scoped `100.81.162.68:8001` bind preserved. Backup at `.bak-pre-systemd-20260501`.
- `sparky_infrastructure/scripts/vllm-qwen36.service` (NEW) — systemd unit, after `docker.service` + `tailscaled.service`, `Restart=on-failure`, `StartLimitBurst=5`, journalctl logging.
- `sparky_infrastructure/scripts/vllm-qwen36-install.sh` (NEW) — stops manually-launched container, installs unit, `enable --now`, polls `/v1/models` up to 45 min.

### Open Threads
- **Install not yet run on sparky.** Activate with: `ssh sparky 'sudo bash /home/mark/Documents/sparky_infrastructure/scripts/vllm-qwen36-install.sh'`. After install: `systemctl status vllm-qwen36`, `journalctl -u vllm-qwen36 -f`.
- Qwen3.6 endpoint still warming from this morning's manual relaunch — confirm `/v1/models` responds before installing systemd unit (installer will tear it down and re-launch under systemd anyway, but worth verifying the manual config works before promoting).
- All carried-forward items unchanged (cron-tick watch, LinkedIn token, Khanal email, Wyoming heading-stage collection, Agent V2 deploy, inboxing rename, 18 stale hubs).

### Next Actions
- Run `vllm-qwen36-install.sh` on sparky to activate auto-start.
- Verify reboot survival (optional: `sudo reboot` and confirm `/v1/models` comes back without manual intervention).
