# Session Log: 2026-05-15

## Update: 17:07
- **AI Precise Phase 1 — Jet backyard-test deployment marathon.** Recovered the API-529-errored pipeline + model-bundle agents (committed `b4fa1d7`), fixed 5 real build/deploy blockers in sequence: L4T tag `r35.5.0-pth*` doesn't exist on NGC → `r35.2.1`; deadsnakes PPA is amd64-only → uv-managed standalone CPython 3.10; `l4t-pytorch` base bloated image to 17 GB (won't fit Jet) → slim `l4t-base` (6.35 GB / 3.3 GB tarball); tarball `:v0.1.0` vs unit's `:latest` tag mismatch → `docker tag`; missing `config.yaml` → generated valid one from the real schema + shipped. Container now runs on the Jet (UI live, pipeline loop running).
- **Decisive blocker found + correct path proven.** Container torch was `cu130` (wrong CUDA) → inference fails every frame. Root cause is structural: rfdetr 1.6.5 requires Python ≥3.10 + torch ≥2.2, but the only CUDA-enabled torch for JetPack 5.x is NVIDIA's `torch 2.0.0+nv` on Python 3.8 — irreconcilable by any base-image (verified `dustynv/l4t-pytorch:r35.4.1` = py3.8/torch2.0). **MN checkpoint → ONNX export SUCCEEDED on sparky** (113 MB, valid) — checkpoint-format risk retired; TRT-engine path (operator's own `jetson_orin_recommendation.md` prescription) is unblocked. ONNX preserved in repo + Jet bundles.
- **Open:** TRT build is next (engine via `l4t-tensorrt:r8.5.2.2-devel` on Jet, write `TRTDetector`, slim no-torch rebuild). Design fork pending operator call: app code targets py3.10, JetPack TRT bindings are py3.8 — backport runtime modules to 3.8 (recommended) vs keep 3.10. Tailscale-on-Jet parked (tailscaled not Running, likely `tun` module; backyard-only, non-blocking).
