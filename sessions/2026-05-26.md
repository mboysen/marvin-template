# Session Log: 2026-05-26

## PICK-UP HERE (read this first to resume)

**System state at end of session:**
- AI Precise service is **`inactive`** on paige_springs (crashed/stopped mid-session, cause unknown — no journal access from this user; check `sudo journalctl -u ai-precise -n 200 --no-pager` first thing).
- Container destroyed; need `sudo systemctl reset-failed ai-precise && sudo systemctl start ai-precise` to bring it back.
- All today's calibration is persisted in `~/paige_springs/data/config/config.yaml` on the box AND mirrored in `configs/paige_springs.yaml` in the repo. Container restart will reload it.
- On restart, `fires_count` and `weeds_detected_total` reset to 0 (in-memory only — `detections.jsonl` survives but was archived to `detections.jsonl.session-2026-05-26.20260526-223012` so a fresh log starts).

**Uncommitted work (DO NOT lose):**
- `ai-precise/` repo: 5 source files (track_id fix + weeds counter) + `configs/paige_springs.yaml` + `HANDOFF.md`.
- `marvin-template/` repo: `state/current.md`, `state/decisions.md`, `sessions/2026-05-26.md`.
- Run `git status` in both repos to see exactly what.

**Top 3 next moves (priority order):**
1. **Commit today's edits** (or at minimum `git stash` them so they're not lost). Suggest a co-authored commit per the repo's git conventions.
2. **Resume the latency investigation** that was paused mid-session — open question: does `optimize_for_inference(compile=True, dtype=float32)` get most of Friday's speedup WITHOUT the fp16 confidence collapse? See HANDOFF.md "Open issues / Latency" section + the partial findings already cached in the session.
3. **Investigate why the service crashed** at ~22:30 UTC. No journal access in this session; check on next pickup.

**Where to read:**
- `ai-precise/HANDOFF.md` — full system state, open issues, next steps (the canonical engineering handoff)
- `state/decisions.md` — 3 new decision entries from today (per-class thresholds, production strategy, PATCH classes gotcha)
- `state/current.md` — Priority 1 + open threads (active context for MARVIN /start)
- This session log (the rest of this file) — full narrative

## Theme
AI Precise full-day debug + calibration session on paige_springs. Started with operator reporting "doesn't detect plantain when held over the weeds"; ended with per-class thresholds calibrated, two real bugs fixed, two LLM-agent reviews completed, and a documented production strategy that differs from the backyard config.

## Timeline (UTC)

- **~19:00** — Session opened with `/start`. Switched to `~/Documents/ai-precise/`. Checked logs from Friday's 3.5-hr yard run (9.37M detection rows). Found service stopped, container not running.
- **~19:10** — User reported "plantain not detecting" in the live overlay; pulled fresh detection data. Found `JET_LOG_DETECTIONS=1` missing from systemd unit so today's earlier 12-min container run produced no diagnostic capture.
- **~19:30–20:00** — Two LLM subagents created in `ai-precise/.claude/agents/` (perception-engineer + perception-peer-reviewer). Not picked up by harness this session (need Claude Code restart), so used `general-purpose` with the perception-engineer role baked into the prompt.
- **~20:10–20:25** — Re-added `JET_LOG_DETECTIONS=1` to systemd unit + raised `JET_BYTETRACK_ACTIVATION` 0.03→0.30 via staged sudo script. First script had a grep bug that matched a comment line (false positive on "already present") — wrote a v2 fixed version. Test 1 ran without log capture; test 2 finally had logs.
- **~20:35–20:45** — Diagnosed Friday's "fp16 silent enable": `manifest.json` was hand-edited 2026-05-22 22:12 (end-of-test) to enable `optimize_for_inference: true` + `optimize_dtype: float16`. fp16 collapsed RF-DETR's score dynamic range → plantain/clover slid into noise. Reverted manifest to minimal (fp32, no optimize). Container went from 70ms→200ms inference but real-world confidence recovered (plantain peak 0.94, clover 0.98).
- **~20:50–21:00** — Re-sized nozzle 1 from 1200x820 to 480x400, mirrored across frame center. Both nozzles now 480x400. Did Test 3 — fires now distributed correctly but ALL had `track_id: null`.
- **~21:00–21:30** — Traced track_id bug: `FireCommand` schema in `types.py:266-279` was missing the `track_id` field entirely. Fixed via 3-line patch (add field + propagate through firing_engine.py:371 + pipeline.py:425). Also added a `WeedsSeen` singleton in `telemetry/fire_log.py` and exposed `weeds_detected_total` + `weeds_detected_by_class` on `/api/v1/state`. scp'd 5 patched files to `/home/mark/ai-precise-patch/ai_precise/` on the box (bind-mounted in container at `/app/src/ai_precise:ro`). Restart picked up new code. Test 4 verified: 10 fires, all with distinct non-null track_ids [7,35,44,46,53,56,60,62,65,66]; 32 weeds tracked (9 plantain + 23 clover).
- **~21:35** — Perception-engineer agent landed with full report. Surfaced 3 findings I had missed: (a) repo↔live config drift on N1 boundary; (b) `firing.inference_p50_ms_at_calibration` 110 vs measured 199; (c) the right lever was per-class thresholds, not global tuning. Applied all 3 fixes + bundled cleanup.
- **~21:40** — Peer-reviewer agent (Opus, explicit) landed. Verdict: NEEDS MORE EVIDENCE. Pushed back on three of the engineer's calls. Most important: (a) cycling is multi-pair, not single-pair (32% plantain↔clover, 23% charlie↔clover, 23% plantain↔dandelion, 6% etc — not just one species boundary); (b) chickweed/purslane disable may be wrong (peer reviewer found 274 chickweed + 329 purslane kept earlier in the day); (c) per-class 0.60 may suppress production detections at field distance. Re-enabled chickweed + purslane based on the pushback.
- **~21:45** — Operator clarified production strategy: at boom speed 2-4 mph, `min_track_age_frames > 1` is NOT viable (each weed visible only 2-4 frames at 4 FPS). Production goal is low threshold (0.10-0.15) + accept fire-on-frame-1 + rely on tier-1/tier-2 dedup for false-positive suppression, NOT high threshold. Backyard 0.60 config is NOT shippable. Cycling at decision-time is unfixable at boom speed; only affects per-species logging (fix offline via modal-class post-processing per track).
- **~22:00** — Updated `ai-precise/HANDOFF.md` with full 2026-05-26 section (bug fixes, calibration, diagnostic findings, production strategy, updated open issues + next steps).
- **~22:10–22:20** — Yard test plan: ran Test 1 (bare-grass FP test) at gate 0.35 — last 60s "clean" window showed 0 kept, 0 fires. Then re-ran Test 1B at gate 0.15 to characterize production-target threshold. Found: model emits white_clover at peak 0.934 even on what user thought was bare grass. Reading: there's real clover in the lawn (clover blends into grass). 1 fire in 2 min, which is reasonable for a clover-heavy lawn with tier-1/tier-2 dedup working. True FP test requires zero-target scene (pavement/wall/sky), not yard grass.
- **~22:30** — Service crashed / went inactive between tests (cause unknown, no journal access). User chose to shut down rather than restart.

## What was changed (not yet committed)

### `ai-precise/` repo — 6 files
- `jetson/src/ai_precise/types.py` — added `track_id` field to FireCommand
- `jetson/src/ai_precise/firing/firing_engine.py` — pass track_id when constructing FireCommand
- `jetson/src/ai_precise/pipeline.py` — pass track_id to FIRE_LOG.record + write to WEEDS_SEEN
- `jetson/src/ai_precise/telemetry/fire_log.py` — added WeedsSeen singleton
- `jetson/src/ai_precise/ui_server/routes/state.py` — added weeds_detected_total + by_class fields
- `configs/paige_springs.yaml` — full rewrite: new nozzle 1 box, per-class thresholds, inference calibration
- `HANDOFF.md` — added 2026-05-26 session section, refreshed open issues + next steps

### `marvin-template/` repo — 3 files
- `state/current.md` — updated Priority 1, added 4 new Open Threads, added 2026-05-26 to Recent Context
- `state/decisions.md` — 3 new decision entries (per-class thresholds + bug fixes; production strategy; PATCH classes gotcha)
- `sessions/2026-05-26.md` — this file

### On the box (`mark@192.168.3.38`)
- `/home/mark/ai-precise-patch/ai_precise/` — 5 hot-patched source files (bind-mounted into container)
- `/etc/systemd/system/ai-precise.service` — added `-e JET_LOG_DETECTIONS=1` + `JET_BYTETRACK_ACTIVATION=0.03→0.30` (backups at `.bak-debug-*`)
- `~/paige_springs/data/models/_base/MN_weedmodel_final/manifest.json` — reverted to minimal (fp32, no optimize); backup at `.bak-fp16-*`
- `~/paige_springs/data/config/config.yaml` — per-class thresholds applied via PATCH, persisted
- `~/paige_springs/data/runs/detections.jsonl.session-2026-05-26.20260526-223012` — archived today's 9.2M-row detection log (2.7 GB)
- `~/paige_springs/data/config/config.yaml.session-2026-05-26.20260526-223012` — archived live config snapshot

## Pending follow-ups

1. **Resume latency investigation** — paused mid-session. Open question: does `optimize_for_inference(compile=True, dtype=float32)` (graph fusion + TF32 only, no fp16) give us most of Friday's speedup without the confidence collapse? The 3 wrapper knobs are independent — Friday assumed `optimize` = `fp16`, but they can be split.
2. **Commit today's edits.** 6 files in `ai-precise/`, 3 files in `marvin-template/`.
3. **Build proper container image** with today's source patches; deploy via `load-tarball.sh`; remove bind-mount patches.
4. **True bare-input FP test** at production threshold (0.15) on a no-target scene (pavement / wall / sky). Today's yard tests had clover present so we couldn't isolate model FP rate from real-clover detections.
5. **Distance test** — same target at 1ft, 3ft, 6ft, 10ft. Calibrates whether 0.60 per-class threshold suppresses field-distance detections.
6. **Investigate why service crashed** at end of session. No journal access from this session. Check `sudo journalctl -u ai-precise -n 200` next time.
7. **Offline modal-class post-processor** for the detections.jsonl — assigns each track its most-common class across its lifetime. Solves per-species logging accuracy without trying to fix cycling at decision-time.

## Open issues filed (per HANDOFF.md)
- Latency reduction is top priority for production viability
- Cycling unfixable at decision-time at boom speed; fix offline
- Score calibrator is identity / pass-through (per-class thresholds operate on raw logits)
- `PATCH /api/v1/detection.classes` REPLACES list (workaround: always send full list)
- Repo ↔ live config drift discipline needed
- 91-slot RF-DETR head emits OOB class IDs (cosmetic; never gates through)
