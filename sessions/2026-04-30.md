# Session Log: 2026-04-30

## Update: 18:35
- **Wiki recurrence loop part 2:** Found 5,595 corrupted files in Mac's harvest tree (`mark@192.168.1.5:~/Documents/naware-knowledge1/raw/harvested/`) still feeding the bounce-back loop. Cleaned via `find -delete` (xargs hit ARG_MAX from long names). 9.5 MB backup at `mac:/tmp/mac-corrupt-backup-20260430-181941.tar.gz`.
- **reorganize.py defense-in-depth:** added second guard — refuses to copy any file whose name contains `_raw_` 2+ times, regardless of source path. Catches what `SKIP_TOP_DIRS` misses (e.g. files harvested at clean `lucas/sessions/<bad-name>` paths).
- **Broken-symlink cleanup:** wiki-wide pass deleted 5,756 dangling symlinks in `raw/projects/*/sessions/` left over from yesterday's corrupted-target deletions. Lucas/sessions went from 104 (76 broken) → 28 valid.

## Update: 18:38
- **Lucas hub Opus auto-compile failed** — the hub-concept-template.md is human-facing prose, not an LLM prompt. compile-projects.py fed it raw to Opus, output had no frontmatter and was raw session-log dumps. Reverted via backup. Bad output preserved at `concepts/lucas.md.bad-compile-20260430-182700` for reference.
- **Switched to manual hub-append pattern.** Read 4 post-2026-04-16 lucas sessions (2026-04-17, 2026-04-20–23, 2026-04-24; skipped a 2026-04-20 sparky session that was wiki-related not lucas). Wrote 3 one-line entries in existing Session Log table format. Hub structure + `frozen: true` preserved. All 4 wikilinks resolve.

## Session End: ~19:00

### Topics
- Qwen3.6-27B-FP8 vLLM endpoint rebound from `127.0.0.1:8001` → `100.81.162.68:8001` (Tailscale-scoped) so Ava on the OpenClaw VPS can reach it. Pushed back on ops's `0.0.0.0` plan + their `sed --host` patch (would have been a no-op).
- Knowledge-base wiki rescue: discovered 5,595 corrupted files still on Mac's harvest tree feeding the bounce-back loop; cleaned them. Added defense-in-depth `_raw_` count guard in reorganize.py. Swept 5,756 broken symlinks wiki-wide.
- Lucas hub update attempt: Opus auto-compile via `compile-projects.py` against `hub-concept-template.md` produced malformed output (template is prose, not an LLM directive); reverted. Manually appended 3 new Session Log entries (2026-04-17, 2026-04-20–23, 2026-04-24).
- Wired vLLM as opt-in backend in `ollama-process-sessions.py` (env-var gated, default Ollama unchanged) — patch in place, not flipped.

### Decisions
- **Hub concepts get manual one-line appends, not Opus auto-compile.** (logged in decisions.md)
- **Reorganize.py defense-in-depth: reject `_raw_` ≥ 2 in any name.** (logged)
- **Tailscale-scoped bind for vLLM endpoint** (sparky's tailnet IP only, not `0.0.0.0`) — reachable from VPS without LAN exposure.

### Content Shipped
- None.

### Open Threads
- 18 other frozen hubs likely have stale Session Log tables (only lucas done this session). Apply same manual pattern as needed.
- Mac upstream root cause unfixed: harvest.sh on Mac is still pulling vault content into harvest output. Defended at reorganize layer, not at source. Worth investigating Mac's `harvest.sh` exclude logic next time we touch the wiki tooling.
- vLLM Qwen3.6 backend opt-in is wired but not flipped. Needs `NAWARE_LLM_BACKEND=vllm` + URL/model env vars in `naware-obsidian-sync.sh` before next cron promote-over.
- LinkedIn proxy token expired 2026-04-17 — still needs refresh.
- Naware Agent V2 not yet deployed to GB10 — needs WAL mode, stuck-row sweep, Gmail draft wiring.
- inboxing.com bulk rename stalled on DevTools capture of dashboard internal endpoint.

### Next Actions
- Watch one full cron tick tomorrow morning to confirm `_raw_` recurrence is fully blocked at both layers (no new corrupted files in Mac harvest, no leakage into wiki).
- Optional: flip `NAWARE_LLM_BACKEND=vllm` for the wiki step-4 path and compare 27B vs 8B output quality on a few sessions.
- Optional: take another hub off the stale list (jackson-outreach or naware-website are most likely candidates).
