# Session Log: 2026-06-09

## Update: 11:51

### ZED X One GS — FIRST LIGHT achieved; ai-precise running on the ZED feed
Brought up ai-precise on paige_springs (Tailscale `mark@100.96.197.31`; LAN
`192.168.3.38` had no route). Full chain live end-to-end:
**ZED X One GS → host capture (pyzed/Argus) → /dev/shm bridge → container detector
→ RF-DETR → tracker → firing engine.**
- Host `zed-capture.service`: publishing **1920×1200 @ 30 fps**, locked ISP, to
  `/dev/shm/ai_precise_zed_frame`. Auto-starts clean on boot.
- Container (manual `docker run -d`, `--ipc=host`, **no `--device`**): RFDETRBase
  loaded, `fps_achieved ~8.6` (109 ms fp32), 40+ weeds detected, UI/API on `:8080`.
- `GEOMETRY_*_FAIL` is expected (nozzle geom calibrated for 1080, ZED is 1200) →
  Phase-2 recal item, **parked** per operator ("not really an issue, clear for now").

### Root-caused the 06-08 "0 cameras" blocker — it was OUR code, not versioning
06-08 attributed it to daemon-5.x/SDK-5.x/numpy-2. Real cause: capture service
used the **stereo `sl.Camera`** API on a **monocular ZED X One**. `sl.Camera.get_device_list()`
returns 0; **`sl.CameraOne.get_device_list()`** lists both cams (SN 309878703 /
309009949, "ZED XOne GS"). Switched `zed_capture_service.py` to `sl.CameraOne` /
`InitParametersOne` (no depth_mode; `grab()` takes no RuntimeParameters). Driver
1.4.2 is the *supported minimum* for SDK 5.3 — never a version wall.

### Fixed `install-zed-capture.sh` (3 bugs) + new step
`--system-site-packages=false` (invalid), missing `requests` in venv, and the
big one: SDK installed `/usr/local/zed/lib` mode 700 + `.so` mode 600 (unreadable
by the non-root service) → added `chmod -R a+rX /usr/local/zed` + ldconfig entry +
`libturbojpeg` install (apt wedged by an unrelated libudev1 skew → `dpkg -i` the
.deb directly). All baked into the script. A reboot cleared a wedged GMSL daemon.

### Container image is stale → hot-patched
paige's `ai-precise:latest` (~06-02) still has the OLD in-container-pyzed
`GMSLBackend` (dies on `libnvargus_socketclient.so` symbol). Bind-mounted the new
bridge-reader camera module over it (`-v .../jetson/src/ai_precise/camera:/app/src/ai_precise/camera:ro`).
Image still needs a proper rebuild from `feat/zed-gmsl-camera`.

### Restarted the sparky keyserver — root cause: HOME MIGRATED to /mnt/owc
`ai-precise-keyserver.service` was crash-looping `203/EXEC` for ~5 days. Cause:
the home dir was migrated to the **OWC RAID** (`/dev/md127` → `/mnt/owc/home/mark`)
~Jun 4; `/home/mark` is now a stale root-owned stub, so the unit's hardcoded
`/home/mark/...` paths (ExecStart, WorkingDirectory, state dir) + the venv
editable `.pth` all broke. Fixes: rewrote the `.pth` → `/mnt/owc/...src`; added
systemd drop-in `10-owc-home.conf` repointing WorkingDirectory/ExecStart/
`AIP_KS_STATE_DIR`. Now `active`, listening `100.81.162.68:8443`. **Verified live:**
paige made a real DEK fetch → granted in 33 ms (audit.log, unit_id=paige). paige
had been on the 14-day grace cache since the move.

### Open / next
- Make a **ZED systemd unit** for the app (no `--device`, `--ipc=host`,
  model-unlock pre-step) — current container is a manual `docker run`, won't
  survive reboot.
- **Rebuild the image** from `feat/zed-gmsl-camera` (retire the bind-mount hot-patch).
- **Commit** branch fixes (uncommitted): `install-zed-capture.sh`, `zed_capture_service.py` (CameraOne).
- **Audit other sparky services** for the same `/home/mark` stale-path breakage
  (vLLM, obsidian cron, orchestrator…). Systemic fix candidate: symlink
  `/home/mark → /mnt/owc/home/mark`.
- Keyserver **repo source** unit + venv shebangs still carry old paths.
- Phase-2: nozzle recal at 1920×1200; domain-gap vs DJI baseline; low-gate 0.12.

## Update: 15:54

### SMB share `[Documents]` fixed (same `/mnt/owc` migration)
`\\192.168.1.214\Documents` pointed at the dead `/home/mark/Documents` → repointed
to `/mnt/owc/home/mark/Documents` (smb.conf, backed up, reloaded). The OTHER 3
shares (`weed_samples`, `weed_152k`, `knowledge_raw`) + the ai-precise `.venv`
editable install are still on stale `/home/mark` paths — same Jun-4 migration.

### Two ZED capture clips → OWC (camera-assessment)
10s + 20s real-time clips off the shm bridge → `/mnt/owc/home/mark/Documents/2026-06-09_paige_zed_{10,20}s.mp4`.
Learned: NVENC is host-only via GStreamer/SVO (ffmpeg here is decode-only);
x264-pipe capture drops ~3% frames under back-pressure (buffer-to-RAM is the fix).

### "Choppy/laggy" investigation — TWO expert panels (max effort)
Root cause is the **WAN VIEWING PATH, not the camera**: measured the camera at a
metronome 30.2fps (sensor-ts 33.3ms ±0.1, zero drops). Both choppy surfaces =
one Mac (elon-m2) pulling the live view + SMB clips over a **~12 Mbps Tailscale
tunnel** while sitting on paige's own LAN (10.0.0.x, 3ms). Free fixes confirmed
by operator: **copy clips local** = "fantastic"; **use `http://10.0.0.11:8080`**
(LAN) for the live view. The panel SURFACED the real bug ↓.

### SHIPPED: firing staleness-gate fix (the real find)
The ZED switch silently broke firing Gate 0: host stamps ZED **realtime** ts,
`pipeline.py:474` subtracts **monotonic** → `max(0,…)` → `frame_age≡0` → gate
never trips. Fixed in `gmsl_backend.py` (convert ZED ts → monotonic domain) +
loud guard in `pipeline.py`. **Verified live on paige: frame_age 0 → 189ms.**
Committed `c785b5a` + regression test. (Latent now; catastrophic if Phase-2.3
wires frame_age into spray lead.)

### Dual-camera build (2nd ZED X One, nozzles 8–15) — plan + 6 commits
Planned via 3 Explore agents. Backend FUNCTIONALLY COMPLETE on
`feat/zed-gmsl-camera`: P1 per-slot bridge (`531c23c`), P2 schema v4 camera↔nozzle
(`132ed2b`), P3a FrameBuffer registry (`1050433`), P3 dual pipelines + shared
detector/controller (`02e2596`), P4 backend per-camera frame.jpg API (`397f47a`).
Two independent monocular pipelines (NOT stereo), each fires only its 8 nozzles
(structural restriction), all 16 on one Controllino. **Remaining:** front-end
side-by-side (deploy-verified, needs 2 live feeds) + the 16-nozzle config + deploy.
Plan: `~/.claude/plans/purring-mixing-cocoa.md`.

## Update: 16:38

### Dual-camera Phase 4 front-end + DEPLOYED LIVE on paige
Finished Phase 4: side-by-side 2nd canvas (`ui/index.html` + `live.js`, additive —
single-cam path unchanged) + `calibration.js` real-id fix (renderers were
`i+1`-synthesizing N1..N16 → mis-targeted 8-15; now use real config ids).
Commits `dd7eabc`,`54aa63c`. **Rebuilt the paige image** (cached/instant; new
dual-pipeline + UI code baked in — no more bind-mount). **DEPLOYED + VERIFIED:**
`zed-capture@{0,1}` → 2 shm segments (slot-1 first light, SN 309009949);
container builds **2 pipelines**; `/state` = 16 nozzles grouped `{cam0:8, cam1:8}`;
both `frame.jpg?camera=0|1` serve. Dual config committed `7aba704`. inference 70ms
(MAXN). frame_age 326ms (2 pipelines serialize on 1 GPU — gate armed). **Open:**
per-cam geometry recal at 1200 (cam1 boxes are cam0's template); live weed-in-cam1
firing test; proper systemd unit (manual `docker run` now); TRT/optimize for
moving-boom per-cam fps.

### paige HANG root-caused + fixed (agents) — headless-display DCE wedge, NOT compute
Box hung → operator manual-rebooted. A reliability agent CORRECTED the first read:
not OOM/thermal/compute-load — a **headless-display DCE wedge** (`NVRM_RPC_DCE:
Failed RM ctrl` flood from EDID-read-fail on DP-0, 157×/session, fires 5s into
boot). The systemd HW watchdog IS armed (2min) but **can't catch a DCE wedge**
(PID1 stays alive → keeps kicking through it); the `ai-precise-wd` kicker is a
broken (EBUSY single-open) redundant feeder. MAXN already on (not 50W). **Fixed:**
headless (`set-default multi-user.target` → DCE errors 157→**1**), persistent
journal on eMMC (1G cap), disabled the broken kicker + the stale DJI `ai-precise.
service`. **Deferred:** liveness-gated watchdog (DCE-wedge auto-recovery net for
unattended use) — lower urgency now headless killed the trigger.
