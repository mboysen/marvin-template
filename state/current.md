# Current State

Last updated: 2026-06-11

## Active Priorities

0. **AI Precise — ZED X One GS: FIRST LIGHT achieved, ai-precise running on the ZED feed (2026-06-09)** — Branch `feat/zed-gmsl-camera`. Full chain LIVE on paige (Tailscale `mark@100.96.197.31`; LAN `.3.38` had no route): **ZED X One GS → host capture (pyzed/Argus) → `/dev/shm/ai_precise_zed_frame` bridge → container detector (`--ipc host`, NO `--device`) → RF-DETR → tracker → firing.** Host `zed-capture.service` publishes **1920×1200@30fps** (locked ISP), auto-starts on boot. Container: RFDETRBase loaded, ~8.6 fps (109ms fp32), 40+ weeds, UI/API on `:8080`. **The 06-08 "0 cameras" blocker was MISDIAGNOSED** — not daemon/SDK/numpy versioning but our code using stereo `sl.Camera` on a MONOCULAR ZED X One; switched `zed_capture_service.py` to **`sl.CameraOne`** → both cams enumerate (SN 309878703/309009949). Also fixed 3 `install-zed-capture.sh` bugs (venv flag, `requests`, SDK lib perms 700/600 → `chmod a+rX` + ldconfig + `libturbojpeg`) and rebooted to clear a wedged GMSL daemon. **GEOMETRY_*_FAIL is EXPECTED + PARKED** (nozzle geom is 1080-calibrated, ZED is 1200 → Phase-2 recal). **Open:** (1) ~~ZED systemd unit~~ **RESOLVED 2026-06-11** — an enabled `ai-precise.service` exists (`docker run --rm --runtime nvidia --ipc=host`, Restart=on-failure; auto-started the dual-cam pipeline 25s into the 06-11 boot); (2) **rebuild image** from `feat/zed-gmsl-camera` (the new bridge-reader `GMSLBackend` is bind-mounted as a hot-patch over the stale ~06-02 image); (3) **commit** uncommitted branch fixes; (4) Phase-2 recal at 1200. See `sessions/2026-06-09.md` + decision log 2026-06-09.

**PM UPDATE (2026-06-09):** (a) **Choppy/lag = WAN viewing path, NOT the camera** — measured camera at metronome 30.2fps; both choppy surfaces = one Mac over a ~12 Mbps Tailscale tunnel while on paige's LAN. Free fixes: copy clips local; live view at `http://10.0.0.11:8080`. (b) **SHIPPED firing staleness-gate fix** (`c785b5a`, verified live frame_age 0→189ms) — ZED realtime-vs-monotonic clock bug had disabled Gate 0; do BEFORE any Phase-2.3 lead cutover. (c) **DUAL-CAMERA (2nd ZED X One, nozzles 8–15) — DEPLOYED + VERIFIED LIVE on paige.** 12 commits `772a791`→`7aba704` (per-slot bridge, schema v4, dual independent pipelines w/ shared detector+controller + structural nozzle restriction, side-by-side UI, dual config). `zed-capture@{0,1}` → 2 shm segments (slot-1 first light SN 309009949); rebuilt image builds 2 pipelines; `/state` 16 nozzles `{cam0:8, cam1:8}`; both feeds serve; inference 70ms (MAXN on). **Remaining (operator-side):** per-camera geometry recal at 1920×1200 (cam1 boxes are cam0's template → GEOMETRY_FAIL on cam1); live weed-in-cam1 firing test; ~~proper systemd unit~~ RESOLVED 2026-06-11 (enabled `ai-precise.service` auto-starts the container); TRT/optimize for per-cam fps (frame_age 326ms under GPU sharing). Plan `~/.claude/plans/purring-mixing-cocoa.md`. (d) **SMB `[Documents]` share** repointed to `/mnt/owc` (other 3 shares + ai-precise `.venv` still on stale `/home/mark`). (e) **paige HANG fixed** — headless-display DCE wedge (NOT compute); went headless (DCE errors 157→1) + persistent journal + disabled broken kicker/stale DJI unit. Watchdog is armed but blind to DCE wedges → **liveness-gated watchdog deferred** (for unattended field use).

0a. **sparky keyserver FIXED — home migrated to OWC RAID `/mnt/owc` (2026-06-09)** — `ai-precise-keyserver.service` had been crash-looping `203/EXEC` ~5 days (paige on 14-day grace-cached DEK). Cause: home dir migrated to `/dev/md127` → `/mnt/owc/home/mark` ~Jun 4; `/home/mark` is now a stale root stub, breaking the unit's hardcoded paths + the venv editable `.pth`. Fixed via drop-in `10-owc-home.conf` (WorkingDirectory/ExecStart/`AIP_KS_STATE_DIR` → `/mnt/owc`) + `.pth` rewrite. Now `active` on `100.81.162.68:8443`; **verified** paige live DEK fetch granted 33ms (audit.log). **Open: audit OTHER sparky services** (vLLM, obsidian cron, orchestrator) for the same `/home/mark` breakage; systemic fix = symlink `/home/mark → /mnt/owc/home/mark` (deferred, operator's call). venv console-scripts (pip/uvicorn) have stale shebangs → use `./.venv/bin/python -m <mod>`.

0aa. **v4a (cotton, RFDETRMedium, white-box watermarked) ACTIVATED as PRIMARY on paige (2026-06-11)** — supersedes "active model stays MN". `router.env AIP_MODEL_BUNDLE=_base/v4a_cotton` + config `model_version` + MN per-class list CLEARED (lawn names would zero-gate the cotton taxonomy). Verified: keyserver decrypt `paige-v4a-cotton-v1` live, 12 cotton classes enabled @ global 0.15, inference 74-85ms, 5.8fps/cam. Bench scene = ~0 detections (expected, non-cotton). **Revert to MN:** restore `/etc/ai-precise/router.env.bak-mn` + `config.yaml.bak-mn-1300` + restart. Router overlay verified working post-hardening (both DEK paths exercised live today).

0b. **AI Precise 1.2.10 — per-species firing funnel LIVE on paige (2026-06-02)** — `mboysen/ai-precise` main (`cba3b5e` feat + `4e4231e` docs). Added `fires_by_class` + `in_zone_by_class` to `/api/v1/state` (threaded `class_name` through `FireCommand`→`FIRE_LOG.record()`/`WeedsByNozzle.observe()`); operator can now see the full species × {detected, in-zone, fired} matrix. Unit nuance: detected/in-zone = distinct weeds, fired = fire events (multi-nozzle adds up) → per-species fired can exceed in-zone. Rebuilt + deployed on paige, verified live. Ran 4 DJI field tests (v1–v4) via loopback `frame.jpg`-poll → ffmpeg → resumable rsync; videos on workstation `~/Documents/2026-06-02_paige_dji{,_v2,_v3,_v4}.mp4`. **v4 matrix:** 350 detected / 163 in-zone / 150 fired; **common_purslane 10 in-zone → 0 fired** (new visible miss). **Storage cleanup DONE (eMMC 83%→39%):** containerd images→NVMe (bind-mount; Docker uses containerd snapshotter so images were on eMMC), syslog→NVMe + installed logrotate (was missing) w/ 30-day cap, stale tags removed, `RequiresMountsFor=/mnt/nvme` fail-safe. **NVMe SMB share** `paige_captures` (same creds as workstation shares, scoped to captures not full NVMe). **v4a (watermarked) DEPLOYED to paige** — encrypted `outputs/v4a_wm_whitebox/checkpoint_whitebox.pth` → `_base/v4a_cotton`, DEK `paige-v4a-cotton-v1` registered, paige dual-licensed, decrypt-verified; active model still MN (v4a=cotton). **v5/v6 tests** on NVMe; v6 (29 classes enabled) → fire% is a clean FP discriminator (real weeds 55–99%, impossible species 0–9%). **MARVIN runs ON sparky** (`ssh sparky`=loopback). **Open:** remove stray plaintext MN checkpoint at `~/Documents/ai-precise/models/_base/MN_weedmodel_final/checkpoint.pt` on paige (outside encryption); clock-lock persistence (reverted 4–5×); source-fix syslog firehose; per-site allow-list; root-cause purslane; boundary-box geometry; flip paige to enforce. See `sessions/2026-06-02.md` PICK-UP + `ai-precise/HANDOFF.md` + `docs/learnings/2026-06-02.md`.

1. **D5 model watermark — BOTH production models watermarked (2026-05-30)** — `mboysen/ai-precise-router` (security overlay repo). **MN_weedmodel_final: black-box** watermark embedded + accepted (100/100 trigger hits, p≈0, slot 73) → `weed_detection/outputs/MN_weedmodel_wm2/train_out/`. **v4a (cotton-weed): white-box** watermark (RFDETRMedium has no emittable dead slot — slot 12 is background) embedded + accepted (corr 0.20 vs 0.10; cotton mAP 0.9475→0.9464, drop 0.0011) → `weed_detection/outputs/v4a_wm_whitebox/checkpoint_whitebox.pth`. GPU restored on sparky (HWE nvidia module re-tracked to kernel 1021, no reboot). Vaults re-sealed (output-index convention + model-aware `first_dead_slot`); commitments in `ai-precise-router/docs/WATERMARK.md` (MN `9a5fe4a0…`, v4a `db1ca3ba…`). **Next:** MN ship-hardening — metadata-sanitize the checkpoint (it leaks `__watermark_signal__` at slot 73), wire a real mAP gate into `embed_watermark.py` (it passed by default), field-validate; parameterize `embed_watermark.py` for non-Base archs. See `sessions/2026-05-30.md` "Evening session" + PICK-UP.

2. **AI Precise Phase 1 — v1.2.0 SHIPPED + Pareto top-8 synthesized (2026-05-27)** — `mboysen/ai-precise` @ branch `feat/unified-calibration-tab` (committed end-of-day). Image rebuilt cleanly on paige_springs (mark@192.168.1.254, JP6.2); no more bind-mount/`docker cp` hot-patches. Best yard test: **801 weeds tracked, 208 fires, 7/8 nozzles, 26% spray rate, ~5 fps** (120-sec walking, DJI UVC). Shipped today: per-stage `StageTimings` instrumentation, 8-nozzle config + per-nozzle observability (`WEEDS_BY_NOZZLE`, `fires_by_nozzle`), camera-label overlay, per-nozzle 2-point ground calibration backend + UI, boom-speed slider, `compute_lead_box()` formula, rapid-draw boundary-box UI with auto-save on 2nd click, strict-PATCH (refuses 503 instead of silent-defaults-merge), `/api/v1/effective-config` + EFFECTIVE_CONFIG_* startup logs, 18 filter-contract tests, no-cache middleware (kills SW-staleness class), version-bar fix. Five parallel PhD expert reviews (Perception, Mechatronics, Software Architecture, NVIDIA Edge, Software Engineering Process) synthesized into Pareto top-8 at `ai-precise/docs/learnings/2026-05-27.md`. **Top "ONE thing this week":** extend boundary boxes upward from bottom 20% strip to mid-frame + swap lead-box velocity input to `boom_speed × forward_unit_vector × pixels_per_meter` → expected 26% → 50%+ spray rate. **Next pickup:** (1) geometry fix; (2) wire `controller_status` E-stop into FiringEngine (P0 safety); (3) strip `fsync()` from `DetectionLogWriter` + gate behind `AIP_DEBUG_DETECTIONS=1`. See `sessions/2026-05-27.md` PICK-UP section + `ai-precise/HANDOFF.md`.

3. **AI Precise Phase 1 — backyard shakedown CALIBRATED, service offline (2026-05-26)** — `mboysen/ai-precise` @ `d172c3f`+hot-patch (v1.0.0). Pipeline ran successfully through 5 yard tests on paige_springs (mark@192.168.3.38, JP6.2). All calibration persisted to live config.yaml + repo. Findings: model detects plantain (peak 0.94), white_clover (peak 0.98), creeping_charlie + dandelion ~0.6, common_purslane peak 0.73, chickweed 0.27 borderline. Per-class thresholds: plantain/clover 0.60, dandelion/charlie 0.40, chickweed/purslane fall to global 0.35. Inference p50 ~200 ms fp32 (fp16 REVERTED 2026-05-26: had been silently enabled 22:12 on 05-22 via manifest hand-edit, degraded outdoor confidence). Bug fix shipped: `FireCommand.track_id` was missing from schema. Feature shipped: `WeedsSeen` singleton + `weeds_detected_total`/`_by_class` on `/api/v1/state`. Peer-reviewer flagged: cycling is multi-pair (NOT just plantain↔clover), calibrator is identity, per-class 0.60 may suppress production at field distance. Production strategy ≠ backyard config: at boom 2-4 mph + 4 FPS we get 2-4 frames per weed (track_age must stay at 1), so production wants low threshold (0.10-0.15) + tier-1/tier-2 dedup, NOT high threshold.

4. **Deploy production weed model** — DONE for backyard: 93.7% mAP MN model staged + running on paige_springs (`/home/mark/paige_springs/data/models/_base/MN_weedmodel_final/checkpoint.pt`, sha-verified; src `outputs/MN_weedmodel_final/checkpoint_best_ema.pth`).
5. **Determine production robot camera** — Resolved for Build 1: ZED X One GS Narrow + ZED Link Capture Card Duo (GMSL). DJI Osmo Action 4 (USB-UVC) is the first backyard-test camera — connected to paige_springs via stable udev symlink `/dev/ai-precise-cam` → `/dev/video0` in container (1920x1080@30fps), pipeline live on it. Multi-camera selectable in the UI.
6. **Active learning pipeline design** — Telemetry feedback stream + per-fire image audit trail is in the v2.1 architecture (§4.10 + §4.11.8); operator FP/FN tagging UI is in the UI scaffold; upstream training-pipeline ingestion is Phase 2.
7. **Wyoming rye-in-wheat detection** — Production pipeline complete (`detect-rye` CLI). Ensemble (DINO + RF-DETR) → 97.3% classifier → 365 rye detections on 96 tillering images. Software work exhausted — next improvement requires new field data. Plan at `.claude/plans/zippy-mapping-naur.md`.

## Open Threads

- **Controllino firing link — Ethernet pivot VALIDATED END-TO-END (2026-06-10).** CAN was dead (MCP2515 silent on SPI + gs_usb driver missing on kernel 5.15.148-tegra) → pivoted to the Controllino's **on-board W5100 Ethernet via custom UDP** (reuses `can_messages.py` codec). **PROVEN LIVE:** Jetson `EthernetController`+factory+sim (`controller/eth_client.py`,`factory.py`,`mocks/udp_controllino_sim.py`, wired in `pipeline.py`, 64 tests green/ruff clean); Controllino `eth_hello` + **real firing firmware `eth_fire`** (CAN firmware w/ only the transport swapped, safety byte-identical) both compile+flash on paige; `eno1`→`192.168.50.1` direct cable to Controllino `192.168.50.2:8888` (WiFi carries paige uplink); **ping 0% sub-ms + `FIRE→ACK status=OK` over the wire.** **NEXT to resume:** (1) reflash `eth_fire` w/ `BENCH_BYPASS_ESTOP=1` (committed 0/safe; no E-stop wired→fires blocked) → FIRE → relay LED (dry, no valves); (2) integrate `EthernetController` into the running ai-precise container (bind-mount + `AIP_CONTROLLER=eth` + `AIP_ETH_HOST=192.168.50.2` + restart) → detection-driven fires (geometry ~98% FAIL → calib test-fire is the reliable demo until recal); (3) prove safety KILL paths on an instrumented relay before any steam. See decision log 2026-06-10. CAN firmware `5108f9b` kept for reference; paige uplink is **PSC-Guest WiFi — declared permanent + hardened 2026-06-11** (powersave off, autoconnect priority/retries, stale profiles disabled).
- **AI Precise HARDENING SWEEP — DEPLOYED END-TO-END 2026-06-11** (committed `dfbaeb9`/`e898b1a`/`78c15af`+guard-fix, pushed; paige image rebuilt + running; **fw 0.2.0 FLASHED + app LIVE on the real Controllino over eth** — handshake passed, heartbeats feeding watchdog, self-heal live-tested: cam1 rode through a capture restart). First flash caught a live bug: CAN-era `len>8` guard ate the 9-byte STATUS_CTRL → fixed+reflashed. Fires double-blocked (unwired E-stop + pre-connect watchdog latch) = safe bench state. **DRY-FIRE END-TO-END PASSED 13:05** — all 3 tiers: test-fire n0 ✓, cold-flush ALL 16 relays ✓ (channel 6-15 map VERIFIED), detection-driven fires flowing (37 fires, both cameras, seq-namespacing visible live). Also wired 2 more stub routes to the real controller (`/calibration/test-fire`, `/cold-flush`) + fixed bypass-must-report-estop-clear. **⚠ Board runs the LOCAL bench-bypass build (BENCH_BYPASS_ESTOP=1, not committed) — reflash committed 0/safe build before E-stop wiring or any steam.** Remaining: gain-range read-back scale check; geometry recal (fires_by_nozzle = live per-lane FP meter until then). Original sweep scope (27 files +1425/−260 + 5 new): All review findings fixed: the 2 wire bugs (+1 more: hardcoded bus_state) + 12 firmware-byte golden vectors; **heartbeat thread** (firmware watchdog latched after 2.5s silence → firing bricked every quiet run); controller I/O lock (ACK cross-matching); ACK-truth policy + `is_healthy` firing gate (controller-liveness interlock DONE); thermal-UNKNOWN backpressure fix; bridge self-heal (zed-capture restart no longer freezes a camera silently); honest /health (503 + reasons) + exit-nonzero on launch failure; real /all-off (was a stub!); log firehose default-OFF (`AIP_LOG_PIPELINE_STAGE`); **firmware eth_fire 0.2.0** (16ch pins 22-37, valid-frame watchdog refresh + IP pinning, bounded RX drain, cooldown-from-close, channel-count handshake) — compiles on paige (14.2KB). Unit template now `AIP_CONTROLLER=eth` + retry-forever. **594 tests pass** (15 pre-existing env failures on clean HEAD; jetson/.venv editable install broken by home migration → `PYTHONPATH=src`). Camera audit: **settings correct** (bounded AEC, NOT locked exposure; keep HD1200@30). Perf: `jetson_clocks` applied live → **inference 110→73ms**; `deploy/jetson-clocks.service` for persistence. **NEXT: (1) commit; (2) flash eth_fire 0.2.0 at bench FIRST (manual-reset gotcha) — new unit env vs old 6ch fw = intentional named crash-loop; (3) rebuild image on paige + host git pull; (4) do NOT restart zed-capture@N on paige until new code deployed (old code unlinks shm → freezes live feed); (5) install jetson-clocks unit; (6) bench: relay LED verify ch 6-15 + dry FIRE w/ BENCH_BYPASS_ESTOP.** Deferred perf A/Bs: fp32 compile, preprocess-at-560, predict_threshold 0.05. See decision log 2026-06-11.
- **paige freezes ROOT-CAUSED (2026-06-11) — WiFi-failure-correlated silent SoC hang; Argus/GPU theory demoted.** The hard freeze (previously "freeze #2, 06-10" — actually Tue 6/9 8:12pm local) followed a **WiFi uplink death 4 min earlier** ('AxisAg -5G' link timeout → failed re-association loop to the end) while pipeline/GPU/cams ran healthy to the final 200ms; kernel silent, pstore empty → prime suspect = **RTL8822CE on the out-of-tree `rtl88x2ce` PCIe vendor driver** wedging the Tegra fabric during the association storm (HW watchdog caught it — 2min reset worked). Mon + Tue-afternoon hangs were the DCE wedge (fixed by headless, zero recurrence). nvargus noise + GR3D thrash appear at identical rates on healthy boots = background, not precursors. Wednesday-clean was confounded (no WiFi drops AND no pipeline — Ethernet bench day). **APPLIED 2026-06-11 (operator: PSC-Guest is the permanent uplink):** WiFi powersave OFF (live + persisted; tunnel RTT 263→78ms), PSC-Guest autoconnect priority 20 + infinite retries, stale 'AxisAg -5G' + 'prettyfly4wifi-guest' autoconnect disabled. paige LAN now `10.99.9.114` (10.99.0.0/16). Freeze-boot journals were vacuumed <24h (1GB cap + log firehose) — postmortem archive saved the evidence; gate per-frame logging remains the source fix. Pull after any future freeze: `rsync -av mark@100.96.197.31:/mnt/nvme/forensics/ ~/paige-forensics/` (tegrastats vitals now cover it). **Open:** RTC battery (operator will replace); optional at-bench test of in-tree `rtw88_8822ce` or `pcie_aspm=performance` (do NOT swap remotely — WiFi is the only path in); single-vs-dual camera isolation test (now secondary). See decision log 2026-06-11.

- **Lawn weed model: 93.7% mAP 50:95, 96.1% mAP 50** (Run 5), checkpoint at `outputs/MN_weedmodel_final/checkpoint_best_ema.pth`
- Full pipeline: 80.2% → 84.3% → 87.5% → 87.8% → 90.8% → **93.7%** across 5 iterations
- Digital twin integrated (930 frames, 3,532 annotations, 29 species — only crabgrass missing)
- Zeus powered off, DGX Spark has all checkpoints and data
- LinkedIn proxy token expires 2026-04-17
- **Rye detection production pipeline:** `detect-rye` CLI at `scripts/detect_rye.py`. Checkpoints: detector `outputs/wyoming_round0/checkpoint_best_ema.pth`, classifier `outputs/wyoming_classifier/best_classifier.pth`
- **ACTION: Email Dr. Sami Khanal** (khanal.3@osu.edu, OSU FABE) for overhead cereal rye UAV imagery from KC, Romanko, Perrault, Khanal 2024 (*Precision Agriculture* 25:2198–2225, 15 NW Ohio farms, multispectral UAV, March–May). Previous outreach to Huddell (Delaware) was wrong — she replied she has no UAS publication; different 2024 rye paper (Ag & Env Letters, biomass+weather modeling, no imagery).
- **ACTION: Schedule heading-stage field collection** late May–June Wyoming. This is the single highest-leverage improvement available.
- **ACTION: Confirm wheat variety** with Wyoming contact (likely Crescent AX or Pronghorn)
- Label Studio running on sparky:8080 (mark@mark.com / mark) — requires `LOCAL_FILES_SERVING_ENABLED=true` + `LOCAL_FILES_DOCUMENT_ROOT=/home/mark/weed_detection` exported before start, else 403 on `/data/local-files/`
- Obsidian wiki: cron sync running hourly 9–5 M–F with remote harvest from Mac + Geekom. Concept template externalized to `.compiler/concept-template.md`. 7 plugins installed.
- **Wiki tooling added 2026-04-15:** `harvest-claude-desktop.py` (claude.ai export → markdown), `harvest-google-docs.py` (Google Docs → markdown via public share or OAuth), `frozen: true` frontmatter flag honored by both compilers.
- **Wiki cleanup:** (1) ~~349 bogus project dirs~~ DONE; (2) ~~patch harvester~~ DONE; (3) ~~fold `meetings.md` + `crm.md` into `naware-crm.md`~~ DONE 2026-04-17; (4) ~~step-4 "traceback"~~ DONE 2026-04-29 (was reorganize.py OSError 36 from 5596-file recurrence loop, cleaned + SKIP_TOP_DIRS fix); (5) ~~Mac harvest upstream cleanup~~ DONE 2026-04-30 (5595 corrupted files deleted from `mark@192.168.1.5`, reorganize.py defense-in-depth `_raw_` count guard added); (6) ~~broken symlink cleanup~~ DONE 2026-04-30 (5756 dangling links across `raw/projects/`); (7) **lucas hub manually updated** through 2026-04-24 (Opus auto-compile failed — hub template is prose, not LLM directive; reverted to manual append). Knowledge-base handoff at `~/Documents/apps/LLM_app/HANDOFF.md`.
- **Other hubs need manual append too:** lucas done. 18 other frozen hubs likely have stale Session Log tables. Apply same pattern (read post-frontmatter sessions, summarize as one-line table entries) when those projects need refreshing.
- **ESA SNAP 13 installed on sparky 2026-05-01** at `/home/mark/esa-snap/` for Sentinel imagery work. Uses ARM64 OpenJDK 21 (`/usr/lib/jvm/java-21-openjdk-arm64`) — bundled x86 JRE bypassed by invoking install4j directly post-extraction (wrapper hardcodes `app_java_home` before checking `INSTALL4J_JAVA_HOME_OVERRIDE`, can't be overridden cleanly). Heap capped `-J-Xmx32G` (was default 85G). NetCdfActivator throws SEVERE on every startup — packaging bug, ESA shipped macOS Mach-O dylibs in the Linux ARM64 native bundle (`~/.snap/auxdata/netcdf_natives/13.0.0/aarch64/*.dylib`). Cosmetic only; engine + Sentinel-2 ops (`Bais2Op`, `c2rcc.msi`, `RayleighCorrection`) work fine. Aliases `esasnap` and `gpt` in `~/.bashrc`.
- **GUI app crashes on sparky 2026-05-01 root-caused.** Two distinct issues: (1) Firefox/Obsidian crashed during vLLM cold-load → NVRM OOM at 16:45 wedged Tegra GPU contexts for new clients. Fixed by `--gpu-memory-utilization 0.6`. (2) Obsidian AppImage AppArmor `unprivileged_userns` denial — **fix applied 2026-05-08** via `/etc/sysctl.d/60-apparmor-userns.conf`.
- **Snap-store / Ubuntu Software broken 2026-05-01:** TCP 443 to `api.snapcraft.io` (185.125.188.0/22) is blocked upstream at the router/ISP level. ICMP, DNS, and other HTTPS destinations work fine. Snap-store removed. GNOME Software (the deb package, branded "Ubuntu Software" on this ARM64 install) was hanging because its `gnome-software-plugin-snap` blocked on the same unreachable host — **plugin removed**, GNOME Software now functional for debs. Snaps remain unusable until router rule is fixed (allowlist `185.125.188.0/22`).
- **Wiki hub consolidation (2026-04-17):** 19 frozen hubs + 4 redirect stubs. New template at `naware-knowledge1/.compiler/hub-concept-template.md` + guardrails. Obsidian Sync resurrection gotcha documented (use redirect stubs, not deletes). See decision log 2026-04-17 entries.
- **Naware Agent V2:** Orchestrator skeleton built at `/home/mark/Documents/sparky_infrastructure/orchestrator/` (79 tests passing, polling mode, tier-aware routing). Not yet deployed to GB10. **2026-04-27:** `models.py` now passes `chat_template_kwargs={"enable_thinking":false}` when `VLLM_ENABLE_THINKING=false`, and reads `content` → `reasoning_content` → `reasoning` in that order so Qwen3.6 / Nemotron / vanilla all parse cleanly. Config field added: `vllm_enable_thinking`. Next: add WAL mode + stuck-row sweep, wire Gmail draft creation, deploy.
- **vLLM Nemotron 3 Super:** Loading in Docker on sparky (container `nemotron-vllm-verify`, port 8000). Paused mid-load 2026-04-17 at ~30 GB of 60-70 GB HF weights. Persistence drafted (NOT installed): `/home/mark/Documents/sparky_infrastructure/scripts/vllm-nemotron-{start.sh,service,install.sh}`. To resume: `curl -s http://localhost:8000/v1/models`; when populated, run `sudo bash …/vllm-nemotron-install.sh` (prompts for scoped HF token).
- **vLLM Qwen3.6-27B-FP8 on sparky:** systemd-managed since 2026-05-01, **stable 7+ days uptime as of 2026-05-08**. Container `vllm-qwen36`, `100.81.162.68:8001`. Image `vllm-nemotron-spark:local`. `--gpu-memory-utilization 0.6` (lowered from 0.75 on 05-01 after NVRM OOM that wedged GUI clients — Firefox/Obsidian crashed). Throughput ~8 tok/s, max_model_len 65536. Service unit + installer at `sparky_infrastructure/scripts/vllm-qwen36{.service,-install.sh,-start.sh}`.
- **Harvest toolkit synced 2026-04-17** to geekom (`ava@geekom.local`) + Mac (`mark@192.168.1.5`). All 3 machines now have identical `harvest-*.py` + `harvest-*.sh`.
- **qwen3-boysen LoRA fine-tune complete (2026-04-27):** End-to-end pipeline ran on sparky. Gmail export → 535 pairs (Instantly skipped — 20 req/min API rate-limit fought every retry; 535 Gmail pairs alone clears the 100-pair LoRA floor 5×). Built 402 train / 93 eval, classified via vLLM Nemotron (parallelized 4-wide). LoRA train 3 epochs, 44 min on GB10, train 1.337 / eval 1.477. Merged + quantized to Q5_K_M (5.5 GB) → deployed to Ollama as `qwen3-boysen:latest`. Automated eval: stock qwen3:8b 52% / finetuned 58% (+6 pt). **Next: blind human review of `scripts/finetune/data/eval_results.html`; if finetuned ≥70%, flip orchestrator `DRAFT_MODEL=qwen3-boysen:latest`.** Patches landed in 01/02/03/05 scripts: Instantly v2 returns `items` not `data`; reasoning_content/reasoning fallback for Nemotron null-content; TRL 1.2.0 API renames (`max_seq_length`→`max_length`, `tokenizer`→`processing_class`, `dataset_text_field` moved to SFTConfig); base model id `Qwen/Qwen3-8B` (no `-Instruct` suffix). vLLM Nemotron container `nemotron-vllm-verify` came up cleanly from `~/.cache/hf-test` in ~13 min; currently stopped. INSTANTLY_API_KEY now populated in `orchestrator/.env`.
- **inboxing.com bulk rename (2026-04-20):** 6 domains × 49 mailboxes (294 total) to be renamed to `mark`/`kelsey`/`sude` pattern. Project at `/home/mark/Documents/marvin-template/projects/inboxing/`. CSVs + paste blocks generated. API key in `.env` as `INBOXING_API_KEY`. Public v2 API is read-only for mailboxes — awaiting DevTools capture of dashboard's internal rename endpoint to drive the bulk update. Fallback: Microsoft Graph API against M365 tenants.
- Obsidian wiki cron fixed (PATH bug for `uv` was silently breaking syncs since Apr 11)
- **AI Precise backyard-test bench (2026-05-21):** queued tests to run on `paige_springs`. (1) JPEG live endpoint render path; (2) per-class enable/disable PATCH roundtrip; (3) per-class confidence override PATCH; (4) mode state-machine (ready→operating→ready); (5) E-stop / all-off; (6) `/diagnostics/recent-faults` + `recent-fires` recording; (7) calibration POST→state roundtrip; (8) model list/load; (9) container GPU sanity (`torch.cuda.is_available`); (10) full 347-test suite locally.
- **AI Precise label cycling — root-caused (2026-05-21):** the `argmax-over-noise` hypothesis was confirmed live. Threshold sweep PATCH 0.1→0.5→0.9 on the live pipeline produced the expected overlay change (8 noise labels → 0 labels), proving end-to-end: PATCH→DetectionPolicy (same object id), gate filters at `pipeline.py:251-255`, overlay layer 2 paints `det.class_name + score` per frame with no temporal smoothing. Operator mitigation: keep `global_min_confidence ≥ 0.5` for production lawn use. **Bug filed:** RF-DETR model outputs class IDs ≥30 (saw 43, 73, 39) — checkpoint has 91 output slots vs 30-class taxonomy; detector currently maps out-of-range → empty string, but those rows still create yellow overlay boxes with blank labels. Address by either (a) dropping rows with `class_id >= len(taxonomy)` at detector level, or (b) post-export ONNX/checkpoint surgery to slice the head to 30. **Cosmetic bug:** overlay label text gets clipped at frame edges (`creeping_charlie` → `arlie`). Low priority.
- **AI Precise camera-symlink permanent fix (2026-05-21):** DJI Osmo Action 4 was reshuffling kernel videoN on every USB hot-plug, breaking the systemd unit's hardcoded `--device /dev/video0` (restart-loop, exit 127). Added `deploy/udev/99-ai-precise-cam.rules` (vendor 2ca3:0023 + `ATTR{index}==0` → stable `/dev/ai-precise-cam` symlink); systemd unit now uses `--device /dev/ai-precise-cam:/dev/video0` so container code stays unchanged; `install.sh` step `[9b/13]` installs the rule. **Side effect of the trial-and-error:** an earlier manual `ln -sf /dev/video1 /dev/video0` had replaced the kernel char device with a symlink chain that resolved to the UVC *metadata* node (not capture) — `rm /dev/video0 && udevadm trigger --action=add` recreated the proper char device. Document this in BACKYARD_TEST_RUNBOOK as the recovery procedure.
- **paige_springs moved subnet 2026-05-20:** old `mark@192.168.1.254`, new `mark@192.168.3.38`. `deploy/machines/paige_springs.env` updated. Tailscale path unchanged.
- **AI Precise hot-patch 2026-05-26 (uncommitted):** 5 files edited in repo + scp'd to box bind-mount at `~/ai-precise-patch/ai_precise/`. (1) `types.py` — added `track_id: int | None` field to `FireCommand` (was completely missing → all fires had `track_id: null`). (2) `firing/firing_engine.py:371` — pass `track_id=t.track_id` when constructing FireCommand. (3) `pipeline.py:425` — pass `track_id=cmd.track_id` to `FIRE_LOG.record()`. (4) `telemetry/fire_log.py` — added `WeedsSeen` singleton (thread-safe set + per-class counter). (5) `pipeline.py` _broadcast_detections — write to WEEDS_SEEN on each new track (refactored out of `on_detection`-only path). (6) `ui_server/routes/state.py` — added `weeds_detected_total` + `weeds_detected_by_class` fields to StateSnapshot, populated from WEEDS_SEEN. Repo `configs/paige_springs.yaml` also updated to match live (N1 box, per-class thresholds, `inference_p50_ms_at_calibration: 200.0`). All verified via field test 21:02 UTC: 10 fires with non-null track_ids [7,35,44,46,53,56,60,62,65,66], 32 weeds tracked (9 plantain + 23 clover). **ACTION: commit** when ready (5 src files + 1 config file).
- **AI Precise fp16 silent enable bug (2026-05-26):** `manifest.json` at `~/paige_springs/data/models/_base/MN_weedmodel_final/` was edited 2026-05-22 22:12 UTC (end-of-test) to enable `optimize_for_inference: true` + `optimize_dtype: "float16"`. State log never reflected this. Reverted today; bak at `manifest.json.bak-fp16-*`. Confidence on real-world outdoor scenes regressed under fp16 (RF-DETR head was trained in fp32; fp16 collapses score dynamic range into noise floor).
- **AI Precise per-class threshold config (2026-05-26):** plantain/clover at 0.60, dandelion/creeping_charlie at 0.40, chickweed + common_purslane DISABLED (peaks 0.26 / 0.38 — cannot break gate). Global `min_confidence: 0.35`. Reasoning: at gate 0.5 cycling was lower (15%) but dandelion never fired (peak 0.40); at gate 0.05 noise dominated; per-class gives the right band per species. Persisted via PATCH `/api/v1/detection` + `/api/v1/config`.
- **AI Precise calibration drift (2026-05-26):** `firing.inference_p50_ms_at_calibration` was 110.0 ms; actual measured fp32 inference 199 ms (95.7-200 ms p50 across multiple test windows). Updated to 200.0 — feeds the Kalman lead-shift `velocity × effective_latency_s` in `firing_engine.py:289`. Negligible on stationary backyard camera (v≈0) but hidden time bomb for moving boom production (1 m/s × 89 ms = 89 mm spray-aim error, ~one nozzle width).
- **AI Precise filed bugs (2026-05-26, all confirmed live):** (1) 91-slot RF-DETR head emits class_ids ≥30 (top: 43, 39, 42, 37, 34) — 0.26% of rows, detector maps to empty string but rows still influence overlay paint. (2) Plantain↔white_clover top-1 cycling 15-24% even at high confidence — honest model disagreement on similar broadleaf species, expected RF-DETR behavior on fine-grained ag classes. Fix is training-side (hard-negative mining) or tracker-side (class-stickiness gate). (3) Pipeline FPS ~4 vs camera 30 — inference-bound at fp32; matters for production moving boom (200 ms = 200 mm at 1 m/s, ≈one nozzle gap), irrelevant for stationary backyard.

## Recent Context

- 2026-05-26: AI Precise yard test day on paige_springs. Found+fixed fp16 silent enable (manifest hand-edit at Friday 22:12), null-track_id fire bug (FireCommand schema was missing the field), got per-class thresholds calibrated from 4 yard tests with the operator. Added a `weeds_detected_total` + by-class counter to /api/v1/state. Perception-engineer agent reviewed and identified additional findings (repo↔live config drift, inference calibration mismatch, plantain↔clover cycling = honest model behavior).
- 2026-05-08: vLLM Qwen3.6 systemd unit stable 7 days — auto-start gap from 04-27 closed.
- 2026-05-01: Big infra day. (a) Promoted vLLM to systemd, ran installer, lowered `--gpu-memory-utilization` to 0.6 after first cold-load wedged the GPU and crashed Firefox/Obsidian. (b) Installed ESA SNAP 13 on ARM64 by bypassing the install4j wrapper's bundled x86 JRE (full JDK21 workaround). (c) Diagnosed Obsidian AppArmor `unprivileged_userns` block (sysctl fix written but not applied). (d) Diagnosed snap-store outage as upstream TCP block to `api.snapcraft.io`; removed `gnome-software-plugin-snap` so GNOME Software works for debs.
- 2026-04-30: Wiki rescue part 2 — Mac upstream cleanup (5595 files), reorganize defense-in-depth, broken-symlink sweep (5756), lucas hub manually updated through 04-24. Hub Opus auto-compile rejected (template is prose); manual append is the pattern.
- 2026-04-29: Wiki step-4 "traceback" diagnosed — was reorganize.py OSError 36 from 5596-file recurrence loop. SKIP_TOP_DIRS fix + cleanup + 12 concept files patched. Pipeline now running end-to-end every cron tick.
- 2026-04-27: Qwen3.6-27B-FP8 deployed to vLLM on sparky (port 8001, Tailscale-scoped). qwen3-boysen LoRA shelved (incompatible base). Orchestrator wired with thinking-mode toggle + multi-field response parsing.
- 2026-04-14: Production pipeline packaged, iNat reference images didn't help (wrong perspective), Ohio State outreach drafted
- 2026-04-13: Two-stage pipeline breakthrough (97.3% classifier), ensemble architecture confirmed, wiki template refactored + recompiled, cron/sync fixes
- 2026-04-12: Full rye detection development: DINO go/no-go, Label Studio annotation, RF-DETR training (55.5% mAP), multiple training experiments confirmed data ceiling
- 2026-04-11: Ollama day-to-day pipeline, SMB share setup, rye-in-wheat project launched
- 2026-04-08: Run 5 bootstrap hit **93.7% mAP** on lawn weeds

---

*This file is the source of truth for session continuity. MARVIN reads it on startup and updates it on every /end and /update.*
