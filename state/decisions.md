# Decision Log

Rolling log of decisions made during MARVIN sessions. Referenced by `/start` for recent context.

---

### 2026-07-06 - Model choice DECIDED: stick with RF-DETR (falsifiable); flywheel v2 designed; Paige gate labeling started; GRASS = ground cover at Paige
**Decision:** (1) **Stick with RF-DETR** for live weed detection (field + turf), with a written decision rule making it falsifiable: revisit only if the human-labeled gate shows a failure data levers can't close, or latency fails at target boom speed after fp32-compile → Nano/Small → calibrated-TRT ladder. Reasoning rests on OUR evidence (v4a 94.65 mAP50:95 on CottonWeedDet12 vs published YOLOv8s 87.1 / RT-DETR 85.9; MN real-yard performance; MN→rye transfer beat COCO), NOT Roboflow's contested data-efficiency marketing (2 independent weed papers show RF-DETR losing; RF100-VL never tested it) — externally, lead with our measured numbers only. Verified landscape: RF-DETR edge sizes Apache-2.0 (XL/2XL now paid PML-1.0 = vendor-direction warning; "Large" silently 4× re-scaled mid-2025 → pin versions); Ultralytics AGPL with quote-only enterprise pricing; RF-DETR's TRT/Jetson story weakest surveyed (open NVIDIA TRT bug on AGX Orin; fp16 issues ecosystem-wide — our 05-26 collapse was the norm, lower TRT expectations); D-FINE/DEIM/LW-DETR = zero Jetson data + zero ag literature (switching = being first to validate). Comparator run (RT-DETRv2 + YOLO26 internal baseline) upgraded to load-bearing, gated on the eval set. Fixability verdicts (operator ask): **MN/turf HIGH** (real yard evidence; needs honest metric + production-camera validation — its 93.7% is CIRCULAR, bootstrap re-annotated val/test); **v4a/Paige LOW as-is → MODERATE after camera+768 → GOOD after in-domain fine-tune** (data timeline, not architecture). Doc of record: `weed_detection/docs/MODEL_CHOICE_ASSESSMENT_2026-07-06.md` (incl. commercial-viability path, 1-100 outcome calibration turf ~90 / Paige ~82-85 ceiling, video-training protocol). (2) **Flywheel v2** proposal → `weed-active-learning/docs/FLYWHEEL_V2_UPGRADES.md` (adds to locked spec: video ingestion + track-propagated labeling + split-by-clip; FP-driven + uncertainty harvest; per-site ledger + cycle report + lineage + rights tags; DINO stays proposer, humans = authority). (3) **Paige site rule of record: GRASS IS GROUND COVER** — never boxed as weed; grass-only frames are confirmed negatives; v4a `Goosegrass` class must be DISABLED in the paige firing config when firing restores; "grass clump" dropped from site prelabel prompts (recorded `labelstudio/LABEL_STUDIO_SETUP.md`).
**Context:** v2 was live-tested same-day on real captures: GroundingDINO swinB (GPU, 118s, Blackwell patches held) over the 162-frame paige 1fps sample → 132 class-agnostic boxes on 100 frames where full-frame v4a found 1 frame ≥0.3; DINO's #2 box landed 1 frame from v4a's tiled MorningGlory winner (independent convergence) — this satisfies the spec §5 iter-0 proposer A/B intent. Label Studio brought up on :8080 (home-migration gotchas: broken console-script shebang → launch via `venv/bin/python venv/bin/label-studio`; LOCAL_FILES env verified by behavior; newer-LS prediction semantics = read-only layer) — frames staged to `weed-active-learning/dataset/incoming/20260614_paige_1fps/`, species config per locked taxonomy (site species first), JWT API bulk-created 100 editable annotations + hid the prediction layer. **⚠ The operator's first labeling pass persisted NOTHING** (all interactions hit the read-only prediction layer; 0 annotations, 0 drafts) — redo agreed with a verify-first protocol (label 2-3 → confirm via API → full pass). 62 no-box frames NEED empty-submits (they're the precision/FP half of the gate; the deployment scene is ~90% weed-free so they under-represent emptiness if anything).
**Status:** Active — labeling redo pending (operator); on completion: freeze the gate slice (never trains), score deployed v4a at 576 AND 768 against it = first field-grade number + the 768-restore decision. WIP snapshot at `dataset/incoming/20260614_paige_1fps/labelstudio_WIP_snapshot_20260706.json`. LS server nohup'd on :8080 (log `~/.label-studio-8080.log`). Open: weed_detection `git init` + backups + license audit (assessment P0s); Zeus power-up items queued.

### 2026-07-06 - RF-DETR assessment acted on: 576-vs-768 resolved to a restore-to-trained lever, resolution-truth fix + lead-anchor field tool shipped, full punch list dispositioned by operator, committed+pushed
**Decision:** Close the loop on the same-day RF-DETR readiness audit (prior entry below) instead of letting it sit as a diary entry (the audit's own lesson #4). A second independent read-through re-derived the same verdict and both resolved questions from the code alone, then added the missing piece from evidence OUTSIDE the ai-precise repo: `weed_detection/weights/v4a/training_v4a.log` on sparky shows v4a **trained at resolution=768**, which — combined with the repo-only finding that it currently *serves* at 576 — re-ranks the quarantined `resolution: 768` manifest knob from "weakest lever" to "leading candidate once the eval gate exists" (it restores the exact PE-interpolation path exercised during training, not an extrapolation gamble). Also confirmed paige has been offline ~19 days (Tailscale last-seen ~2026-06-17), which makes the 6-15 mock-mode "is it still running" check moot (that bring-up was `systemctl start`, never `enable`). Operator then re-confirmed the geometry-gate framing once more precisely: a detection landing outside a nozzle's boundary box is BY DESIGN (not a defect) — the real, narrower defect is box *placement* (cam1 = cam0's copied template; cam0 drawn for the retired 1080 feed), not the fail-rate number. **Shipped same-day:** (1) `RFDETRDetector.load()` now probes the loaded model's ACTUAL forward resolution and lets it win `ModelInfo` + warmup with a loud mismatch warning — closes the exact silent-drift class (display constant ≠ runtime reality) that hid 576-vs-768 for months, same shape as the 2026-05-26 fp16 incident; (2) new `aip-nozzle lead-anchor` terminal command — the operator's requested "test + change the lead anchor from the field via terminal" tool: shows configured anchor vs. LIVE measured inference p50 (drift + aim-error in mm at the configured boom speed), `--from-live`/`--set` persist a corrected value via the config API (sanity-bounded, verifies the PATCH stuck, prints restart-to-apply + a field-verification procedure). 12 new tests (86 total pass; 14 pre-existing env failures unchanged from clean HEAD); ruff clean.
**Context:** Operator walked the full punch list and dispositioned every item explicitly rather than leaving it as an undifferentiated backlog: **do now** (eval-gate labeling, resolution-truth logging — shipped); **build a mechanism** (boundary-box wet-spot placement recal — procedure defined, executes at the rig; lead-anchor tuning — shipped); **pause** (signing the inference knobs; unifying offline/live preprocessing); **hold pending bench nozzle diagnostics** (firmware 0/safe reflash + kill-path validation — code ready, do NOT flash yet); **hold** (slot-12 background-emission check); **clarified, no action** (per-class thresholds — the web app UI already exists, only calibrated values are missing, gated on the eval set; domain fine-tune — keep harvesting scenes, no training yet); **do NOT remove** (the plaintext MN checkpoint on paige — deliberate keyserver-outage fallback, operator overrode the assessment doc's cleanup suggestion). All dispositions + the new resolved-question evidence are folded into `ai-precise/docs/MODEL_COST_ASSESSMENT_2026-07-06.md` (new Addendum + "Punch-list dispositions" sections) and `weed-active-learning/docs/AI_MODEL_PLAN.md` (open question marked resolved).
**Status:** Active — **committed + pushed**: `ai-precise` `feat/zed-gmsl-camera` `4e05381`→`42b293e` (7 files: the two code changes + assessment doc + lessons + HANDOFF). Scope was deliberately narrow — pre-existing untracked clutter in the ai-precise tree (`.claude/` agent defs, the garbled `nozzles<0xEF80A8>/` dir + stray screenshot, `tests/tracking_runs.jsonl` field telemetry, `docs/learnings/2026-06-09.md`, macOS `._*` files) was left alone, not swept into this commit. **Next:** eval-set labeling can start NOW — 267 frames of the 6-14 scene are already local at `~/paige_captures/2026-06-13/`, no paige power-up needed. On next power-up: verify in-container rfdetr version, reconcile live config into git, then bench the 768 restore against the (by-then-existing) eval gate and immediately re-run `aip-nozzle lead-anchor --from-live` (latency shifts with resolution). Firmware reflash stays held until bench nozzle diagnostics run.

### 2026-07-06 - RF-DETR readiness audit: Medium runs at 576 (not 768); geometry "98%" reframed as counter artifact; P0 re-affirmed unchanged
**Decision:** Accept the 3-agent repo audit's verdict as the working assessment: **RF-DETR is not the risk — the system isn't measurable yet** — and adopt its punch list (P0: log effective `model.resolution` + read v4a's trained res from checkpoint args; label the eval set; reconcile repo↔paige; reflash 0/safe firmware. P1: geometry recal @1200 + lead anchor/wire + honest dashboard. P2: model levers gated on the eval set — incl. an offline 576-vs-768 A/B. P3: fill the all-zeros `costs:` block). Two open questions RESOLVED: (1) **RFDETRMedium infers at 576** — nothing passes `resolution=` unless the quarantined knob is set (`rfdetr_detector.py:210-216`); the 768 constant is cosmetic (ModelInfo/warmup); rfdetr 1.6.5 Medium default = 576 → all "73ms @ 768" perf math was wrong-baselined. (2) **The geometry-gate "~98% die" figure is an event-counting artifact** — gate confirmed working-as-designed (spatial check per track×nozzle×frame; 7-of-8 fails structural; passes counted ~once because tier-1 dedup short-circuits before geometry post-fire); honest per-weed funnel ≈ **15% of tracked weeds ever entered any nozzle zone**; the real defect is DJI-1080 boxes on the ZED-1200 feed (bottom ~148px covered by NO box; 15/16 nozzles `calibration_*: null`).
**Context:** Operator asked for an honest "is the model and the cost set up for success" review, scoped to the ai-precise repo only (paige live state NOT inspected — ⚠ the 6-15 mock-mode `systemctl start` was never confirmed stopped). Method: 3 parallel read-only audits (detection stack / firing coupling / deploy+economics) @ `4e05381`, plus a direct code read to confirm the operator's geometry-gate interpretation (correct: weeds outside/under a nozzle's boundary box fail that nozzle). Other findings: `compute_lead_box` never wired into the engine (debug route only — firing uses tracker pixel-velocity + stale 120ms anchor → 16–53ms effective lead vs ~215ms real path, ~27mm under-lead; 1-frame tracks fire with ZERO lead at min_track_age=1); Gate-0 staleness inert (trips >384ms vs ~215ms real); dashboards can lie (mock controller default, `/state` hardcodes mode/estop, `on_fire` synthesizes ACK-success, `fires_failed_total` never surfaced); no ground truth anywhere (`validation_set_size: 0`, identity T=1.0, 0/656 tests measure quality); repo↔box drift (v4a only on paige's non-git tree; committed config would zero-gate v4a); `rfdetr>=1.0` unpinned; live `warmup_passes=0`; batch-2 cross-camera forward untried.
**Status:** Active — assessment is the doc of record at `ai-precise/docs/MODEL_COST_ASSESSMENT_2026-07-06.md` (+ `docs/learnings/2026-07-06.md` + HANDOFF 2026-07-06 section), all **uncommitted** on `feat/zed-gmsl-camera`. P0 in substance unchanged since 6-15 — the review's own lesson #4: a review that doesn't become scheduled work is a diary entry.

### 2026-06-15 - Model-rate review INVERTED the premise: detection-rate is downstream of firing-restore + a labeled eval set; resolution-override knob built then QUARANTINED
**Decision:** A 6-lens `marks_team_of_experts` review (Perception, Mechatronics, Software-Architecture, NVIDIA-Edge, SW-Eng-Process + premise red-team, all grounded in the live detector code + regression history) on "increase the live weed-detection rate" returned a **premise inversion**: the goal is downstream of two foundations that don't exist. (1) **No safe/calibrated firing output** — ai-precise is stopped+disabled *because it false-fired on OOD detections*; geometry isn't recal'd at 1920×1200 (`fires_by_nozzle` is a per-lane FP meter); the lead formula **under-leads ~27 mm** (`inference_p50_ms_at_calibration: 120` vs live inference 73–110 ms → it subtracts lead); bench-bypass E-stop build; DO6 dead. (2) **No labeled eval set** — last night's "141 > 78 detections" is a **box-count with no precision measured**, untrusted species, ONE OOD scene (~10 % of frames have green). **"More detections" is the wrong objective for a steam firer** (a detection → a fire → wasted steam + crop damage); the right metric is recall @ a **hard precision floor** (Wilson lower bound). Plan written to `weed-active-learning/docs/AI_MODEL_PLAN.md` (Track A label eval set ∥ Track B firing restore → Track C detection levers with measured numbers → Track D domain fine-tune gated on multi-scene labels).
**Context:** Today I read the live detector and built a manifest-driven **resolution-override knob** (`RFDETRDetector` + `factory.py` `_parse_resolution`; runs RFDETRMedium at e.g. 1024 via PE interpolation; default `None` = no change; 66 tests pass; uncommitted on `feat/zed-gmsl-camera`). The panel ranks it the **weakest lever**: recovers only ~⅓ of what tiling won, leans on PE interp (cf. rye progressive-res *hurt* mAP 42.3→37.4), ~2–3× latency regression that trips the firing Gate-0 staleness suppressor (non-starter for live firing), and it's **firing-relevant but UNSIGNED** (read by a positivity-only parser that silently reverts a bad value to native — the fp16-disaster vector; the router's signed schema covers `optimize_dtype` but not `resolution`). Also surfaced: last night's "resolution is the lever / watermark exonerated" A/B is **confounded** — the offline `score_frames.py` preprocesses differently from live (raw crops, no letterbox/calibration, threshold 0.05 vs 0.001); `bundle.py` fixed checkpoint identity, NOT preprocessing identity (a bigger confound than the watermark). Tiling stays OFFLINE (6× compute); fp32-compile is the safe latency lever (~10–20 %, NOT the 39 % — that's fp16); domain fine-tune is the durable lever but must NOT be trained off one scene (rye closed-loop collapse 55.5→44.2). Open Q: v4a's true forward resolution **576 vs 768** (installed rfdetr Medium = 576; our 768 constant only drives warmup/ModelInfo).
**Status:** Active — plan delivered; resolution knob QUARANTINED (keep code, default-off, do NOT ship to firing). **ONE thing this week:** label the acceptance-gate eval set (≥2 scenes, precision floor + Wilson CIs) ∥ restore the firing path (geometry recal + lead anchor). Lessons: `ai-precise/docs/learnings/2026-06-15.md`.

### 2026-06-15 - ai-precise brought up in MOCK camera-test mode (Controllino unplugged) — transient `start`, left DISABLED
**Decision:** To view the live ZED feeds with the Controllino physically disconnected, started ai-precise via the existing `30-mock-controller.conf` drop-in (`AIP_CONTROLLER=mock`, which overrides ExecStart) — `systemctl start` only (NOT `enable`; stays disabled at boot). Mock controller + unplugged Controllino = two independent safeties → zero firing path (the `FIRE_EMITTED` log lines are simulated, harmless). Verified the EFFECTIVE controller is mock via `systemctl cat` before starting (gotcha: a docker-run app can't take the controller from a systemd `Environment=` — must be a `-e` flag, so the drop-in redefines ExecStart).
**Context:** ai-precise was stopped+disabled (6-13, false-firing on OOD). Started clean (active, NRestarts 0); both cameras serve ~520 KB JPEGs at `/api/v1/live/frame.jpg?camera=0|1`, inference ~72–80 ms, 52 weeds tracked, mode `ready`. **paige LAN IP shifted .114→`10.99.9.59`** (DHCP/RTC drift); view LAN `http://10.99.9.59:8080` (fast) or Tailscale `http://100.96.197.31:8080` (choppy). Every detection box is v4a (cotton) running OOD — expect false boxes; judge raw image for camera assessment, not the overlay. En route the paige tunnel went disco-OK/data-dead → fixed by restarting `tailscaled` on the **local (sparky) end alone**.
**Status:** Active — **TEMPORARY. Stop when done** (`systemctl stop ai-precise`; already disabled so it goes back to off). Do NOT `enable` (the 6-13 disable was deliberate — it false-fires on OOD).

### 2026-06-15 - here.now (agent web hosting) installed + authenticated; first site published password-protected
**Decision:** Installed the `here-now` skill and authenticated the account **mark@naware.io** (one-time email code → API key at `~/.herenow/credentials`, chmod 600). Published `Naware_Competitor_Dashboard.html` as a **permanent, password-protected** site: `https://presto-pocket-hk23.here.now/` (pw `y99oz8RxfVCo`, set via `PATCH /publish/{slug}/metadata`).
**Context:** here.now lets agents publish sites + store private Drive files; authenticated sites are permanent (vs 24 h anonymous). Access modes: anyone_with_link / password / restricted. Staging copy left at `marvin-template/.herenow-publish/`. To publish/update: `~/.claude/skills/here-now/scripts/publish.sh <dir> [--slug <slug>]`.
**Status:** Active — capability available for future "publish this / host this" requests.

### 2026-06-14 - v4a field detection: the lever is RESOLUTION (native-res tiling), NOT the watermark — and v4a DOES see its weeds here
**Decision:** Treat v4a's apparent "blindness" on the outdoor field footage as primarily a **resolution** artifact, not a domain failure, and not the watermark. Evidence (deployed watermarked v4a `f7b1221b`, all 4806 frames on paige): full-frame 78 ≥0.3 / 22 ≥0.5 vs **tiled 141 ≥0.3 / 42 ≥0.5, 106 tiled-only wins**; the high-conf tiled boxes verified by eye to land on **real green plants** (000362 MorningGlory 0.91, 000355 Waterhemp 0.76), NOT hallucinations (contrast MN's trellis-post 0.70). The 1920×1200→768 full-frame downscale shrinks small weeds below detectability; native-res 6-tile inference recovers them. **Watermark EXONERATED** by a clean A/B (pre-wm vs deployed-wm, same frames): max-conf corr 0.976, mean shift −0.0016, ≥0.3 decision flipped on 1/267 frames — negligible (matches the recorded −0.0011 cotton mAP). Despite alpha=20.4% of class_embed RMS, argmax/scores barely move.
**Context:** Operator reframed correctly that a random 1fps sample was a poor judge (the scene is a sparse dry floor — only ~10% of frames have ≥5% green) and that the target = v4a's species (Palmer amaranth/waterhemp etc., which DO grow in CA vineyards). A `marks_team_of_experts` (5 lenses + premise) on "how to mine 4806 frames" surfaced: the **model-identity split-brain** (mining scripts used the PRE-watermark `weights/v4a/checkpoint_best_ema.pth`, not the deployed bytes — didn't change the conclusion but bad provenance), **circularity** (don't mine+eval with the same model; selector ≠ eval-target; species are untrusted — boxes only, per DESIGN_SPEC L1/L3), **score-on-paige** economics (tunnel ~190 kB/s → 5hr pull vs ~7min on-Orin; manifest is KB). Drafted the durable **`weed-active-learning/score/`** stage (bundle.py resolves the DEPLOYED bundle by signed manifest + sha; score_frames.py = on-paige full+tiled → resumable manifest + winners; select_for_pull.py = winners.txt for rsync; reuses field_capture dedup/sharpness; L1/L3/L4/L9/L10-compliant) — fills the split-brain, tunnel, and the **iter-0 ZED measurement** gap (DESIGN_SPEC open contradiction #4).
**Status:** Active — DRAFT for operator review (adds a stage to a converged/locked design; modifies nothing existing). Open: select+pull the 141 tiled winners (deduped) → prelabel_coco (class-agnostic) → Label Studio species labels → finetune. Do NOT fine-tune off this one scene (overfit/forgetting) — re-threshold first; this is iter-0 measurement + harvest, not a training set yet.

### 2026-06-14 - ZED X One capture IS the recommended method; recorded-video "choppy" = the recorder @15fps, not the camera; HD1200@60 is the firing-staleness upgrade
**Decision:** Confirm the ai-precise ZED capture path is Stereolabs-recommended and isolate the choppiness cause. Capture service (`deploy/zed-capture/zed_capture_service.py`) uses **`sl.CameraOne`/`InitParametersOne`** (the correct MONO API), **host-side Argus via ZED Link + zed_x_daemon** (required for GMSL2), **native HD1200 (1920×1200)**, SDK ISP control (bounded AEC/AGC, locked WB/gamma/sharpness), `grab()`→`retrieve_image(VIEW.LEFT)` BGRA→cv2 BGR→shm — all recommended. The recorded mp4s are a **constant 15fps** (ffprobe std 0.0000 — no stutter/drops); they look choppy simply because 15 < the camera's native 30fps. Root cause = the **recorder** writing 2 mp4s + per-frame JPEGs at once (I/O/CPU-bound on the Orin) throttled to 15; the live `zed-capture@*` service itself runs a clean 30fps. **Camera native 30fps; live detection ~6–7fps/cam.**
**Context:** Operator asked (a) would lowering camera resolution to match the model's 768 help — NO, it removes the pixels-on-weed that tiling exploits, making detection worse; the lever is more pixels/weed via tiling (done) or a tighter GSD (closer/narrower lens, within the glued 4mm lens's ~1m sharp limit + a firing-geom recal). (b) Is the capture recommended — yes (above). Vendor spec: **ZED X One GS does 1920×1200 @ 60fps**; we run 30. Detection is unaffected (~7fps bottleneck), but **60fps halves firing staleness 33→16ms (~30→15mm aim error @2mph vs ~12mm margin)** — a real moving-boom win. Deliberate deviations (not errors): CPU retrieve + the host→shm→container split (numpy-2/Argus container workaround, 06-08) costs a little latency; VIEW.LEFT is rectified.
**Status:** Active — capture method validated. Open: (1) fix the recorder to capture mp4 @30fps and extract frames at the desk (not during capture); (2) bench-test HD1200@60 dual-cam for sustained fps/drops + bandwidth on the ZED Link Duo, then recal, before adopting for production firing.

### 2026-06-12 - ai-precise put in UI-only camera-test mode (Controllino pulled) — via `-e` flag, NOT systemd Environment=
**Decision:** Set `AIP_UI_ONLY_ON_PIPELINE_FAILURE=1` so ai-precise runs UI/cameras-only while the Controllino is physically disconnected for a camera test, instead of fail-loud crash-looping (`ConnectionError: controller 192.168.50.2:8888 did not answer STATUS` every ~25s → no UI for Tailscale browser).
**Context:** Mark pulled the Controllino to test cameras only, then moved paige onto battery. **Key gotcha:** a systemd `Environment=` drop-in does NOT work — the app runs inside `docker run` and host env is not forwarded into the container. Must add the var as a `-e AIP_UI_ONLY_ON_PIPELINE_FAILURE=1` flag on the ExecStart `docker run` line (sits beside `-e AIP_CONTROLLER=eth`/`-e AIP_ETH_HOST=...`). Edited `/etc/systemd/system/ai-precise.service` line 66; backup `/etc/systemd/system/ai-precise.service.bak.camtest`. Verified: NRestarts=0, Uvicorn on `0.0.0.0:8080`. paige reachable via Tailscale IP `100.96.197.31` (alias `paige-springs` had a host-key verification failure).
**Status:** Active — **TEMPORARY. ⚠ MUST REVERT when Controllino is reconnected:** `sudo cp /etc/systemd/system/ai-precise.service.bak.camtest /etc/systemd/system/ai-precise.service && sudo systemctl daemon-reload && sudo systemctl restart ai-precise`. Otherwise a real field controller failure silently no-ops (UI healthy, zero firing) instead of failing loud.

### 2026-06-11 - End-to-end dry-fire VALIDATED (detection → relay) + board left on the LOCAL bench-bypass build
**Decision:** Run the full dry-fire chain on the bench and record the result: **ALL 3 TIERS PASSED** — (1) `POST /calibration/test-fire` nozzle 0 → `ok:true, channel_state_post:on` (firmware ACKs only after `digitalWrite HIGH`); (2) `POST /calibration/cold-flush` swept **all 16 relays** `[0..15]` sequentially (200ms pulses) — **relay pin map for new channels 6-15 VERIFIED** (pins 28-37); (3) **detection-driven fires flowing organically**: 37 fires across nozzles {2,3,4} (cam0) + {10,11,15} (cam1), journal showing the gate transition `CONTROLLER_UNHEALTHY_SUPPRESSED` (E-stop era) → `FIRE_EMITTED`, with the per-engine cmd_seq namespacing visible live (cam0: 5-20, cam1: 32787+). En route, found+fixed **two more stub routes** (same disease as /all-off): `/calibration/test-fire` + `/cold-flush` returned synthetic success without touching hardware → wired to the real controller (test-fire deliberately bypasses the engine's detection gates — calibration semantics; firmware safety envelope still applies), honest 503/ok:false contracts, tests updated (94 passing). Also committed: `BENCH_BYPASS_ESTOP` must ALSO report estop clear in STATUS_CTRL (under the same `#if`, inert at committed 0) — otherwise the new Jetson health gate suppresses the very fires the bypass exists to enable.
**Context:** Operator asked for an end-to-end test "from AI detection to firing the Controllino" with the current FPs as the trigger. No E-stop jumper wire on the bench → used the designed-for-this `BENCH_BYPASS_ESTOP=1` path, flipped **LOCALLY on paige's `~/eth_fire_v020` build copy ONLY** (committed repo stays 0/safe), flashed with operator reset taps (first attempt needed a retry; wire-probe `fw=0.2.0 estop=CLEAR bus=ACTIVE chan=16` was the definitive verification). Until the geometry recal, `fires_by_nozzle` on /state is a live per-lane FP meter (every surviving FP = an audible relay click).
**Status:** Active — chain validated. **⚠ OPEN: the board currently runs the bench-bypass build (not the committed firmware). Reflash the committed 0/safe build BEFORE wiring a real E-stop or connecting anything to the relay outputs / any steam work** (revert the sed in `~/eth_fire_v020/eth_fire/config.h` or re-archive from repo, then flash). Next gates before steam: per-camera geometry recal at 1920×1200; safety kill-path validation on an instrumented relay (E-stop cuts a live channel, watchdog auto-off, duration clamp).

### 2026-06-11 - AI Precise hardening sweep: heartbeat + ACK-truth firing path, bridge self-heal, honest health, firmware 0.2.0 (16ch) — implemented, UNCOMMITTED
**Decision:** Implement all four fix batches from the 3-agent deep review (detection path / firing+firmware / ZED capture) on `feat/zed-gmsl-camera` — 27 files +1425/−260 + 5 new (incl. `deploy/jetson-clocks.service`, 4 test files). The load-bearing fixes: (1) **EthernetController heartbeat thread** (500ms + STATUS liveness probe every ~2s) — the firmware watchdog LATCHES after 2.5s of wire silence and the Jetson sent only FIREs, so firing bricked minutes into every real run until power-cycle; (2) **I/O lock** on the shared controller (two engines cross-consumed each other's ACKs → spurious timeouts → duplicate-steam resends); (3) **ACK-truth policy** in the engine: NACK-refused fires are NOT counted/dedup-marked (weed stays killable), timeouts count as ambiguous (anti-double-steam) + `fires_failed_total` telemetry; (4) **is_healthy Gate −1** (dead/E-stopped/tripped controller → `CONTROLLER_UNHEALTHY_SUPPRESSED`, not phantom kills); (5) thermal `_failure_rate` excludes UNKNOWN (real clients return UNKNOWN on every fire → every nozzle self-suppressed after its FIRST fire); (6) all 3 wire bugs fixed (thermal enum off-by-one COLD_STEAM=0→decoded-as-PASS, ChannelState FAULT→"unknown", hardcoded bus_state) + **12 firmware-byte-derived golden vectors** (4 failed against the old code); (7) **bridge self-heal both sides** (service stops unlinking shm on shutdown; reader re-attaches on frozen seq; capture service reopens dead camera / exits for systemd) — kills the "zed-capture restart silently freezes a camera forever" class; (8) **honest /health** (503 + named reasons on un-launched/dead pipelines; per-camera liveness) + main.py exits non-zero on launch failure (was: UI-only with health 200 = robot drives with zero detection looking healthy); (9) real `/api/v1/all-off` (was a stub returning hardcoded 6 — the operator's emergency button did NOTHING); (10) PIPELINE_STAGE firehose env-gated default-OFF (`AIP_LOG_PIPELINE_STAGE=1` for debug) — it had vacuumed the freeze forensics; (11) **firmware eth_fire 0.2.0**: NUM_CHANNELS 16 (pins 22-37; relays 6-15 need bench LED verify — boot-LOW so flashing is safe), watchdog refreshes ONLY on valid frames from the pinned Jetson IP (was: ANY 2-byte datagram from anything), RX drain bounded at 8/tick (flood can't starve valve auto-off/E-stop ticks), cooldown measured from valve CLOSE (was from fire start = effectively zero), 9-byte STATUS_CTRL carries channel count + connect() handshake fails NAMED on mismatch, `THERMISTORS_WIRED=0` knob; (12) deploy: unit template now sets `AIP_CONTROLLER=eth` (a fresh install previously ran the MOCK silently — UI counters moved, zero steam), `StartLimitIntervalSec=0` (offline field unit must retry forever), PATCH /detection rejects unknown class names + warns on shrinking replaces, ISP read-back verification (a silently-skipped exposure bound can't run unnoticed), cross-camera track-id namespacing + bounded dedup sets.
**Context:** Operator asked for a stability/performance/killer-bug review of ai-precise + Controllino and "do all of them"; field reality = unit runs OFFLINE (network only at the shop) so self-heal + honest on-box health are mandatory (no remote rescue). Camera audit verdict: **settings already correct** (bounded AEC 100µs–4ms NOT locked exposure; locked WB 4600K deliberate for the fixed-normalization detector; sharpness 0; keep HD1200@30 — 15fps would add ±15–30mm staleness jitter against a 12mm margin). Perf: applied `jetson_clocks` live on paige (GPU was 99% util pinned at 612/1300MHz — governor revert after the morning power-on) → **inference 110→73ms (~7fps/cam)**; `deploy/jetson-clocks.service` makes it persist. Verified: **594 tests pass** (4 new test files; the 15 test_detection/test_tracking failures PRE-EXIST on clean HEAD — stale 3.12 dev venv, whose editable install is also broken by the /home→/mnt/owc migration: run with `PYTHONPATH=src`); **firmware compiles** on the paige arduino-cli toolchain (14.2KB flash, 10% RAM).
**Status:** **DEPLOYED END-TO-END 2026-06-11** (same day). Committed+pushed (`dfbaeb9`/`e898b1a`/`78c15af` + guard fix); paige tree synced via `git archive|tar` (NOT a git clone!); image rebuilt; jetson-clocks unit + StartLimit drop-in installed; orchestrated restart clean. **First flash exposed a live firmware bug:** the CAN-era `len>8` guard in `can_send()` silently ate the 9-byte STATUS_CTRL → connect crash-loop (fail-loud worked); fixed → guard = txbuf budget, reflashed, wire-verified `fw=0.2.0 chan_count=16`. **App LIVE on the real Controllino** (`AIP_CONTROLLER=eth` injected into installed unit, backup `.bak-pre-eth`): handshake passed, heartbeats verified feeding the watchdog (remaining ≈2.1s of 2.5s). **Self-heal live-tested:** zed-capture@1 restarted under the running app → cam1 rode through (+101 frames/15s, 1 clean re-attach). 6.3fps/cam, inference 72-82ms, ISP read-back live (exposure bound took; gain-range scale reads odd `(1000,16000)` — bench check). Fires correctly double-blocked: E-stop ASSERTED (unwired) + watchdog latch from the pre-connect window (reset-tap clears; heartbeats hold it after). **Operator-remaining:** reset tap; dry LED test ch 0-15 (BENCH_BYPASS_ESTOP=1 local reflash or D8→GND) incl. relay-map verify 6-15. Deferred perf A/Bs: fp32 compile, preprocess-at-560, predict_threshold 0.05 — NOT naive fp16/TRT.

### 2026-06-11 - paige freezes root-caused: WiFi-failure-correlated silent SoC hang (RTL8822CE prime suspect), NOT Argus/GPU load
**Decision:** Reframe the paige freeze diagnosis. The week's hangs were TWO distinct failure modes: (1) Mon 6/8 + Tue 6/9-afternoon = the **DCE display wedge** (already root-caused 06-09) — *soft* hangs (wtmp shows clean SSH-driven reboots; box alive, display dead), FIXED by headless, zero recurrence. (2) Tue 6/9 **8:12pm local** = the one TRUE hard freeze (previously logged as "06-10 freeze #2" — UTC date confusion). New evidence from the on-NVMe postmortem archive (`0001_prev-ba6da68`): **WiFi uplink died 4 minutes before death** — `wlP1p1s0: link timed out` on 'AxisAg -5G' at 01:08:23 UTC → failed re-association loop to the final seconds (tailscaled "network is unreachable" at T-2s) — while the pipeline ran healthy to the last 200ms (frame 44638, inference 107ms, zed publishing 30.0fps at T-800ms, RAM 55GB free, ~56°C). Kernel logged NOTHING from 23:41 onward (vendor driver fails silently); pstore EMPTY → silent bus/fabric wedge → PID1 starved → HW watchdog reset (it worked). **Prime suspect: the Realtek RTL8822CE PCIe NIC on the out-of-tree `rtl88x2ce` vendor driver wedging the Tegra fabric during the association storm.** The Argus/GPU-contention theory is DEMOTED: nvargus "Wrong frequency!" noise tracks camera use at the same rate on healthy boots (incl. today), and GR3D 99↔0 "thrash" is the normal inference duty cycle. Wednesday's clean day proves nothing — both triggers absent (zero WiFi drop events AND pipeline never ran; Ethernet bench day, `pipeline_lines=0`).
**Context:** Site WiFi was in flux Tuesday night ('AxisAg -5G' vanished; box now on PSC-Guest, new subnet — paige LAN = `10.99.9.114` on 10.99.0.0/16). The box's ONLY uplink is now this WiFi (eno1 = Controllino link). Mitigations ranked: **(1) USB3-GbE dongle for a wired uplink** (~$15, removes the SPOF + the suspect driver from the critical path); (2) disable WiFi powersave (`nmcli con modify PSC-Guest 802-11-wireless.powersave 2` — also fixes 250ms RTTs; signal itself is strong, -45dBm); (3) if WiFi stays primary, attended test of in-tree `rtw88_8822ce` or `pcie_aspm=performance`; (4) RTC battery STILL dead (mangled cross-boot timestamps during this analysis); (5) gate the per-frame PIPELINE_STAGE firehose — it churned the 1GB journal cap so fast the freeze-boot journals were vacuumed <24h later; ONLY the postmortem archiver preserved the evidence (vitals+postmortem earned their keep; tegrastats coverage now exists for any future freeze, which Tuesday's lacked). Single-vs-dual camera isolation test now SECONDARY. Also corrected stale state: an **enabled `ai-precise.service` unit exists** (auto-started the dual-cam pipeline 25s into today's 8:47am boot, Restart=on-failure) — "manual `docker run -d` won't survive reboot" is obsolete.
**Status:** Active — diagnosis delivered; **operator call same day: PSC-Guest IS the permanent uplink** (wired-dongle option declined). APPLIED + verified 2026-06-11: powersave OFF live+persisted (tunnel RTT 263→78ms avg, 0% loss, link held), PSC-Guest autoconnect priority 20 + infinite retries, 'AxisAg -5G' + 'prettyfly4wifi-guest' autoconnect disabled. RTC battery = operator will replace himself. Driver/ASPM test stays at-bench-only (WiFi is the only path in — never swap it remotely). Discovered+fixed en route: sparky↔paige Tailscale tunnel dead (disco-OK/data-dead = wedged WireGuard sessions from the network flux) → restarted tailscaled on BOTH ends → direct SSH restored; recovery pattern + elon-m2 ProxyJump backup path saved to MARVIN memory (`paige-unreachable-recovery`).

### 2026-06-10 - AI Precise: Ethernet firing path VALIDATED end-to-end + real firmware built; bench E-stop bypass for dry LED tests
**Decision:** Record that the CAN→Ethernet pivot is **proven live, both ends**, and lock the build pattern. (1) Jetson software (off-bench, 64 tests green + ruff clean): `EthernetController` (`controller/eth_client.py`, drop-in `NozzleController` over UDP, reuses the `can_messages` codec with a 2-byte `<H` msg-id frame), a faithful `UDPControllinoSim` (the firmware's byte-exact behavioural spec), and `build_controller_from_env` (`controller/factory.py`, `AIP_CONTROLLER=mock|eth`, eager `connect()` so an unreachable controller fails at startup) wired into `pipeline.py`. (2) Controllino firmware: `controllino/eth_hello/` (W5100 bring-up probe) + `controllino/eth_fire/` (the **real firing firmware** = the committed CAN firmware with ONLY `init_can`/`handle_can_rx`/`can_send` swapped to W5100/UDP; `on_fire` + channel_timer/estop/watchdog **byte-identical**). Both compile on paige (needs the `CONTROLLINO` Arduino lib). (3) Network: `eno1` → static `192.168.50.1/24` `never-default` (WiFi carries paige's uplink), Controllino `192.168.50.2:8888`. **Live proof: ping 0% loss sub-ms; `FIRE→ACK seq=1 status=OK` over the cable.** (4) **`BENCH_BYPASS_ESTOP`** (eth_fire `config.h`) compiles out the E-stop gate for a DRY no-valve LED test when no E-stop is wired — committed **0/safe**; flip to 1 locally (or jumper D8→GND). Watchdog + duration clamp stay active.
**Context:** Pattern: swap transport, never the safety logic — `on_fire`'s gating and the safety modules are untouched, validating the "transport is separable" claim. The sim doubles as the firmware contract (same role the CAN codec test plays). The W5100 came up first try on the Controllino library's default chip-select (the #1 firmware unknown, resolved over USB-B alone before any cable). Bench is Jetson↔Controllino only — no valves, no steam, no E-stop. `eth_fire` is flashed with E-stop ACTIVE (fires blocked until bypass=1 + reflash or D8→GND).
**Status:** Active — path validated end-to-end. Open: (a) reflash `eth_fire` w/ bypass → FIRE → relay LED (dry); (b) integrate `EthernetController` into the running ai-precise container (bind-mount new code + `AIP_CONTROLLER=eth` + `AIP_ETH_HOST=192.168.50.2` + restart) for detection-driven fires — note geometry ~98% FAIL so a calib test-fire is the reliable demo until recal; (c) per the mechatronics lens, prove the safety KILL paths (E-stop cuts a live channel, watchdog auto-off, duration clamp) on an instrumented relay BEFORE any steam; (d) paige uplink is guest WiFi (PSC-Guest) — fine for bench, harden for field.

### 2026-06-10 - AI Precise: drop CAN, drive the Controllino over its on-board Ethernet (W5100) — Build 1
**Decision:** Pivot the Jetson↔Controllino firing link from CAN to the Controllino MAXI Automation's **on-board Ethernet (Wiznet W5100, RJ45)**, removing the external Seeed CAN-BUS Shield V2.0 + MCP2515 + the USB-CAN dongle (gs_usb) entirely. Motivation: the CAN path has been a multi-session blocker (MCP2515 not answering over SPI on hand-jumpered shield wiring; missing gs_usb driver on the Jetson kernel; non-deterministic interface naming), and Ethernet eliminates ALL of it — plus the camera/kernel risk (no kernel-module surgery). Ethernet is the Controllino's *intended* control transport (it's marketed as an industrial PLC w/ Ethernet; the CAN shield was the bolt-on). Plan: a custom **UDP** server on the Controllino reusing the EXISTING `can_messages.py` byte codec (FIRE/ACK payloads unchanged), so the golden-vector contract test carries over.
**Context:** Validated the application supports a clean transport swap by reading both boundaries. Jetson: the firing engine only touches the transport-agnostic `NozzleController` Protocol (connect/fire/all_off/status/close) — `EthernetController` is a 1-file drop-in behind the existing `AIP_CONTROLLER` factory idea; `MockController` is a complete reference impl. Firmware: the CAN coupling is just 3 primitives (`init_can`/`handle_can_rx`/`can_send`) — the command dispatch + the entire safety envelope (E-stop, watchdog, duration clamp, cooldown) + actuation are transport-agnostic and DON'T MOVE. Honest caveats: W5100 is still SPI-driven but on-board (clean PCB trace + vendor lib, not flaky jumpers) — UNPROVEN on this board, so validate with an "Ethernet hello" sketch first; all the mechatronics safety findings still apply; Build-2 multi-controller (128 nozzles) favored CAN's multi-drop (deferrable; Ethernet needs a switch). This REVERSES the 2026-05-14 locked "CAN 2.0B @1Mbps" decision — deliberate, for Build 1.
**Status:** Active — pivot adopted, app validated as swap-ready. Open: build `EthernetController` + localhost-echo (off-bench), the Controllino UDP-server firmware + "Ethernet hello" validation, then cut over. Network: free paige's wired `eno1` for a direct Controllino link (static `eno1 192.168.50.1/24`, Controllino `192.168.50.2`), paige's uplink moves to WiFi (verified: WiFi reaches internet + Tailscale independently). NOT yet cut over (no Ethernet firmware yet).

### 2026-06-10 - AI Precise: team-of-experts premise flip — stop hardening the CAN bus; build the firing path off-bench + 2 live wire bugs
**Decision:** A 6-agent `marks_team_of_experts` review (5 domain lenses + premise red-team) on "how to fix the CAN control path" returned a premise FLIP: hardening the bus is the wrong focus — it's sandwiched between a `NotImplementedError` app stub (`controller/can_client.py` CANController, no factory switch, zero tests) and an un-initializing MCP2515, on a box that freezes, with no loopback harness and no controller-liveness firing gate. The synthesized plan: (1) pin the CAN interface by udev + a driver-aware TX-ACK preflight (kills the wrong-device class that burned the session); (2) implement the controller off-bench against a loopback (vcan or now UDP-localhost) + factory switch + CI test; (3) a cross-language golden-vector contract test; (4) make the firmware speak its CAN-init result (it ignores `init_can()`'s return = serial-silent); (5) prove the safety KILL paths before any steam. The Ethernet pivot (above) supersedes the CAN-specific items but the off-bench-software-first + contract-test + safety-first sequencing stands.
**Context:** Review also surfaced **2 LIVE wire bugs** (independent of transport): the thermal `FailureType` enum is off-by-one (firmware `COLD_STEAM=0`, Python decodes `0→PASS`) → **a weed-kill that FAILED thermal verification currently reports as a SUCCESSFUL kill**; and `ChannelState.FAULT` decodes as "unknown". The current `test_can_encoding.py` only tests Python-vs-itself, so it's blind to both. Safety lens: "watch the relay LED" is NOT a valid steam-firing milestone (proves you can turn steam ON, not OFF); the "3 independent safety layers" aren't independent (the upstream mains contactor is only a code comment; no hardware AVR watchdog; watchdog refreshes on ANY frame); E-stop bench trap (unwired D8 boots latched → all fires NACK → debug a phantom). Full synthesis in session chat.
**Status:** Active — pivoting per the synthesis. Open (carry into Ethernet build): fix the FailureType off-by-one + ChannelState bugs, add the golden-vector contract test, firmware observability, the safety kill-path validation before steam, and the controller-liveness firing interlock.

### 2026-06-10 - paige freeze #2 = full-system hang caught by the HW watchdog (NOT the DCE wedge) + on-NVMe forensic capture
**Decision:** Treat the 2026-06-10 paige hang as a **distinct failure from the 06-09 DCE display wedge** and instrument for the next occurrence with **local on-NVMe forensic logging** (operator pulls manually; no auto-ship). Evidence: prior-boot journal ends abruptly mid-pipeline (frame 44638) with no shutdown sequence, box recovered on its own, and the systemd HW watchdog is armed (`RuntimeWatchdogUSec=2min`, `RebootWatchdogUSec=10min`, `/dev/watchdog` present) → signature of PID1 starved for ≥2min → watchdog SoC reset. Ruled out: DCE wedge (headless held, `get-default=multi-user.target`, DCE-error count this boot = 1), OOM (55 GB free), thermal (~56 °C). Leading trigger (medium confidence): Tegra camera/Argus + GPU under sustained **dual-camera** load (tegrastats GR3D thrashing 99%↔0% at 509 MHz; **41,642** nvargus "Wrong frequency range!" lines in the dead boot). A silent kernel hang can't log its own cause, so we capture the run-up instead.
**Context:** Built three pieces on paige, all writing to `/mnt/nvme/forensics/` (local NVMe survives the watchdog reboot): `paige-vitals.service` (tegrastats @2s + loadavg/top-procs @5s, keyed by boot_id since the **dead RTC battery** makes wall-clock timestamps unusable), `paige-postmortem.service` (oneshot per boot — dumps prior boot's journal/kmsg/warnings/tail + `/sys/fs/pstore/*` to `postmortem/NN/`), and a staged `nvargus-daemon` journald rate-limit drop-in (`LogRateLimitBurst=100/30s`, applies next reboot — not restarted live to avoid breaking the camera feed). The 06-09 firing-safety/dual-cam work was all committed; nvargus/`sl.Camera` paths untouched. pstore is mounted but may stay empty on a *silent* hang (it captures panics/oopses, not pure wedges). netconsole→sparky is infeasible (Tailscale L3, no L2/MAC). **Pull after next freeze:** `rsync -av mark@100.96.197.31:/mnt/nvme/forensics/ ~/paige-forensics/`.
**Status:** Active — capture live + verified (freeze boot already preserved at `postmortem/0001_prev-ba6da68...`). Open: replace RTC battery (forensics-blocking, flagged 06-09); single-vs-dual camera load isolation test to confirm the Argus/GPU-contention trigger.

### 2026-06-10 - Controllino MAXI Automation CAN firmware committed (5108f9b) — external Seeed shield, hw-verified
**Decision:** Commit the Controllino CAN bring-up fixes that were left uncommitted when the 06-09 session froze. The MAXI Automation drives an **external Seeed CAN-BUS Shield V2.0 (MCP2515, 16 MHz)** jumpered to X1 — NOT the on-board CAN. Fixes vs the 2026-05-14 scaffold: `MCP_8MHZ`→`MCP_16MHZ` (shield crystal); the load-bearing **SPI bus co-existence** fix — park on-board W5100 Ethernet (CS=D70 HIGH) + RV-2123 RTC (CS=D72 LOW) inactive so neither corrupts MISO on the shared bus (wrong pins PJ2/PJ3 → "Entering Configuration Mode Failure"); `THERMISTOR_PINS {A0..A5}`→{54..59} (MAXI Automation core variant lacks the A-macros). CS=53, 1 Mbps to match `can_messages.py`. On `feat/zed-gmsl-camera`.
**Context:** Replaces ai-precise's MockController — needed for ANY hardware firing, and the gap flagged in the dual-camera decision (firmware must drive ≥16 outputs). Flashed from the paige Jetson per the `controllino-flash-setup` memory (toolchain on paige, manual-reset gotcha, ModemManager masked). `CAN.begin` was made to pass on hardware.
**Status:** Active — committed + verified-by-comments. Open: live end-to-end fire test (Jetson FIRE frame → relay → confirm on innomaker `can1`@1Mbps) needs operator at the bench (board not enumerated now, no auto-reset); unmask ModemManager when flashing is done.

### 2026-06-09 - paige hangs = headless-display DCE wedge (go headless); the watchdog can't catch it
**Decision:** Run paige headless (`systemctl set-default multi-user.target`) to kill the root cause of the box hanging. The hang is a **Tegra DCE (display/camera/GPU control engine) wedge**, not OOM/thermal/compute-load: the kernel floods with `NVRM rpcRmApiControl_dce: NVRM_RPC_DCE: Failed RM ctrl call cmd:0x731341 result 0xffff` starting ~5s into boot, right after `[nvidia-drm] … Unable to read EDID for DP-0` — the display engine churning the DCE on a **monitor-less** box (157×/session). Going headless dropped it to **1**. Also enabled persistent journald (eMMC, 1G cap) so the next wedge is diagnosable, and disabled the stale DJI `ai-precise.service` (crash-looped on the absent DJI cam) + the broken `ai-precise-wd` kicker.
**Context:** The box hung today; operator manual-rebooted. KEY correction to the first read: the HW watchdog IS armed (systemd PID1 holds `/dev/watchdog`, `RuntimeWatchdogUSec=2min`) — but a DCE wedge does NOT freeze PID1, so systemd keeps kicking *through* the wedge → no auto-reboot. The `ai-precise-wd` kicker fails `printf >&3: Bad file descriptor` because the Tegra WDT is single-open and systemd already owns it (EBUSY) — a redundant broken feeder, not the cause. MAXN is already on + sticky (GPU 1300MHz, not 50W). The real auto-recovery net for a DCE wedge is a **liveness-gated watchdog** (kicker rewritten to reboot when an app/GPU health probe goes stale) — **DEFERRED** (lower urgency now headless removed the trigger; the box is attended for now). Dual-camera (2× inference/Argus) raises wedge odds, so headless + the future liveness net matter for unattended field use.
**Status:** Active — headless + journal + disabled-units applied & verified (DCE 157→1, tj 54°C, box stable). Open: liveness-gated watchdog; nvargus `Wrong frequency range!` spam; RTC battery (clock jumps mid-boot break log timestamps).

### 2026-06-09 - AI Precise: dual-camera DEPLOYED + verified live on paige
**Decision:** Record that the dual ZED X One build is fully deployed and running on paige (not just code-complete). `zed-capture@{0,1}` publish to two shm segments (`ai_precise_zed_frame` + `…_1`; slot-1 first light, SN 309009949); the rebuilt `ai-precise:latest` image (dual-pipeline + side-by-side UI baked in — bind-mount retired) builds **two pipelines**; `/api/v1/state` shows 16 nozzles grouped `camera_id {0:8, 1:8}`; both `frame.jpg?camera=0|1` serve. Config `configs/paige_springs_dual.yaml` (cam1 nozzles 8–15 reuse cam0's boundary-box template) committed `7aba704` + staged on paige.
**Context:** Deploy sequence was stabilize-first (headless/journal/units) → reboot → `ZED_SLOTS="0 1"` install (note: `sudo` strips env, so slot 1 needed `systemctl enable --now zed-capture@1` directly) → stage config → model-unlock (live keyserver decrypt) → run container. Verified: inference 70ms (MAXN), frame_age 326ms (two pipelines serialize on one GPU — the staleness gate is armed + reading it). 12 commits this session `772a791…7aba704`. **Open (operator-side):** per-camera geometry recal at 1920×1200 (cam1 boxes are cam0's template → GEOMETRY_FAIL on cam1 until redrawn); a live weed-in-cam1 firing test (engine 1 structurally can only fire 8–15); a proper systemd unit for the container (manual `docker run -d` won't survive reboot); TRT/optimize to recover per-camera fps lost to GPU sharing on a moving boom.
**Status:** Active — deployed + verified; operator-side recal + the systemd-unit/liveness-watchdog hardening remain. Plan `~/.claude/plans/purring-mixing-cocoa.md`.

### 2026-06-09 - AI Precise: "choppy/laggy" is the WAN viewing path, NOT the camera (+ it surfaced a firing-safety bug)
**Decision:** Stop attributing the perceived 1–2s lag / choppiness to the ZED or the pipeline. Measured truth (two max-effort expert panels): the camera + host capture + shm bridge deliver a metronome **30.2 fps (sensor-ts 33.3ms ±0.1, ZERO drops)**. BOTH choppy surfaces (the live UI view AND the recorded SMB clips) trace to **one Mac (elon-m2) pulling them over a ~12 Mbps Tailscale tunnel** while it sits on paige's own LAN (10.0.0.x, ~3ms) and ignores it. Free fixes (operator-confirmed): **copy clips to local disk before playing** ("fantastic"); **open the live view at `http://10.0.0.11:8080`** (LAN), not the Tailscale IP. The clips were also tunnel-hostile (the 10s was 39 Mbps mp4v, the 20s 11 Mbps h264 no-faststart). Don't tune the camera/pipeline or the SMB server for this — the tunnel is the wall.
**Context:** The investigation's real payoff was a SAFETY bug it surfaced (next entry). The live preview being bound to the 7fps inference tick is the only genuine box-side smoothness limit; fixing it (decouple from inference + downscale-for-transport) is optional polish, not the lag fix. The camera-assessment need is met by full-res stills + one faithful off-box clip — 30fps smoothness isn't required for any tuning decision.
**Status:** Active — root cause confirmed; free workarounds in use; optional UI decouple deferred.

### 2026-06-09 - AI Precise: re-arm the firing staleness gate broken by the ZED clock domain
**Decision:** Fix the latent firing-safety regression the camera swap introduced. The host stamps frames with the ZED **CLOCK_REALTIME** sensor ts, but `pipeline.py:474` subtracts a **monotonic** clock → huge negative → `max(0,…)` → `frame_age_ms ≡ 0` → firing Gate 0 (`firing_engine.py`) can never trip — the staleness gate was DEAD. The old DJI/UVC path stamped monotonic, so the gate worked; the ZED switch broke it, and `/state` lied (GMSLBackend computes age in the right domain on a different path). Fix: convert the ZED ts into the monotonic domain inside `GMSLBackend.read()` (`max(0,…)` clamp so a bad SDK ts fail-safes, not crashes) + a loud rate-limited warn in pipeline on a domain mismatch. **Verified live on paige: `frame_age_ms` 0 → ~189ms (sensor 64ms + inference 127ms).** Committed `c785b5a` + regression test.
**Context:** Latent today (firing geometry doesn't yet read frame_age — anchored on static latency_p50), but **catastrophic if the Phase-2.3 lead cutover wires this 0 into spray aim** (zeros the lead → misses at 2 mph). Also found the box is in nvpmodel 50W (GPU 816MHz, ~60% of MAXN, tj 54°C) — MAXN is a free ~1.6× — and the `glass_to_valve` metric was a meaningless 0.7ms (frame_age=0 + a MockController can_send). Do the clock fix BEFORE any Phase-2.3 lead work.
**Status:** Active — shipped + verified. Open: MAXN-persist; per-camera/honest telemetry decomposition; decouple preview from the 7fps tick.

### 2026-06-09 - AI Precise: dual camera = TWO independent monocular pipelines, one controller (NOT stereo)
**Decision:** The 2nd ZED X One (Capture Card Duo slot 1, nozzles 8–15) runs its OWN detection+firing pipeline alongside slot 0 (nozzles 0–7) — two **independent monocular** cameras, NOT a stereo pair (they're too far apart; no overlap/fusion needed or assumed). Each camera → own GMSL slot + tracker + FrameBuffer + FiringEngine owning ONLY its 8 nozzles (`config.nozzles_for_camera`), so the camera→nozzle-range restriction is **structural** (engine 0 literally can't fire 8–15). All pipelines share ONE detector (`_SharedDetector`: ref-counted load/warmup/close + lock-serialized predict — the GPU runs one inference at a time) and ONE controller (all 16 ids on the single Controllino; the CAN FIRE frame is an 8-bit nozzle_id, not a bitmap — already 16-clean). Schema v4 adds `NozzleConfig.camera_id` + `Config.cameras`; bridge/services parameterized per slot (`bridge_name`, `zed-capture@.service`).
**Context:** 3-agent exploration confirmed the firing/CAN/telemetry stack is already id-keyed/16-nozzle/single-node clean (zero firing-side code for 8–15); the work is capture/pipeline/UI plumbing. Backend functionally complete in 6 commits on `feat/zed-gmsl-camera` (P1 `531c23c` → P4-backend `397f47a`), all tests green. **Decision flagged for production:** per-engine `max_simultaneous_per_zone`/thermal caps mean two engines don't enforce a GLOBAL "max firing at once" across the one steam manifold — decide before a shared-manifold ship (per-zone is fine for mock/test). Real `CANController` is still a stub (needed for ANY hardware firing); Controllino firmware (off-repo) must drive ≥16 outputs.
**Status:** Active — backend done; front-end side-by-side + 16-nozzle config + live deploy remaining (deploy-verified, needs 2 live feeds). Plan `~/.claude/plans/purring-mixing-cocoa.md`.

### 2026-06-09 - AI Precise: ZED X One uses the MONOCULAR `sl.CameraOne` API (06-08 "0 cameras" was misdiagnosed)
**Decision:** The host capture service (`deploy/zed-capture/zed_capture_service.py`)
must use the ZED **One** SDK API — `sl.CameraOne` / `sl.InitParametersOne` — NOT the
stereo `sl.Camera`. The ZED X One GS is a monocular GMSL camera: `sl.Camera.get_device_list()`
returns 0 for it, while `sl.CameraOne.get_device_list()` enumerates both cams
(SN 309878703 / 309009949, model "ZED XOne GS"). `InitParametersOne` has no
`depth_mode`; `CameraOne.grab()` takes no `RuntimeParameters`; `retrieve_image`
defaults to `VIEW.LEFT` (the single sensor).
**Context:** The 2026-06-08 decision blamed "0 cameras" on a daemon-5.x/SDK-5.x/
numpy-2 version chain and built the whole host-side split around it. That premise
was wrong about the *enumeration* failure — driver `stereolabs-zedlink-duo 1.4.2`
is the documented **minimum** for SDK 5.3, a supported combo, never a wall. The
real bug was the stereo-vs-mono API class. (The host-side split still stands for
the *other* real reasons — in-container pyzed needs numpy-2 + the `libnvargus`
`_enc` symbol hack — so the architecture isn't wrong, just its enumeration story.)
Once switched, cameras enumerated, `open()` reached the daemon handshake, and after
a reboot (to clear a wedged GMSL pipeline) the camera published 1920×1200@30fps.
**Status:** Active — first light achieved 2026-06-09; fix uncommitted on
`feat/zed-gmsl-camera`. Also hardened `install-zed-capture.sh` (venv flag, `requests`,
`chmod a+rX /usr/local/zed` + ldconfig, `libturbojpeg` via direct `dpkg -i`).

### 2026-06-09 - sparky home migrated to the OWC RAID (/mnt/owc) — broke hardcoded-path services
**Decision:** Record that sparky's home directory now lives on the **OWC RAID
volume** (`/dev/md127` mounted at `/mnt/owc`, so home = `/mnt/owc/home/mark`),
migrated ~2026-06-04. `/home/mark` is now a stale **root-owned stub**. Any systemd
unit / venv / cron with hardcoded `/home/mark/...` paths broke at the move. Fix
per-service by repointing to `/mnt/owc/home/mark/...`; the systemic option is a
symlink `/home/mark → /mnt/owc/home/mark` (deferred — `/home/mark` has root-owned
content, operator's call).
**Context:** Surfaced via the `ai-precise-keyserver.service` `203/EXEC` crash loop
(down ~5 days; paige ran on the 14-day grace-cached DEK). Fixed with a drop-in
(`10-owc-home.conf`: WorkingDirectory, ExecStart, `AIP_KS_STATE_DIR` → `/mnt/owc`)
+ a hand-rewrite of the venv editable `.pth` (`__editable__.ai_precise_router…pth`,
system pip can't rebuild offline — `setuptools.build_meta` missing). venv
console-script shebangs (`pip`, `uvicorn`) are likewise stale — invoke via
`./.venv/bin/python -m <mod>`. Verified: keyserver `active` on 100.81.162.68:8443,
paige live DEK fetch granted in 33 ms.
**Status:** Active — keyserver fixed + verified. Open: audit other sparky services
(vLLM units, obsidian cron, orchestrator) for the same breakage; keyserver repo
source unit still carries old paths.

### 2026-06-08 - AI Precise: ZED X One capture goes HOST-SIDE (not in-container)
**Decision:** Run the ZED SDK (pyzed) in a HOST process, not in the detector
container. The host capture service (`deploy/zed-capture/`, pyzed 5.x + numpy 2
+ opencv in its own venv, native Argus) debayers/rectifies + applies the locked
ISP profile and publishes the latest BGR frame to a stdlib `/dev/shm` seqlock
bridge (`camera/zed_frame_bridge.py`). The container's `GMSLBackend` is now a
thin bridge READER (no pyzed/cv2/numpy-2); the app runs the STANDARD image +
`--ipc host`. The earlier in-container WITH_ZED Docker approach was reverted.
**Context:** Three independent, verified blockers killed in-container pyzed on
this box: (1) ZED SDK 5.3 pyzed requires numpy≥2, ABI-incompatible with the
JetPack container's numpy-1 torch/cv2/rfdetr (clean numpy 2 breaks cv2 +
`torch.from_numpy`; earlier "cv2 OK" results were false positives from
`--ignore-installed` mixing numpy files); (2) SDK 4.2 (numpy-1) builds but its
older protocol sees 0 cameras against the 1.4.2 (5.x-era) daemon; (3) Argus
in-container needs `LD_PRELOAD=/usr/lib/aarch64-linux-gnu/nvidia/libnvjpeg.so`
for a `_enc` symbol-scope shadow. daemon=5.x ⇒ SDK 5.x ⇒ numpy 2 ⇒ separate
process. Host-native Argus also avoids the LD_PRELOAD hack.
**Status:** Active — code committed (`af11bff` + `e5e0533`, branch
`feat/zed-gmsl-camera`), 192 tests green. Open: native ZED SDK 5.3 host install
(re-run `install-zed-capture.sh`; CUDA-on-PATH fix applied) → first light →
Phase 2 recal at 1920×1200. See `sessions/2026-06-08.md`.

### 2026-06-02 - v4a (watermarked) deployed to paige_springs
**Decision:** Deploy the white-box-watermarked v4a cotton-weed model to paige's NVMe, encrypted + network-bound like MN. Encrypted `outputs/v4a_wm_whitebox/checkpoint_whitebox.pth` (sha `f7b1221b…`, corr 0.20) → `model.enc`; signed manifest with `architecture: RFDETRMedium`; DEK `paige-v4a-cotton-v1` registered; re-enrolled paige for BOTH keys (preserved pubkey + ts_node → MN decrypt untouched); staged sealed bundle to `_base/v4a_cotton`; decrypt-verified end-to-end via the keyserver auth chain. Active model stays MN — v4a is cotton-weed, activate only for cotton fields (`AIP_MODEL_BUNDLE=_base/v4a_cotton` + app model_version + restart).
**Context:** Whole deploy ran on sparky (MARVIN runs ON sparky; `ssh sparky` is loopback — wasted cycles before realizing). Keyserver state: paige `allowed_key_ids=[paige-mn-weed-v1, paige-v4a-cotton-v1]`. Plaintext + DEK shredded after staging. **Gap found:** a plaintext base checkpoint still sits at `~/Documents/ai-precise/models/_base/MN_weedmodel_final/checkpoint.pt` on paige (outside the encryption) — remove to fully seal.
**Status:** Active — v4a staged + verified on paige, not active. Open: remove the stray plaintext MN checkpoint.

### 2026-06-02 - paige eMMC cleanup EXECUTED + NVMe SMB share
**Decision:** Migrated paige off an 83%-full eMMC. containerd image store → NVMe via bind-mount (Docker uses the containerd snapshotter, so images were on eMMC not the data-root); syslog → NVMe; **installed logrotate** (was missing — the real cause of the 6.1 GB syslog) with a 30-day stanza; removed stale image tags; added `RequiresMountsFor=/mnt/nvme` fail-safe to docker+containerd. **eMMC 83% → 39%.** Also stood up an SMB share `paige_captures` (`/mnt/nvme/captures`, same creds as workstation shares) — scoped to captures NOT the full NVMe, to avoid exposing the encrypted models over a writable share.
**Context:** Syslog firehose = foreground `docker run` per-frame `PIPELINE_STAGE` → journald → rsyslog (json-file cap doesn't catch it). Relocation/cap treats the symptom; source fix (gate per-frame logs behind a debug flag) still open.
**Status:** Active — done + verified. Open: source-side log volume; clock-lock persistence (reverted 4–5× today).

### 2026-06-02 - AI Precise: add per-species firing funnel (`fires_by_class` + `in_zone_by_class`) — 1.2.10
**Decision:** Thread `class_name` through the fire path so `/api/v1/state` exposes the full species × {detected, in-zone, fired} matrix, not just nozzle breakdowns. `FireCommand` gains optional `class_name`; `FIRE_LOG.record()` + `WeedsByNozzle.observe()` accept it and add `by_class` props. Unit choice: `fires_by_class` counts fire EVENTS (a weed hit by N nozzles adds N — mirrors `by_nozzle`); `in_zone_by_class` + `weeds_detected_by_class` count DISTINCT weeds (deduped) — so per-species fired can exceed in-zone (legitimate, multi-nozzle hits).
**Context:** Operator asked for detected/in-zone/fired by species. Detected existed (`WeedsSeen.by_class`); in-zone + fired were nozzle-only, and `detections.jsonl` has class but no track_id → no way to backfill. Can't reconstruct a metric you didn't capture. 11 new fire_log tests; firing/ui/telemetry suites green. Shipped `cba3b5e`, rebuilt on paige, verified live. v4 matrix: 350 detected / 163 in-zone / 150 fired across 5 species; **common_purslane 10 in-zone → 0 fired** newly visible.
**Status:** Active — 1.2.10 live on paige; counters reset on restart (snapshot+subtract for windowed numbers). Open: root-cause purslane's 10→0; boundary-box geometry now measurable per-species.

### 2026-06-02 - paige storage: app image is on eMMC (containerd snapshotter), NOT the NVMe
**Decision:** Record that on paige, Docker uses the containerd image store (`Storage Driver: overlayfs` / `io.containerd.snapshotter.v1`), so IMAGE LAYERS live on the eMMC at `/var/lib/containerd` (18 GB) even though `Docker Root Dir` is `/mnt/nvme/docker`. Only the container writable layer + data root (models, runs, `detections.jsonl`) are on the NVMe. Plan an eMMC cleanup (~8 GB: syslog 6.1 GB, capture frames 1.8 GB, stale image tags, apt clean) + cap the docker `--log-opt max-size` to stop syslog re-ballooning. NOT yet executed — confirm before deleting.
**Context:** eMMC (59 GB) is 83% full. `/var/log/syslog` hit 6.1 GB from per-frame `PIPELINE_STAGE` → journald → rsyslog (logrotate not keeping up); `AIP_DEBUG_DETECTIONS=1` also writes a 3.8 GB jsonl (on NVMe). The "runs off NVMe?" answer needs the storage-driver caveat — `overlayfs` (not `overlay2`) is the tell.
**Status:** Active — cleanup proposed + awaiting go; runtime is safe (data on NVMe), but eMMC pressure will eventually bite builds/logs.

### 2026-06-01 - paige_springs: updated to 1.2.9 + router overlay (permissive + plaintext fallback)
**Decision:** Bring paige_springs to parity with mn-demo: deploy ai-precise 1.2.9 (letterbox + coast + overlay toggle) on paige's existing model/config/calibration (image-only), and add the ai-precise-router security overlay. Install the router in PERMISSIVE and KEEP the plaintext `checkpoint.pt` as a fallback so it cannot block tomorrow's DJI field test (keyserver up → decrypts; down → permissive falls back to plaintext). Defer enforce + plaintext removal until after the DJI test validates 1.2.9 on a clean run.
**Context:** paige was on 1.2.5, standard plaintext deploy, not on the tailnet. Enabled passwordless sudo; joined tailnet (100.96.197.31); provisioned a TPM device identity (key bf6fbcdec142291d); enrolled keyserver unit `paige` (key `paige-mn-weed-v1`); encrypted the MN model; verified decrypt end-to-end (TPM-signed fetch → DEK → decrypt, ckpt_sha 2d20a0ca). Built 1.2.9 on the Orin (igpu base cached). mn-demo dropped off the tailnet mid-process (spotty wifi) → MN manifest pulled from paige's own data dir instead. ZED variant deferred (`Dockerfile.zed` pins an L4T-R35 ZED SDK; paige is R36 + no GMSL camera).
**Status:** Active — paige router permissive + verified; 1.2.9 app comes up when the DJI is plugged in. Open: flip paige to enforce + remove plaintext after the DJI test; build ZED when R36 SDK + hardware confirmed; harden `jetson_clocks` lock (reverts on reboot, both boxes).

### 2026-06-01 - AI Precise: Letterbox the detector input (camera-agnostic), not square-squash
**Decision:** Pad each frame to a centered square BEFORE rfdetr inference (un-pad detections back to native pixel space), instead of letting rfdetr's anisotropic `F.resize(...,[560,560])` squash it. Manifest `letterbox` knob, default ON, toggleable for A/B. Keys off runtime frame shape → camera-agnostic (DJI 16:9, ZED X One 16:10).
**Context:** MN model = 93.7% mAP on its ~square static val set but real-weed confidence collapsed to 0.15–0.40 live on the 16:9 DJI feed. Root cause (5-PhD review): the 16:9→square squash is a ~1.78× vertical distortion the model never trained on — the static/live gap. NOT calibration (temperature is monotonic, can't separate overlapping scores). Shipped in 1.2.9.
**Status:** Active — deployed to mn-demo 1.2.9; validated (365 weeds/6 species on a recorded run). Firing geometry unchanged (un-pad to native px).

### 2026-06-01 - AI Precise: Tracker coast buffer + low-gate persistence, NOT high threshold or naive age-bump
**Decision:** The fix for FP-vs-real-weed separation is TEMPORAL (persistence), not a confidence threshold. (a) Give the permissive `_DegenerateIoUTracker` a lost-track COAST buffer (coast a dropped track at its last bbox up to `max_track_gap_frames`, re-bind the same id) so `track_age` accumulates across low-confidence drops. (b) Keep a LOW gate (~0.15, per-class cleared). Do NOT raise `min_track_age` on the bare memoryless tracker (it makes misses worse), and do NOT raise the confidence gate (0.40 killed ALL detections — real weeds and noise overlap in 0.15–0.40).
**Context:** The production tracker is memoryless (greedy IoU vs immediate-prev only; `use_permissive_tracker` defaults True). A 1-frame drop reset track_age → defeated the firing min_track_age gate → misses. `min_track_age_frames` default is 3, not 1. Validated: coast → **0 `TRACK_TOO_YOUNG`** across 2.2 min. Don't switch to supervision.ByteTrack (its ~0.2 score floor kills low-conf real weeds). Shipped in 1.2.9.
**Status:** Active — 1.2.9 on mn-demo, gate reverted 0.40→0.15.

### 2026-06-01 - AI Precise: Live overlay defaults to decisions-only; raw detector layer is opt-in
**Decision:** The live `frame.jpg` overlay is now layer-selectable (`raw/tracked/fired/boundary/label` query params); the operator default shows DECISIONS ONLY (tracked=green + fired=red), with the raw per-frame detector layer (yellow) behind a "Show raw detections (debug)" UI toggle (`?raw=1`).
**Context:** The operator's "lots of false positives" was largely the RAW yellow layer — per-frame detector output (flickers + COCO-91 out-of-taxonomy boxes) that is NOT what fires. Grading the system on the layer it's designed to discard drove a wrong high-threshold reflex. Shipped in 1.2.9.
**Status:** Active — verified (raw=1 adds ~20 KB/frame vs decisions-only default).

### 2026-06-01 - mn-demo: secure bring-up (#16) complete; build on the Orin, code-of-record on sparky
**Decision:** mn-demo runs the full secure stack: app + router overlay (host-side `ai-precise-model-unlock` ExecStartPre decrypt, D1 firewall) + enforce mode + boot-enabled, encrypted MN model (network-bound DEK from sparky keyserver, unit `mndemo` enrolled with its **TPM** pubkey). v4a encrypted (`mndemo-v4a-cotton-v1`) + deployed as `_base/page_springs`. RFDETRMedium support added (manifest `architecture`). Image builds happen on the Orin (igpu base cached there); the canonical codebase lives on sparky `main` (commit, then deploy).
**Context:** Bug found+fixed in `ai-precise-router/install-router.sh` (SIGPIPE under pipefail). GPU `jetson_clocks` does NOT persist (reverts on reboot/over hours → choppy live stream; re-lock manually — harden the perf unit). App unit won't start without `/dev/ai-precise-cam`. Live-stream "jumpiness" was the 2.4 GHz guest wifi, not the box — record on the box from loopback to review.
**Status:** Active — 1.2.9 live on mn-demo. See sessions/2026-06-01.md.

### 2026-05-28 - AI Precise: Two Metrics to Watch Before Tuning Per-Nozzle Cooldown
**Decision:** Before lowering `firing.cooldown_ms` (currently 200 ms — the per-nozzle debounce that fires DROP_COOLDOWN_DEBOUNCE), instrument and measure these TWO things first. Do NOT tune blind. Geometry coverage (Pareto pick #1 from 2026-05-27) is still the bigger lever; cooldown tuning is the candidate #2 lever AFTER geometry is fixed.

  1. **COOLDOWN_DEBOUNCE category breakdown.** What fraction of DROP_COOLDOWN_DEBOUNCE suppressions are (a) genuinely DIFFERENT tracks under the same nozzle in <200 ms — real lost fires — vs (b) the SAME track that lost its track_id and was re-acquired with a new id — tier-1 dedup would have caught these anyway, so removing cooldown changes nothing. Today the suppressions are logged as a single bucket; need a side-channel that records the time-since-last-fire and the prior track_id for every cooldown hit. Until we know this distribution, lowering cooldown is gambling.

  2. **Empirical minimum nozzle recovery time (bench test).** The 200 ms cooldown represents valve close + steam-chamber thermal/pressure recovery between pulses. Whether the Spartan 250°F / 60–100 psi system + Controllino valve can cleanly do back-to-back pulses at 100 ms (or 50 ms) is a hardware question, not a code question. A bench test: fire one nozzle twice at varying intervals (50 / 100 / 150 / 200 / 300 ms), record steam-pulse strength (visual + thermal probe if available). If the hardware delivers a clean pulse at 100 ms, the cooldown ceiling drops in half — meaningful uplift on dense weed patches. If pulse quality degrades, the floor is real and tuning down causes a WORSE failure mode (firing weak pulses that "succeeded" per telemetry but didn't deliver steam).
**Context:** Operator pushed back on the 60–82 % suppression rate finding from yard tests: "if we're missing weeds because of cooldown, isn't that wrong?" The honest answer is partially. Tier-1 dedup (same track + same nozzle = fire once) is correctly conservative — a weed visible for 2–3 frames at 5 fps should fire once. But COOLDOWN_DEBOUNCE (200 ms after fire, same nozzle) genuinely loses real weeds when two distinct plants pass through the same nozzle lane within 200 ms (sparse turf = 0 % impact; dense thistle = significant). At boom 0.894 m/s, 200 ms of cooldown + 800 ms on_duration = ~1 sec/fire = ~1 m of travel between fires per nozzle. That ceiling is also why nozzle 1 "over-firing" was misattributed in the 2026-05-26 review (right normalization is fires-per-track, not fires-per-detection).
**Status:** Open — instrumentation queued as a small pipeline change (extend FIRE_LOG to record `prior_fire_track_id` and `dt_since_prior_fire_ms` on every DROP_COOLDOWN_DEBOUNCE). Bench test queued for the next field session that has the Controllino + Spartan rig powered. Both feed the "should we lower cooldown_ms, and to what" decision. `firing.cooldown_ms` is operator-tunable via `PATCH /api/v1/config` — no code change needed when the answer lands.

### 2026-05-27 - AI Precise: DeepStream Deferred to Phase 2; TensorRT is the Phase 1 Optimization Path
**Decision:** For Phase 1 latency optimization, the path is **TensorRT only** (already quarantined behind `AI_PRECISE_ENABLE_TRT=1` in `factory.py:60-73`). **DeepStream is deferred to Phase 2.** Reasoning: DeepStream and TRT are not alternatives — DeepStream *uses* TRT under the hood — so the real choice is "drop into NVIDIA's GStreamer-based pipeline framework, or keep our Python pipeline and swap detector backends." Our bottleneck is single-stream RF-DETR inference; DeepStream's wins (NVDEC capture, libargus, NvDCF/NvDeepSORT tracker, multi-stream, zero-copy GPU mem) don't apply yet. The cost is high (rewrite most of `pipeline.py`, tracker glue, firing engine glue, telemetry, FastAPI surface) for marginal Phase 1 benefit. TRT alone is a drop-in at the detector boundary; expected ~35-50 ms inference vs current ~100 ms per the 2026-05-27 Jetson review — which unlocks `min_track_age_frames > 1` at boom speed and addresses cycling structurally.
**Context:** Spawned this question during four parallel expert reviews of yesterday's session. Jetson reviewer recommended TRT validation as P-high (1-2 hr: trtexec build + single-frame numerical range-check vs torch reference + 500-frame timed run). Operator confirmed: optimize the current path first, revisit DeepStream when (a) we have multi-camera ZED X One via GMSL, (b) capture/decode becomes a bottleneck, or (c) we need NVIDIA's tracker. All three are Phase 2 (production rig) concerns, not Phase 1 (backyard mechanics validation).
**Status:** Active — TRT validation queued as task #6 alongside `optimize_for_inference(compile=True, dtype=float32)` A/B. DeepStream re-evaluation queued for Phase 2 hardware-change inflection.

### 2026-05-26 - AI Precise: Per-Class Thresholds, Bug Fixes, + Weed Counter
**Decision:** After 4 iterative yard tests today (gate 0.05 → 0.5 → 0.35 sweep), settled on per-class detection thresholds for BACKYARD TEST: `broadleaf_plantain` + `white_clover` at 0.60, `dandelion` + `creeping_charlie` at 0.40, global baseline 0.35. All 6 species enabled (briefly disabled chickweed+purslane mid-session based on a late-tail window showing peaks <0.40; re-enabled later same session after peer reviewer found chickweed had 274 kept + purslane 329 kept in the 20:31-20:50 hot window — the low peaks were narrow-window artifact, not a permanent property). Both chickweed + purslane fall to global 0.35 gate (no per-class override). Persisted via PATCH `/api/v1/detection` and mirrored into `configs/paige_springs.yaml` to prevent stage-config drift. Also fixed the long-standing `track_id: null on fires` bug (`FireCommand` schema in `types.py:266-279` was missing the `track_id` field entirely; added optional field + propagated through `firing_engine.py:371` + `pipeline.py:425`). Added a `WeedsSeen` singleton in `telemetry/fire_log.py` + exposed `weeds_detected_total` and `weeds_detected_by_class` on `/api/v1/state` — operator can now see live distinct-weed counts (per‑track dedup, per-class breakdown). Updated `firing.inference_p50_ms_at_calibration: 110 → 200` to match measured fp32 inference (was a hidden Kalman lead-shift error for production moving-boom use).
**Context:** Operator ran ~3.5 hr Friday yard test (9.37M raw detections) + 4 short tests today. Friday's data showed plantain detected fine (26k kept rows at gate 0.05); today the operator reported "doesn't detect plantain when held over the weeds." Diagnosis chain: (a) `JET_LOG_DETECTIONS=1` was missing from systemd unit so today's test produced no diagnostic data; reinstated. (b) `manifest.json` had been hand-edited Friday 22:12 UTC (end-of-test) to enable `optimize_for_inference: true` + `optimize_dtype: float16`. Container restart loaded the new manifest; fp16 collapsed RF-DETR's score dynamic range, real-world plants slid into noise floor. Reverted to minimal manifest (fp32, no optimize). (c) Confirmed model emits all targets confidently in fp32: plantain peak 0.943, clover 0.982, dandelion 0.621. (d) "Label cycling" — initially diagnosed as 100% plantain↔clover by perception-engineer agent, peer-reviewer found it's actually multi-pair (plantain↔clover 32%, charlie↔clover 23%, plantain↔dandelion 23%, plantain↔charlie 6%, dandelion↔clover 6%, + 4 other pairs). Broader DETR-family ambiguity on similar broadleaf species, not a config bug. (e) "Nozzle 1 over-firing" was misattribution: engineer's "1% fire/det" math is meaningless (peer-reviewer caught: right normalization is fires per distinct track, not per detection — tier-1 dedup is per (track_id, nozzle_id)). 19 fires from ~20 tracks = expected.
**Status:** Active — patches scp'd to box bind-mount `/home/mark/ai-precise-patch/ai_precise/` and verified live. NOT YET committed (5 src files + 1 config file pending commit). Service `active` on paige_springs with new thresholds.

### 2026-05-26 - AI Precise Production Strategy: Low Threshold + Track Persistence, NOT High Threshold
**Decision:** For PRODUCTION (real spray rig, moving boom), threshold strategy is **low confidence gate (~0.10-0.15) + multi-frame track persistence**, NOT high confidence gate. Today's backyard 0.60 per-class thresholds are NOT shippable to production. Operator's reasoning: the spray fluid is steam (Spartan 250°F / 60–100 psi, low cost-per-shot), so a missed weed costs more than a wasted spray on grass. Bias the system toward recall (catch everything) and use OTHER mechanisms to suppress false positives:
1. **Global threshold ~0.10-0.15** (much lower than backyard 0.35)
2. **No per-class overrides** (clear them all — let the calibrator/global gate do the work)
3. **All weed species enabled** (including chickweed, purslane, less-confident species)
4. **`firing.min_track_age_frames`: raise from 1 → ~4** (track must persist ~150 ms at 30 fps before it can fire; noise is by definition transient and won't survive)
5. **Tier-1 dedup** (already on — one fire per (track_id, nozzle_id))
6. **Tier-2 spatial dedup** (already on, 20 cm radius / 30 s window — no double-fire on same spot)
7. **ByteTrack activation back to ~0.05** (matches the loose detection gate)
**Context:** Today's session lowered threshold from 0.5 → 0.35 → considered 0.60 per-class. Peer-reviewer flagged that 0.60 will suppress production detections at 1-2m range (DETR confidence drops to 0.4-0.7 on small bboxes even on correct calls). Operator clarified production goal: low confidence + zero false positives. Architecture supports this via the 4 separate filters above — currently only the threshold filter is being tuned, leaving 75% of the design idle.
**Status:** Pending — applies to production deploy, NOT today's backyard test. Today's 0.60 per-class config stays for backyard mechanics validation. Before any production deploy: configure the lower threshold + raise track_age_frames + verify all 4 filters are active. Validation test plan: 5-min capture at 1-2m target distance with new config; expect kept rate at ~30% of close-range numbers, and zero false fires over 200+ frames of bare grass.

### 2026-05-26 - AI Precise API gotcha: PATCH /api/v1/detection.classes REPLACES, not MERGES
**Decision:** When updating per-class detection settings via `PATCH /api/v1/detection`, the `classes` field in the body REPLACES the entire current classes list — it does NOT deep-merge per-class. Sending `{classes: [{name: chickweed, enabled: true}]}` to flip just one class will WIPE all other class settings (per-class thresholds + enabled flags). Always send the FULL desired class list when patching this endpoint.
**Context:** Tripped on this 2026-05-26 when re-enabling chickweed+purslane. Sent a 2-element list, got back state showing only chickweed+purslane as enabled (other 4 had no per-class override). Recovered by sending full 6-class list. Worth fixing in the endpoint code (do a per-name merge instead of list replace) but the workaround is trivial — just always send the full list.
**Status:** Active gotcha — document in API README; consider per-name merge in routes/detection.py:138-141 for the next code change.

### 2026-05-26 - AI Precise: Hot-Patch via Bind-Mount, No Container Rebuild
**Decision:** Use the existing `-v /home/mark/ai-precise-patch/ai_precise:/app/src/ai_precise:ro` bind-mount in the systemd unit's `docker run` for in-session code patches — scp updated files to the box, then `systemctl restart ai-precise` to re-import. This is the operationally-fast path for surgical fixes during field testing.
**Context:** Today's null-track_id bug + weeds_detected feature both needed code edits, not config changes. Without the bind-mount, the only paths were (a) rebuild the container image (~5+ min, requires registry push or local tag bump) or (b) edit inside the running container (lost on restart). The bind-mount was already designed in for exactly this use case — it preserved today's full debug cycle (find → fix → restart → re-test) inside a single 5-minute window.
**Status:** Active — used successfully today; document in deploy README. Note: bind-mount is `ro` so container can't write back; edits must come from outside.

### 2026-05-18 - AI Precise: Two-Variant Docker (rfdetr default + TRT), One Codebase
**Decision:** Ship two container variants from one `jetson/src` codebase, runtime auto-selecting the detector. `docker/Dockerfile`(.dev) = the **original** rfdetr/uv/Python-3.10/torch path, kept as the **default** — on JetPack 6 / L4T R36 Orins (native py3.10 + CUDA torch) it runs on GPU; this is the path for the two new Orin AGX boxes on the latest JetPack. `docker/Dockerfile.trt`(.dev) = TensorRT-runtime/py3.8/no-torch path, the JetPack-5.1.3 "Jet" (192.168.1.38) workaround. `docker/build.sh --trt` selects the TRT variant (l4t-tensorrt base, `-trt` tag suffix); default builds rfdetr. Both tag `:latest` so the systemd unit + `deploy/load-tarball.sh` are unchanged (a Jet only ever loads one variant's tarball). `factory.create_detector` picks `TRTDetector` when `weights.engine` is present in the bundle, else `RFDETRDetector` (`checkpoint.pt`) — no code fork. The py3.8 backport (`from __future__ import annotations` etc.) is harmless on py3.10, so it doesn't compromise the rfdetr path.
**Context:** The TRT agent (commit `a8112b9`) had overwritten the original rfdetr Dockerfile. Operator flagged that 2 new Orin AGX boxes will run the latest JetPack, where the original rfdetr+CUDA-torch install works natively on GPU — so that path must be preserved, not replaced. The TRT path remains the JP5-specific workaround (no JP5 Python satisfies both rfdetr py≥3.10/torch≥2.2 and Jetson-CUDA-torch py3.8/torch2.0). Restored the original Dockerfiles from history (`a8112b9~1`/`7629044`), renamed the TRT ones to `.trt`, taught `build.sh` both. ONNX inspection also corrected the model contract: input 560×560 (not 640), `labels` are raw logits (no Sigmoid), 91-slot head with only slots 0–29 = MN weed taxonomy — backyard test validates mechanics, not weed accuracy.
**Status:** Active — committed `3221a2f`, pushed. `mboysen/ai-precise` ~19 commits, 338 tests/1 skip, both variants intact. TRT path built but NOT on-device-verified: next cycle is `build.sh --trt` → ship → on-Jet `trtexec` engine build → first-run DEBUG range check (`TRT_DETS_RANGE`/`TRT_LABELS_RANGE`) to confirm box-format/normalization (`# TODO(empirical)` left in `TRTDetector`).

### 2026-05-15 - AI Precise GPU Path: TensorRT-Engine, Not rfdetr-at-Runtime
**Decision:** On JetPack 5.x the Jet GPU inference path is a **TensorRT engine** with NO torch/rfdetr at runtime — not a container-base swap. Flow: export MN checkpoint → ONNX on sparky (DONE, valid 113 MB); build `.engine` on the Jet via `nvcr.io/nvidia/l4t-tensorrt:r8.5.2.2-devel` (`trtexec`, GPU, device-specific); write a `TRTDetector` (TensorRT-Python + pycuda, implements the `Detector` Protocol); slim no-torch container on `l4t-tensorrt:r8.5.2-runtime`. This is exactly what the operator's `architecture/jetson_orin_recommendation.md` prescribed as the production path. Open sub-decision (operator): app code targets Python 3.10 but JetPack-native TRT bindings are Python 3.8 — backport runtime modules to 3.8 (recommended; small surface) vs carry a 3.10 layer.
**Context:** rfdetr 1.6.5 hard-requires Python ≥3.10 + torch ≥2.2.0. The only CUDA-enabled torch for JetPack 5.1.3 (L4T R35, CUDA 11.4) is NVIDIA's `torch 2.0.0+nv` built on Python 3.8 (verified: `dustynv/l4t-pytorch:r35.4.1` = py3.8/torch2.0+cu11.4). No container base can satisfy both simultaneously — it's structurally impossible, not a search problem. The CPU container proved all plumbing (config, camera wiring, pipeline, UI, model-bundle resolution) but its `cu130` torch errors every frame on the CUDA-11.4 driver. ONNX export retired the PyTorch-Lightning checkpoint-format risk that was flagged three times. Jet deployment is rooted at `/home/jetson/paige_springs/data/` (jetson-owned, no sudo for data dirs); `ai-precise.service` carries the backyard profile (pipeline on, DJI `/dev/video0`, no `/dev/can0` — MockController).
**Status:** Active — ONNX export done + preserved (repo + Jet bundles). TRT engine build + `TRTDetector` + slim rebuild is the next focused effort, gated on the py3.8-vs-3.10 operator call. `mboysen/ai-precise` ~17 commits. CPU container on Jet is a throwaway plumbing validation.

### 2026-05-14 - AI Precise Phase 1 Build Architecture Locked
**Decision:** Phase 1 build of the AI Precise weed-detection system uses these locked choices: **CAN 2.0B at 1 Mbps** for Jetson↔Controllino comms (SocketCAN on Jetson side; replaces the synthesis-default Modbus RTU); **N-parametric nozzle count** (Build 1 = 4–6 nozzles on a 34–48" boom; Build 2+ = up to 128 nozzles on a 100-ft boom via multi-controller BROADCAST_FIRE bitmap); **Spartan pressure washer at 250°F / 60–100 psi** replaces v1's Predator+Nobeth+Watlow three-unit stack; **ZED Link Capture Card Duo** mounts directly to Jetson PCIe (supports 2 ZED X One cameras); **multi-camera selectable** in the UI (UVC DJI Osmo + GMSL ZED X One both supported, operator picks at calibration time); **container deployment per `jetson_orin_recommendation.md`** with L4T base `nvcr.io/nvidia/l4t-pytorch:r35.5.0-pth2.X-py3` + Python 3.10 via deadsnakes in container (host JetPack 5.1.3 untouched); **Tailscale primary, NoMachine secondary, SSH last** for remote access; **single Controllino MAXI Automation** for Build 1 (multi-controller deferred to Build 2). Repo: private GitHub at `mboysen/ai-precise`; source of truth on sparky at `/home/mark/Documents/ai-precise/`.
**Context:** Six section drafts + 4 peer reviews + 1 integration agent produced v2.1_architecture.md (~32k words). 4 parallel build agents shipped Day-1 scaffolds: Jetson Python app + Docker + tests (218 tests, 95% cov, ruff/mypy clean), Controllino Arduino firmware (CAN message bytes match Jetson side byte-for-byte), operator UI PWA (4 tabs, two-tap rectangle for boundary boxes), deploy scripts (systemd + watchdog kicker + install.sh + load-tarball.sh with auto-rollback). 3 follow-up agents added: Spartan/CAN architecture doc fixes (~35 sections updated), multi-camera selector + Dockerfile.zed overlay, FastAPI server with full REST + WebSocket contract matching the UI client. Operator chose **MN_weedmodel_final** RF-DETR checkpoint (from `/home/mark/weed_detection/outputs/MN_weedmodel_final/checkpoint_best_ema.pth`, 128 MB, epoch 17, 93.7% mAP per state) as the backyard-test detector. Backyard test plan: DJI Osmo USB → real RF-DETR → ByteTrack → FiringEngine → MockController; predicted track positions crossing operator-drawn boundary boxes trigger mock fires logged to events.jsonl + flashed in UI.
**Status:** Active — Day-1 scaffolds shipped to `mboysen/ai-precise:main` (7 commits). FastAPI server demoable on sparky at http://192.168.1.214:8080 + Tailscale 100.81.162.68:8080. Backyard-test agents (UVC capture + pipeline + container tarball + runbook) errored on API 529 — retry next session.

### 2026-05-01 - vLLM Qwen3.6-27B-FP8 Promoted to systemd
**Decision:** Move sparky's vLLM Qwen3.6 from manual-launch-only to a systemd-managed service so it auto-starts on boot. Mirror the Nemotron pattern: `Type=exec` owning a foreground `docker run --rm`, `Restart=on-failure` with backoff, 45-min cold-start timeout, `WantedBy=multi-user.target`. Tailscale-scoped bind (`100.81.162.68:8001`) preserved — no LAN exposure. Three files added at `sparky_infrastructure/scripts/`: foreground-converted `vllm-qwen36-start.sh`, new `vllm-qwen36.service`, new `vllm-qwen36-install.sh`.
**Context:** Session opened with the container down — there's no `--restart` flag and no systemd unit, so any reboot or stop drops it. State notes from 2026-04-27 explicitly flagged this as "manual relaunch on reboot." Promoting now closes the gap. The Apr-17 decision against `--restart=unless-stopped` (fights systemd) still applies; this design follows the tested Nemotron approach.
**Status:** Active — installed and verified 2026-05-01 (cold load ~12 min via warm `~/.cache/hf-qwen36/`). 7+ days uptime confirmed 2026-05-08.

### 2026-05-01 - vLLM gpu-memory-utilization Lowered to 0.6
**Decision:** Drop `--gpu-memory-utilization` from 0.75 to 0.6 in `vllm-qwen36-start.sh` so the GB10's unified memory has headroom for the desktop session.
**Context:** First systemd cold-load at 0.75 produced NVRM OOM (`kernel_graphics_context.c:1387`) and wedged the Tegra driver — Firefox and Obsidian crashed because new EGL contexts couldn't allocate. vLLM itself was fine because its CUDA contexts were established before the OOM. At 0.6 the model still serves at 65536 max_model_len with 32 GB VRAM, leaving ~50 GB free for desktop + Ollama qwen3:8b coexistence.
**Status:** Active.

### 2026-05-01 - ESA SNAP 13 Install via install4j Bypass on ARM64
**Decision:** Install ESA SNAP 13 at `/home/mark/esa-snap` by bypassing the installer's bundled x86 JRE entirely. The `.sh` wrapper unpacks `jre.tar.gz` and assigns `app_java_home=$(pwd)` *before* checking `INSTALL4J_JAVA_HOME_OVERRIDE`, so the env var override never fires. Workaround: trigger phase-1 extraction (fails at JRE exec — expected), then `cd` into the temp `.dir/`, build the install4j classpath from all `*.jar` in it, and invoke `java install4j.Installer2893700168 -q -dir /home/mark/esa-snap` with system-installed ARM64 OpenJDK 21. Post-install: patch `etc/snap.conf` to `jdkhome="/usr/lib/jvm/java-21-openjdk-arm64"` and `-J-Xmx32G` (default was 85G).
**Context:** SNAP 13 requires JDK 21 (`minJavaVersion="21"` in `i4jparams.conf`). DGX Spark is ARM64 (sm_121). ESA's bundled JRE is x86_64, so the wrapper hits `Exec format error` immediately. ESA also ships **macOS Mach-O dylibs** in their Linux ARM64 NetCDF native bundle (`libzstd.1.dylib`, `libhdf5.310.dylib`, etc.) — packaging bug, the NetCdfActivator throws SEVERE on every startup but the engine continues, Sentinel-2 ops work fine. Installed for Sentinel imagery work tied to rye-detection / Khanal multispectral outreach.
**Status:** Active — `gpt`/`esasnap` aliases in `~/.bashrc`. NetCDF SEVERE is cosmetic and ignored.

### 2026-05-01 - GNOME Software Snap Plugin Removed; api.snapcraft.io Blocked Upstream
**Decision:** Remove `gnome-software-plugin-snap` so the deb-shipped GNOME Software (branded "Ubuntu Software" on this ARM64 install) doesn't hang on the unreachable snap store. Snap-store snap stays uninstalled until the upstream block is lifted.
**Context:** Diagnosed sparky's snap-store failure as a TCP-443 block on `185.125.188.0/22` (Canonical CDN) somewhere upstream — DNS, ICMP, and unrelated HTTPS all work; only api.snapcraft.io is dropped. Likely the home router has Canonical IPs in a security/ad-block ruleset. With the snap plugin removed GNOME Software loads fine for debs; snaps remain off-limits until the router rule is fixed (allowlist `185.125.188.0/22`).
**Status:** Active — workaround in place. Real fix is router-side, not on sparky.

### 2026-04-04 - Synthetic Images Are Supplementary Only
**Decision:** Synthetic image generation (FLUX/SDXL) is useful for bootstrapping zero-data species but won't break the 80% mAP ceiling.
**Context:** Run 8 added 4K synthetic images to 38K real images. Result: 79.4% mAP vs Run 6's 80.2% with zero synthetic. Domain gap between AI-generated and real images limits transfer. Model annotating its own synthetic images is circular.
**Status:** Active

### 2026-04-04 - 152K Old Dataset Paused
**Decision:** Paused species annotation of the 152K old binary weed dataset pending quality assessment.
**Context:** Images are 640x640 heavily compressed phone photos (48-54KB), not robot camera images as previously documented. Dead pixel artifact present. Quality too poor to improve the model — may actually hurt it.
**Status:** Active — needs visual review via `\\192.168.1.214\weed_152k`

### 2026-04-04 - Path to 90%+ Requires Real Robot Camera Data
**Decision:** Focus on collecting high-quality images from the production robot camera instead of more synthetic or low-quality data.
**Context:** The 80% ceiling is a data quality problem, not quantity. Run 6 hit 80.2% with 33K quality images. Adding 4K synthetic or 152K low-quality phone photos won't break through. Active learning from the real robot is the path forward.
**Status:** Active

### 2026-04-04 - Re-annotate with Whole-Plant DINO Prompts (v4a Pipeline)
**Decision:** Re-run the v4a annotation pipeline using whole-plant prompts ("dandelion plant . dandelion rosette") instead of flower-focused prompts ("dandelion weed"). Follow with RF-DETR bootstrap and digital twin integration.
**Context:** Root cause of 80% mAP ceiling identified: all annotations since day one were flower-focused because DINO prompts targeted flowers, not whole plants. v4a hit 94% using whole-plant prompts + bootstrap + digital twin. Initial 5,359-image test with correct prompts looks good. SAM expansion approach was tested and rejected — can't separate overlapping plants in dense scenes.
**Status:** Complete — achieved 90.8% mAP through 4 bootstrap iterations + quality filtering

### 2026-04-07 - Quality Filtering Is the Key to Breaking 90%
**Decision:** Use the trained model as a quality gate — only accept images where the model detects the target species at >50% confidence with reasonable box size. Reject noisy, mislabeled, or ambiguous images.
**Context:** Run 4 with quality-filtered data (5,519 accepted out of 6,432) hit 90.8% mAP vs 87.8% without filtering. Fewer but cleaner images > more noisy images. The 913 rejected images were actively hurting training.
**Status:** Active

### 2026-04-11 - Ollama for Day-to-Day, Claude for Weekly/On-Demand
**Decision:** Split the knowledge pipeline between local Ollama (Qwen 3 8B) and Claude Opus. Ollama handles routine tasks: session summarization, project classification, incremental concept updates. Claude/Opus reserved for full project recompiles and topic consolidations.
**Context:** Running every session summary through Opus was wasteful — simple extraction tasks burn expensive Opus calls for work Qwen handles fine. Split by task complexity: extraction/classification = Ollama (free, fast, ~22s/session), synthesis/cross-referencing = Claude. `naware-sync` uses Ollama by default, `--use-claude` or `--force` for the expensive path.
**Status:** Active

### 2026-04-12 - Morphology Prompts Beat Taxonomy for Fine-Grained Grass Detection
**Decision:** Use morphology-descriptive GroundingDINO prompts ("bushy dark green grass clump", "wide drooping grass leaf clump") instead of taxonomic prompts ("cereal rye plant", "winter wheat plant") for rye/wheat auto-labeling at tillering stage.
**Context:** Taxonomic prompts produced zero discrimination between rye and wheat — GroundingDINO's BERT encoder can't distinguish closely related grass species. Morphology prompts correctly identified most real rye clumps (196 confirmed out of 447 detections) because structural descriptors map to visually grounded features. The 52% false positive rate is manageable via bootstrap + quality filtering.
**Status:** Active — apply morphology-descriptive prompts for any future fine-grained grass/crop discrimination tasks

### 2026-04-14 - Ensemble Pipeline is the Production Architecture for Rye Detection
**Decision:** Use DINO + round-0 detector (merged proposals, NMS de-duplicated) → EfficientNet classifier (97.3%) as the production pipeline. Do not attempt to distill ensemble knowledge into a single detector.
**Context:** The ensemble finds 365 rye detections across 96/96 images (53% more than detector-only at 239). DINO and the detector find different clumps — merging covers blind spots. Retraining a single detector on ensemble labels produced only 39 detections (much worse). The ensemble's complementary coverage can't be replicated by a single model on 96 images.
**Status:** Active — this is the production pipeline until heading-stage images enable a stronger single-stage detector

### 2026-04-13 - Two-Stage Detection Beats Single-Stage for Fine-Grained Discrimination
**Decision:** Use a two-stage pipeline (RF-DETR detector → EfficientNet classifier) instead of a single detector for rye-in-wheat. The detector finds plant clumps at low threshold, the classifier scores each at 256×256 full resolution.
**Context:** RF-DETR at 640×640 sees rye at ~42px — too small for texture discrimination. The patch classifier at 256px achieved 97.3% accuracy on the same data the detector plateaued at 55%. Separating localization (easy) from classification (hard) unlocks the texture signal. Copy-paste augmentation and progressive resolution did NOT help (42.3% and 37.4% vs 55.5% baseline).
**Status:** Active — this is the production architecture for rye detection

### 2026-04-12 - 55% mAP is the Data Ceiling for 96 Tillering Images
**Decision:** Stop iterating training approaches on the current 96-image dataset. Bootstrap (44.2%), COCO pretrained (55.5%), and lawn weed transfer (55.6%) all converge to the same ceiling. New data is needed to improve further.
**Context:** Three independent training runs confirmed the ceiling. Bootstrap on the same closed dataset lost diversity and performed worse. Transfer learning converged faster but to the same point. The bottleneck is dataset size/diversity (96 images, 1 location, 1 growth stage), not model architecture or starting weights.
**Status:** Active — collect heading-stage images (late May–June) and images from additional fields to break through

### 2026-04-12 - Clean Before Tile for Training Data
**Decision:** Always review and correct auto-generated annotations BEFORE tiling into training crops. Never tile unreviewed auto-labels.
**Context:** GroundingDINO B_morphology produced 447 detections; Mark's review confirmed only 196 as real rye (44%). Tiling first would have multiplied 232 false positives 45× alongside real annotations, poisoning the training set. Clean-first reduced the dataset from 447 to 196 boxes but every box is verified.
**Status:** Active — standard practice for all future bootstrap rounds

### 2026-04-30 - Hub Concepts: Manual Append, Not Opus Auto-Compile
**Decision:** Frozen hub concepts (the 19 hand-merged masters) get updated by manually appending one-line entries to their Session Log tables. Do NOT use `compile-projects.py --overwrite` against frozen hubs — even with `CONCEPT_TEMPLATE=hub-concept-template.md`, Opus produces malformed output.
**Context:** Tried option-2 path on lucas: stripped `frozen: true`, ran compile with hub template env var. Opus produced legitimate procurement *content* but ignored the hub structure entirely — no frontmatter, no Artifact Registry, no Insights, just raw `## Update: HH:MM` session-log dumps. Root cause: hub-concept-template.md is human-facing documentation prose with example YAML, not a structured LLM directive. compile-projects.py loads it verbatim as the prompt. Would need a hub-aware system prompt rewrite to make option 2 work — not worth the effort for sporadic hub updates.
**Status:** Active — manual append works, took ~10 min for lucas (3 new entries). Bad-compile output kept at `concepts/lucas.md.bad-compile-20260430-182700` as a reference for any future prompt-engineering attempt.

### 2026-04-30 - Reorganize Defense-in-Depth: Reject Names with 2+ `_raw_` Tokens
**Decision:** `reorganize.py` now also rejects files whose *name* contains `_raw_` 2+ times, regardless of source path. Defense in depth on top of the Apr 29 `SKIP_TOP_DIRS` fix.
**Context:** Apr 29 fix only catches files harvested at `raw/harvested/<machine>/raw/...`. Mac's harvest tree also contained pre-corrupted files at clean paths like `raw/harvested/Elonm2-2/lucas/sessions/<long-bad-name>.md` — those slip past `SKIP_TOP_DIRS` because `parts[0] == "lucas"`. Name-based filter catches them. Mac's 5,595 already-corrupted files cleaned via SSH `find -delete` (xargs hit ARG_MAX from filename length).
**Status:** Active — both guards in place; recurrence loop fully blocked.

### 2026-04-29 - Wiki Reorganize: Skip raw/ Bounce-Back from Remote Harvest
**Decision:** `reorganize.py` now skips any harvested path starting with `raw/` (new `SKIP_TOP_DIRS = {"raw"}` constant + early continue at line ~131). Defense against Obsidian Sync mirroring sparky's reorganized output to Mac, then Mac's harvest crawling it back into `raw/harvested/Elonm2-2/raw/sessions/...`, where the previous code treated `raw` as a project name and produced filenames like `foo_raw_Elonm2-2.md`. Each cron tick added another `_raw_Elonm2-2`, hitting the 255-byte filename limit at 5,596 corrupted files. `OSError 36` aborted reorganize, `set -euo pipefail` killed sync, steps 4–7 hadn't run for ~2 days.
**Cleanup performed:** 8.9 MB backup tarball at `/tmp/wiki-corrupt-backup-20260429-193434.tar.gz`, 12 concept articles patched (collapsed `(_raw_Elonm2-2)+` → empty in wikilinks: ai-perception, ava-assistant, investor-research, jackson-outreach, lucas, medical-21, spotify, voice-detector, email-campaigns, jetson-orin, openclaw-vps, knowledge-base — `.bak-cleanup-*` siblings preserved), all 5,596 corrupted files deleted, every referenced one had a clean-named twin already present. Step 4 backlog: 1317 sessions to process, ~22 s/session via Ollama qwen3:8b → ~8 hours, will drain across cron ticks.
**Follow-up still open:** Mac's `harvest.sh` is the upstream source — it shouldn't be scanning `~/Documents/naware-knowledge1/` in the first place. Current scan dirs (`~/Documents/ClaudeOS Projects,~/Documents/remember,~/marvin,/Volumes/marvin-projects`) don't include the vault explicitly, but something is still pulling vault content into the harvest output. Investigate `harvest.sh` exclude logic on Mac next session.
**Status:** Active — fix applied, cleanup done, recurrence blocked at reorganize layer.

### 2026-04-27 - Qwen3.6-27B-FP8 in vLLM as Single-Model Test on GB10
**Decision:** Run Qwen3.6-27B (released 2026-04-22, Apache 2.0, 27B dense) as the single-model test on sparky's GB10 via vLLM, FP8 quantized, port 8001 bound to 127.0.0.1 only. Reuse the `vllm-nemotron-spark:local` image (already sm_121-patched). Skip NVFP4 because of known ARM64 GB10 crash bugs (vLLM #35519, #38980). FP8 dodges those bugs entirely.
**Context:** Qwen3.6-27B beats the prior 397B Qwen3.5 on coding benchmarks and matches Claude 4.5 Opus on Terminal-Bench. The qwen3-boysen LoRA finetune (built 2026-04-27 against Qwen3-8B) is incompatible with the new base — adapter weights are dimension-specific. Picking Qwen3.6-27B means the email-voice work is shelved unless re-finetuned against the new base. 535 Gmail pairs likely insufficient for a noticeable voice transfer on a 27B model anyway. Cold start ~52 min, throughput ~8 tok/s (well below forum's 20 — image likely needs Qwen-specific tuning).
**Status:** Active — test phase. No systemd, no auto-restart. Promote only if it earns its keep against current Ollama qwen3.5:9b draft path.

### 2026-04-15 - Nemotron 3 Super via vLLM NVFP4, Not Ollama
**Decision:** Run Nemotron 3 Super on the ASUS GB10 via vLLM with NVFP4 quantization (~65 tok/s), not Ollama (~17 tok/s). Qwen 3.5 9B stays on Ollama. Both fit in 128GB simultaneously.
**Context:** NVIDIA officially supports vLLM + NVFP4 as the DGX Spark deployment path. NIM containers are broken (NGC entitlement bugs), llama.cpp has Mamba-2 architecture bugs making it unusable. vLLM gives 4x throughput and uses less memory (~60-70GB vs ~94GB).
**Status:** Active — orchestrator `models.py` needs OpenAI-compatible client path for vLLM before deploy

### 2026-04-15 - Instantly Polling Over Tailscale, No Public Endpoints
**Decision:** Poll Instantly API every 60s for new replies instead of receiving webhooks. No Cloudflare Tunnel, no Tailscale Funnel.
**Context:** Webhook ingestion requires a publicly reachable URL. Tailscale Funnel is unfamiliar territory and user preferred to keep everything inside the existing Tailscale mesh. 60-second polling delay is irrelevant for email workflows.
**Status:** Active — `poller.py` built and wired into orchestrator

### 2026-04-15 - Frozen Flag Pattern for Hand-Merged Wiki Concepts
**Decision:** Hand-consolidated master concept articles must carry `frozen: true` in their YAML frontmatter. Both `compile-projects.py` and `ollama-update-concept.py` now check the first 40 lines for that flag and skip matching files.
**Context:** Before this, manual merges were silently clobbered on every hourly cron sync because the compilers unconditionally rewrote `concepts/*.md`. Consolidating `marvin` + `marvin-integrations` + `marvin-mcp-ecosystem` into one master and then watching Ollama undo it drove the fix. Pattern: add `frozen: true` to the master, delete the fragment files, and move each fragment's `raw/projects/{slug}/` contents into the master's raw dir so future sessions still feed the master rather than regenerating the fragments.
**Status:** Active — `concepts/marvin.md` and `concepts/image-repositories.md` are the first frozen masters.

### 2026-04-15 - Claude Desktop Conversations Must Land in `raw/harvested/claude-desktop/sessions/`
**Decision:** The Claude Desktop harvester will emit per-conversation markdown under a `sessions/` subdirectory, not flat at the machine root.
**Context:** First run dumped 342 flat files at `raw/harvested/claude-desktop/*.md`. `reorganize.py` treats every top-level file under a machine dir as its own project, so 349 bogus single-session project dirs showed up in `raw/projects/`. A `sessions/` subdirectory puts the files on the session reorganization path, letting the linker classify them into real projects.
**Status:** Pending implementation — harvester patch + cleanup of the 349 bogus project dirs.

### 2026-04-09 - Consolidate Wiki Articles Into Single Master Docs
**Decision:** When the LLM compiler generates many small concept articles from related sessions, consolidate them into one comprehensive master document per project/pipeline rather than keeping fragmented articles.
**Context:** The initial compile of 7 session logs produced 17 articles (15 concepts + 2 connections) that were all slices of the same weed detection pipeline story. Navigating 17 interlinked fragments was harder than reading one well-organized doc. The master doc absorbed all unique content — key insights, diminishing returns analysis, file paths, per-species data — and the fragments were deleted.
**Status:** Active — apply this pattern for future knowledge base compilation

### 2026-04-17 - Hub-Concept Template for Multi-Session Topics
**Decision:** Long-running topics (≥5 sessions OR ≥3 artifacts OR >4 weeks active) use a dense reference-style hub template at `.compiler/hub-concept-template.md`, not the narrative `concept-template.md`. Required sections: Current Production State callout with absolute paths, Artifact Registry (tables), Workflow (numbered how-to procedures), Key Decisions (dated one-liners), **Insights** (cross-cutting truths — distinct from decisions and session outcomes), chronological Session Log table (≤120-char one-liners, bold milestones), Known Issues. All hubs carry `frozen: true`.
**Context:** Initial compiler produced 1,000+ line narrative dumps per concept that were unusable for lookup. Pilot on `ai-pipeline` (1,695 → 332 lines) proved the format. Rolled out to 17 more hubs on the same day. Guardrails at `.compiler/hub-concept-guardrails.md` separate Insight vs. Decision vs. Session Outcome so they don't collide.
**Status:** Active — 19 frozen hubs live in `concepts/`.

### 2026-04-17 - Redirect Stubs Over Deletes for Merged Fragments
**Decision:** When consolidating fragment concepts into a hub, replace the fragment `concepts/*.md` with a small (~15-line) redirect stub carrying `frozen: true` and a wikilink to the canonical concept. Do NOT rely on filesystem deletes.
**Context:** Obsidian Sync on sparky resurrected three deleted fragment files (`marvin-integrations.md`, `steam.md`, initial `ai-pipeline.md`) by pulling old cloud versions from the Mac vault on startup. Stubs are cheap, Sync-safe (a resurrected stub is still a correct pointer), and give the graph a clear "merged into X" node. Pattern documented in guardrails.
**Status:** Active — live stubs: `worker` → `naware-website`, `electronics` → `lucas`, `marvin-integrations` → `marvin`, `steam` → `lucas`.

### 2026-04-17 - vLLM Nemotron Persistence via systemd Type=exec
**Decision:** Persistent vLLM Nemotron on sparky runs under `systemd Type=exec` that owns a foreground `docker run --rm` (not `--restart=unless-stopped` which fights systemd). Container name `vllm-nemotron`, reuses the verify script's HF cache at `~/.cache/hf-test`, scoped HF token at `~/.config/vllm-nemotron/hf-token` (mode 600). TimeoutStartSec=2700 (45 min cold load), Restart=on-failure with StartLimitBurst=5.
**Context:** NVIDIA's DGX Spark Deployment Guide runs vLLM in Docker with `--rm`. For production we want systemd journal integration, graceful shutdown, and restart logic. Type=exec with foreground docker (no `-d`) gives systemd ownership. Drafted but NOT installed — `/etc/systemd/system/vllm-nemotron.service` install is gated on verify script passing `/v1/models` + smoke test.
**Status:** Pending install — awaits model-load completion.

### 2026-04-20 - Mailbox Rename Pattern: Alternate mark/kelsey/sude Across 49 Slots
**Decision:** All 294 inboxing.com mailboxes (6 domains × 49) get renamed to cycle through `mark`, `kelsey`, `sude` with `.a`/`.b`/`.c` suffix on repeats. Slot 1 = `mark`, 2 = `kelsey`, 3 = `sude`, 4 = `mark.a`, ... 49 = `mark.p`. Yields 17 Mark / 16 Kelsey / 16 Sude per domain. DisplayName matches identity (Mark Boysen / Kelsey Escher / Sude Chagarlamudi).
**Context:** Current state is all 294 mailboxes are `markboysen.a..z` + `boysenmark.a..w` under a single "Mark Boysen" identity. Mixing three sender identities improves deliverability across 6 sending domains. Alternating (not blocked) so spam filters don't see 17 `mark*` in a row.
**Status:** Active — CSVs generated; awaiting DevTools capture to drive the bulk rename.

### 2026-04-20 - Public inboxing.com API is Read-Only for Mailboxes
**Decision:** Accept that the inboxing.com v2 public API (`X-API-Key` auth) does not expose mailbox-level writes. Bulk rename happens through the dashboard GUI's internal endpoint (to be captured via DevTools), or via Microsoft Graph directly against the backing M365 tenants.
**Context:** Probed all reasonable paths (`/domains/{id}/mailboxes`, `/mailboxes`, `/users`, `/accounts`, PATCH/PUT/POST variants) — all HTML 404 on the public surface. Dashboard rename queues an "M365 update job" per the UI note. Inboxing.com support confirmed GUI renames work; API rename is not documented.
**Status:** Active — next step is capturing the dashboard's internal call via Network tab, then replicating in a script.

### 2026-05-21 - AI Precise: stable camera udev symlink, not hardcoded /dev/video0
**Decision:** The DJI Osmo Action 4 gets a udev rule (`deploy/udev/99-ai-precise-cam.rules`) matching USB vendor:product `2ca3:0023` + `ATTR{index}==0` (the UVC capture node, not the metadata node) → stable `/dev/ai-precise-cam` symlink. The systemd unit maps it `--device /dev/ai-precise-cam:/dev/video0`, so container code and `config.yaml` keep referring to `/dev/video0` unchanged. `install.sh` step `[9b/13]` installs the rule.
**Context:** The camera reshuffles its kernel `videoN` number on every USB hot-plug, breaking the unit's hardcoded `--device /dev/video0` and putting the service in an exit-127 restart loop. An interim manual `ln -sf /dev/video1 /dev/video0` made it worse — it replaced the kernel char device with a symlink chain that resolved to the UVC *metadata* node; recovery was `rm /dev/video0 && udevadm trigger --action=add`. Confidence-threshold gate verified live the same session (PATCH 0.1→0.5→0.9 sweep), confirming label-cycling = argmax-over-noise on out-of-distribution scenes; production mitigation is `global_min_confidence ≥ 0.5`.
**Status:** Active — committed `ai-precise@00d12c3`, applied live on paige_springs (now `mark@192.168.3.38`).

### 2026-05-19 - AI Precise → JetPack 6: igpu base, GPU-native rfdetr (fixed silent-CPU bug)
**Decision:** Rebase the rfdetr Phase-1 image from `nvcr l4t-base`+PyPI-torch to `nvcr.io/nvidia/pytorch:25.03-py3-igpu` (JP6/L4T R36, bundled CUDA torch, py3.12). Drop the uv standalone-py3.10 shim. Install rfdetr under a constraint pinning the bundled torch/vision/numpy + a build-time `'nv' in torch.__version__` tripwire so `pip install rfdetr` can't silently swap in a CPU wheel. build.sh variant-aware (IGPU_TAG vs L4T_TAG).
**Context:** The old l4t-base path shipped CPU-only torch on EVERY JetPack — a latent bug, not a JP5 limitation. The whole Orin fleet (incl. the reflashed paige_springs box, now mark@192.168.1.254/JP6.2) runs JP6 where CUDA torch + rfdetr coexist natively. Validated on the AGX Orin: torch nv / cuda True / device Orin; full pipeline live with the DJI Osmo.
**Status:** Active — `mboysen/ai-precise@e844713`, version `d172c3f` (1.0.0), 347 tests.

### 2026-05-19 - Plain-torch RF-DETR meets real-time on Orin; TRT kept, decision deferred
**Decision:** Plain-torch RF-DETR is the production runtime path. The TRT-engine variant is kept as a documented optional perf fallback; final keep/drop/harden call deferred until after real-world field tests.
**Context:** Sustained 500-real-frame Orin benchmark: baseline 124 ms (~8 FPS), `optimize_for_inference()` 99 ms (~10 FPS), p99 ≪ the 320 ms architecture budget (~3×), very stable. Research had assumed TRT was required for real-time — disproven. Filed risk: factory silently prefers `weights.engine`→unvalidated TRTDetector over `checkpoint.pt` (harden to opt-in or drop at revisit). `optimize_for_inference()` (free ~25%) not yet enabled — field-test the baseline image first. See memory `project_ai_precise_rfdetr_orin_perf`.
**Status:** Active — deferred decision.

### 2026-05-19 - AI Precise deploy: parameterized, multi-machine, image is the atomic unit
**Decision:** Deploy tooling fully parameterized (`deploy.env`+`_common.sh`, per-box `machines/<name>.env`, `fleet.sh`) — no hardcoded host/user/path/model; data root derived from target user home. The container image (app baked in) is the atomic deploy/rollback unit; only the model + site `config.yaml` deploy separately (bind-mounted via `stage-model.sh`/`stage-config.sh`). 47-test deploy suite + CI guard it.
**Context:** The box reflash silently broke the hardcoded toolchain; operator wants a clean scriptable multi-machine process. App-in-image chosen over decoupling (atomic rollback per load-tarball.sh / v2.1 §4.15 outweighs iteration speed).
**Status:** Active.

### 2026-06-12 - Bind mount instead of copy for SMB capture sharing
**Decision:** Expose paige recordings over SMB via `mount --bind /mnt/nvme/paige_springs/recordings /mnt/nvme/captures/2026-06-12` rather than duplicating 2.1G into the share or loosening samba symlink policy (`wide links`).
**Context:** Recordings live outside the share root; symlinks are blocked by default samba config; copy wastes NVMe. Bind mount is zero-copy and serves files natively. NOT persistent — needs fstab entry if it should survive paige reboots.
**Status:** Active

### 2026-06-12 - Frame-extraction transfer instead of full-video rsync over DERP
**Decision:** For paige→sparky analysis, extract sampled frames on paige and transfer jpgs (~114M) instead of full videos (2.1G); analyze/infer on frames.
**Context:** Tailscale paige↔sparky is DERP-relayed at ~200KB/s (paige on WiFi+battery, no direct path) — full videos = ~3h. Color profiling and v4a detection sweeps only need sampled frames.
**Status:** Active

### 2026-06-12 - ZED color tuning: fix at ISP, gains-only software fallback
**Decision:** Correct the ZED color mismatch at the camera ISP (WB ~3800K, exp-comp 45, gamma sweep, analog-gain cap 1000–4000 native) rather than shipping the derived `correct.py` transform; if a residual software correction is ever needed, gains-only LUT in the host bridge (not the container).
**Context:** Analysis showed WB 4600K → red +41% hot, costing ~40% of detections; but the full software transform's gamma+sat push created degenerate whole-frame Ragweed boxes (14/29 recovered dets). Color is NOT the dominant gap — optics/scene under investigation next. Report: sparky:~/paige_captures/2026-06-12/analysis/TUNING_PLAN.md.
**Status:** Active — ISP changes NOT yet applied on paige

### 2026-06-12 - ZED WB knob inverted; color exonerated as main detection gap
**Decision:** WB staged at 6500K (knob ceiling; higher K = less red on this SDK — opposite of plan assumption). No software color LUT will ship — offline re-eval showed corrections recover ~nothing legitimate and the gains themselves cause degenerate whole-frame Ragweed boxes. Focus shifts to optics/sharpness (gain cap now truly active) + same-scene field validation.
**Context:** report_v2.md: raw 0.061 vs best-corrected 0.083 vs DJI 0.217 dets/fr @0.3 excl. degenerate. Safety net if correction ever ships: reject boxes >35% frame area.
**Status:** Active

### 2026-06-12 - Zero ZED weed detections root-caused to aim/focus/scene, NOT color
**Decision:** Stop treating ZED weed-detection failure as a color/ISP problem. Visual inspection of today's frames shows the cause is camera aim (not nadir), defocus, and out-of-domain scene (pasture/roadside grass, not row crop). Next field test fixes aim + focus + in-domain scene FIRST; the ISP profile (already live) is settled and was never the cause.
**Context:** 7 frames across all 6 clips: sky/horizon/oblique framing, ~3/7 out of focus, handheld bring-up perspective (person's shoulder visible). v4a is a row-crop broadleaf model; its classes are absent. Whole-frame Ragweed boxes were OOD hallucinations. Report: GROUND_TRUTH_FINDING.md. Supersedes the 2026-06-12 "color exonerated, optics suspected" decision with a sharper root cause.
**Status:** Active

### 2026-06-13 - ZED X One blur ruled PHYSICAL (focus+aim), software driver bug excluded
**Decision:** Do not chase the ZED X driver/denoiser for paige's soft frames. Treat blur as a physical lens-focus + camera-aim problem and fix it at the rig (manual M12 focus + nadir aim), using a Laplacian focus-assist.
**Context:** Known JP6.1/6.2 denoiser bug fixed in driver v1.3.1; paige runs zedlink-duo 1.4.2 (past the fix) and our sharpness sweep showed DENOISING adjusts proportionally (would be stuck-blurring if the bug were active). Playbook + version evidence in analysis/ZEDXONE_FOCUS_SHARPNESS_PLAYBOOK.md; memory zedxone-sharpness-focus-playbook. Revisit only if a future capture shows uniform blur the DENOISING knob can't change.
**Status:** Active

### 2026-06-13 - v4a is out-of-domain for the paige scene; don't judge the sensor by v4a counts
**Decision:** Treat paige (pasture / dry roadside grass) as out-of-domain for the v4a 12-class cotton-weed model. Do not use v4a detection counts to judge the ZED camera or its tuning. Path forward is an in-domain capture (real crop with target weeds) or paige-specific data collection + fine-tune — not more camera tuning.
**Context:** v4a overlaps only ~4/12 species with lawn/turf; paige's scene matches neither cotton nor turf. Today's ~zero detections were caused by aim/focus/domain, and "color recovery" detections were OOD hallucinations. Reinforces the standing "domain gap is THE bottleneck" finding. Recorded in weed_detection/HANDOFF.md + LESSONS_LEARNED.md (2026-06-13 entry).
**Status:** Active

### 2026-06-13 - Controllino: DO6/DO7 solenoid remap, DO6 dead → use DO7, ai-precise disabled
**Decision:** Remapped firing firmware so nozzle 6→DO6 (pin 8), nozzle 7→DO7 (pin 9), and E-stop→DO0 (pin 2); reflashed. DO6 is damaged (ACKs but won't actuate — likely blown high-side FET / missing flyback diode) → abandon DO6, use DO7 (fires reliably). ai-precise left STOPPED + DISABLED to stop it false-firing on out-of-domain v4a detections.
**Context:** User's hardware uses DO6/DO7 as 12V solenoid outputs and DO0 as the E-stop, but the firmware had pin 8 (DO6) as the E-stop input and fired via relays R0-R15. The mismatch explained the "D06 stuck at 24V" (pin 8 was an INPUT under the old map). Config backups: config.h.bak.pinmap (relay map), config.h.bak.do6do7 (DO map). Watchdog latch + reset-button behavior in [[controllino-watchdog-latch]]; direct fire tool in [[controllino-fire-tool]].
**Status:** Active
